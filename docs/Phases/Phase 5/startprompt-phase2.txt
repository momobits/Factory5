  ▎ Read CLAUDE.md, then docs/PROGRESS.md (latest two entries cover Phase 1 + pre-Phase-2 polish), then
  ▎ docs/ARCHITECTURE.md, then docs/decisions/0006-phase-1-inline-pipeline-single-shot.md. Phase 1 is verified — pnpm
  ▎ build / test / lint / format:check all green (85 tests); factory doctor round-trips against live claude-cli; factory
  ▎ init / resume / build / status / doctor all wired. The one deliberate scope cut in Phase 1 is ADR 0006: workers
  ▎ currently make single-shot provider calls and agents can't write files. Phase 2 lifts that cut.
  ▎
  ▎ Proceed with Phase 2 as described in PROGRESS.md "Next session" section. Goal: factory build example produces a
  ▎ runnable Python CLI + pytest-green. Order:
  ▎
  ▎   1. Allocate per-task git worktrees at <project>/.factory/worktrees/task-<id>/ via simple-git (init the project
  ▎      repo first if it's not one). Plumb `worktreePath` through `Task` — the core schema already has the field.
  ▎   2. Rewrite @factory5/worker/runWorker so it spawns `claude -p` inside the worktree with tools enabled
  ▎      (Read/Write/Edit/Bash/Glob/Grep) and --output-format stream-json. The current one-shot call path stays as
  ▎      the read-only agents (triage/architect/planner/reviewer/investigator/verifier); only scaffolder/builder/fixer
  ▎      switch to the tool-using subprocess path. Honor the existing AbortSignal plumbing.
  ▎   3. Upgrade ClaudeCliProvider.stream() from the minimum (delegates to call()) to real JSONL parsing of
  ▎      stream-json events — emit ProviderStreamChunk per assistant message delta, yield final usage on result.
  ▎      Reuse the kill-and-settle abort pattern already in call().
  ▎   4. Add a parallel worker-pool executor to the brain loop: topo-sort is already there, schedule independent
  ▎      ready-tasks concurrently up to a configurable limit (default min(4, cpu-count)). Heartbeat into
  ▎      state.tasks_inflight so stuck workers can be reaped.
  ▎   5. Run factory build example against live claude-cli; iterate on prompts, assessor gate thresholds, and
  ▎      worktree merge-back behavior until the example project has working Python modules, passing pytest, and a
  ▎      clean assessor report. Expect $5–20 spend on the first real end-to-end run (Opus architect + Sonnet
  ▎      planner + multiple Opus/Sonnet builder tasks).
  ▎
  ▎ After each step: pnpm build / test / lint / format:check. Update PROGRESS.md at session end and add an ADR for
  ▎ any non-obvious choice (tool whitelist per agent, worktree cleanup policy on success vs failure, merge
  ▎ strategy, etc. — each is load-bearing enough to deserve one). ADR 0006 should be updated to "Superseded by
  ▎ NNNN" once Phase 2 lands.
