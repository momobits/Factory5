  ▎ Read CLAUDE.md, then docs/PROGRESS.md — the latest two entries (Phase 3 infra + Pre-Phase-4
  ▎ outbound worker / channel config loading) are the ones that matter. Skim docs/ARCHITECTURE.md
  ▎ §Channels + §Event sources (both now phase-3 in the component table). Read ADRs 0005 (three
  ▎ autonomy modes + ask_user / escalate_blocked mid-flight tools), 0011 (pidfile), 0012 (brain
  ▎ in factoryd via supervised serve loop), 0013 (doorbell + 250ms polling fallback), 0014 (CLI-
  ▎ RPC transport — HTTP POST + SQLite polling, with a pluggable session listener). Skim
  ▎ CompleteArchitecture.md §9 (ChannelPlugin contract), §11 (autonomy modes + mid-flight tool
  ▎ semantics), and §12 (safety / anti-loop guardrails including the retry budget that
  ▎ escalate_blocked depends on). Phase 3 shipped the daemon infrastructure (168 tests green,
  ▎ e2e 9/9 using a stub provider). Pre-Phase-4 landed the outbound delivery worker
  ▎ (@factory5/daemon/outbound-worker.ts — polls outbound_messages, pushes through
  ▎ ChannelRegistry.send, wakes on doorbell 'outbound.new', maxAttempts cap) and the
  ▎ per-channel config block (`[channels.<id>]` in config.toml, validated lazily by each
  ▎ plugin's configSchema). What's missing before Discord can do real work: (a) brain-side
  ▎ ask_user / escalate_blocked tools that round-trip through pending_questions, (b) the
  ▎ DiscordChannel plugin itself, (c) `factory init` gaining a --discord-token flow, (d) live
  ▎ shakedown that the daemon-mode factory build and factory chat paths actually work end-to-end
  ▎ against a real Claude subscription (Phase 3's e2e only exercised the stub provider).
  ▎
  ▎ Proceed with Phase 4: turn the Discord channel into a first-class destination for directives
  ▎ and agent prompts. Goal: `factory daemon start` + a configured bot token ⇒ the daemon joins
  ▎ the Discord guild, a user mentioning the bot (or posting in a configured channel) creates a
  ▎ directive that runs under the brain, ask_user calls post threaded questions back into the
  ▎ originating channel and await a reply, escalate_blocked posts a structured "I'm stuck — here's
  ▎ what I tried" message. No silent looping. Order:
  ▎
  ▎   1. Live shakedown (don't skip this — Phase 3 only ran against a stub). `factory daemon start`
  ▎      in one terminal, `factory chat` in another — confirm the round-trip renders a real triage
  ▎      reply. Then `factory build example --autonomy assisted` in a third terminal — confirm
  ▎      the daemon claims it, runs the full pipeline (triage / architect / planner / pool /
  ▎      assessor) against live claude-cli, and the CLI's /status-polling loop renders progress.
  ▎      Expected spend $2-5 depending on planner output; stop early if anything behaves
  ▎      surprisingly (that's the whole point). Record any seams that broke — they're Phase 4
  ▎      bug fixes before Discord lands on top.
  ▎   2. ask_user / escalate_blocked brain tools. Extend @factory5/brain with two tool-shaped
  ▎      helpers that agents can call mid-run: `askUser({ directiveId, question, options?,
  ▎      deadline? })` writes a pending_questions row + enqueues an outbound_message to the
  ▎      originating channel (targetRef = directive.channelRef), then polls pending_questions
  ▎      for the answer. `escalateBlocked({ directiveId, reason, attempted, suggestions })` ditto
  ▎      but for the dead-end case. Both survive a brain restart — resume rehydrates open
  ▎      questions. The pool / worker plumbing for "pause the task until an answer arrives"
  ▎      needs threading — don't skip writing an ADR on that (how does a worker suspend? is it
  ▎      the worker's subprocess that blocks, or does the brain checkpoint + replan after reply?
  ▎      the latter is cleaner given the worktree model).
  ▎   3. DiscordChannel plugin — @factory5/channels/discord via discord.js. configSchema covers
  ▎      { token, applicationId, guildId?, defaultChannelId?, allowedUserIds? }. start() logs
  ▎      in, registers a message handler that calls ctx.onInbound with a normalized Directive
  ▎      (source='discord', channelRef=<channelId>#<threadId-or-messageId>, intent='chat' for
  ▎      mentions / configurable via a per-message /build prefix or slash command). send()
  ▎      posts into the target channel / thread, respects rate limits (discord.js handles this
  ▎      by default), returns { delivered, externalId }. Thread discipline: every directive
  ▎      creates its own thread so cross-directive messages don't interleave. File attachments
  ▎      out of scope for Phase 4 — flag it as a capability: false.
  ▎   4. `factory init --discord-token <token> [--discord-application-id <id>] [--discord-guild
  ▎      <id>] [--discord-default-channel <id>]` writes the values under [channels.discord] in
  ▎      config.toml. Daemon auto-registers the plugin when the block is present. Also add
  ▎      `factory doctor` probes for Discord reachability (bot token valid? guild accessible?).
  ▎   5. End-to-end validation. Extend scripts/e2e-daemon.ts with a --discord flag that uses a
  ▎      mock discord.js transport (or just skip if no token is available) so CI can still run
  ▎      the path. Separately, a live smoke in a private guild: bot joins, user posts "build me
  ▎      a Python weather CLI", directive spawns a thread, architect asks a clarifying
  ▎      question (ask_user), user replies, build completes and posts the summary + final
  ▎      status. Record spend in PROGRESS.md.
  ▎
  ▎ After each step: pnpm build / test / lint / format:check. Update PROGRESS.md at session end
  ▎ and add ADRs for any non-obvious choice (how ask_user pauses a task, Discord thread lifecycle
  ▎ vs channel lifecycle, whether `factory build --discord-channel <id>` exists as a shortcut
  ▎ when you want a build to post status to a specific channel without opening a chat first).
  ▎ Phase 4 does NOT cover GitHub events (Phase 5), Telegram, or web UI. If you touch anything
  ▎ outside @factory5/channels, @factory5/brain (new tools), @factory5/cli (new flags), or
  ▎ @factory5/daemon (wiring the Discord plugin through the existing registry), stop and justify
  ▎ it in the session — the channel registry contract from Phase 3 should absorb Discord cleanly.
  ▎ Start this in a fresh conversation; the Phase 3 + Pre-Phase-4 context window is long and the
  ▎ Phase 4 scope reads cleanest from a clean slate.
