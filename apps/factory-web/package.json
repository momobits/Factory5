{
  "name": "factory-web",
  "version": "0.0.1",
  "private": true,
  "description": "factory5 web UI — Astro static bundle served by factoryd under /app (ADR 0025)",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "astro dev --port 4321 --host 127.0.0.1",
    "build": "astro build",
    "preview": "astro preview --port 4321 --host 127.0.0.1",
    "typecheck": "astro check"
  },
  "dependencies": {
    "@factory5/core": "workspace:*",
    "@factory5/ipc": "workspace:*",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.5.0"
  }
}
