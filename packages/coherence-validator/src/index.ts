/**
 * @factory5/coherence-validator — entry point.
 *
 * Validates the project's knowledge graph. Phase A ships schema +
 * reference integrity checks; Phase B adds doc-fiction + dead-code.
 *
 * Re-exports `validateKnowledgeGraph` once `validator.ts` is implemented
 * in Task 16. For Task 13's scaffold, the index re-exports nothing
 * meaningful — Tasks 14/15/16 add the real modules.
 */

export {};
