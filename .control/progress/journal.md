# Journal

Append-only, newest on top. One entry per session, short. Minor fixes land here as one-line entries (see Issue flow in `.control/PROJECT_PROTOCOL.md`).

## 2026-04-26 (session `2026-04-26T13`) — Phase 11 backend triplet (11.1 → 11.4)

- **Sub-steps closed:** 11.1 (ADR 0027), 11.2 (answer route), 11.3 (build route), 11.4 (budget defaults route + project-tier resolution). Phase 11 now 4/7 closed; SPA + live val + close carry to next session.
- **Commit range (`ea2f21c..3231c5c`):** `ea2f21c docs(11.1)` ADR 0027 → `2557800 docs(11.1)` prettier reflow → `1a1af0e feat(11.2)` answer route → `dcaa0a3 feat(11.3)` build route → `3231c5c feat(11.4)` budget route. Session-end `docs(state)` commit lands on top.
- **Decisions:** one new ADR. **ADR 0027** (Accepted, ~280 lines) — Web UI mutation surface. Five sub-decisions in one ADR following the 0024/0025/0026 multi-part shape: (1) HTTP verbs/URLs per route — answer is action-on-resource (`POST …/:id/answer`, not partial PUT), build is top-level collection (`POST /api/v1/builds`, not nested under project; mirrors `factory build <name>`), budget is full-doc PUT (not PATCH; rejected the `{maxUsd:null}` ambiguity). (2) Idempotency — answer same-payload no-op + different-payload 409 with original preserved; build NOT idempotent (each POST mints a new directive; SPA submit-disable handles double-click); budget naturally idempotent via PUT replacement. (3) Error envelope — reuse existing `ipcErrorSchema` (`{error:{code,message,details?}}`); pinned six new mutation codes (`QUESTION_ALREADY_ANSWERED_DIFFERENTLY`, `PROJECT_NOT_FOUND`, `PROJECT_PATH_UNREADABLE`, `PROJECT_METADATA_CORRUPT`, plus the existing `QUESTION_NOT_FOUND` reused on the mutation side). Considered + rejected separate codes for `QUESTION_ANSWER_EMPTY` / `INVALID_LANGUAGE` / `INVALID_BUDGET` — those collapse into `SCHEMA_VALIDATION_FAILED` via Zod. (4) `metadata.budgetDefaults` mirrors `directiveLimitsSchema`, lives under ADR 0021's `metadata` extension point — same slot as 10.8's `metadata.language` (no schema migration, no top-level promotion). (5) Auth = same `FACTORY5_UI_TOKEN` bearer as reads; no weaker/stronger check on mutations; CSRF out of scope per loopback-only design (ADR 0025).
- **Refactors landed in service of the routes:**
  - `languageFromProjectMeta` + new `ProjectLanguage` type moved from `packages/cli/src/commands/build.ts` to `packages/wiki/src/project-metadata.ts` so both CLI and daemon read the same helper. CLI test file updated to import from `@factory5/wiki`. (11.3.)
  - New `projectBudgetDefaultsSchema` + `ProjectBudgetDefaults` type in `@factory5/core`. (11.4.)
  - New `budgetDefaultsFromProjectMeta` (read; mirrors `languageFromProjectMeta`) + `updateProjectMetadata` (atomic read-modify-write cycle for any `metadata.*` writer; reuses the existing `writeFileAtomic` pattern) + `ProjectMetadataNotFoundError` (distinguishes "workspace path lost the file" 404 from corrupt-but-present 422) in `@factory5/wiki`. (11.4.)
  - `InflightTask` type re-exported from `@factory5/state` index (used by daemon test for the orphan-task path; matches the existing `FindingsRegistryEntry` / `SpendFilter` re-export pattern). (11.2.)
  - Build resolution upgraded from two-tier to three-tier in both code paths: CLI (`packages/cli/src/commands/build.ts`) gets `flag → project metadata → config`; daemon (`POST /api/v1/builds`) gets `body → project metadata` (config-tier is CLI-only since the daemon doesn't load brain config). Per-field independent so a body-supplied `maxUsd` doesn't flush the project's stored `maxSteps`.
- **Issues:** none opened, none closed. I009 (carry-forward) becomes more pointed after 11.4 — Telegram inbound now skips two tiers instead of one; the right fix is the shared `resolveDirectiveLimits` extraction.
- **Minor fixes:** none. Each sub-step's fixes were in-scope and bundled into the sub-step's own commit.
- **Significant blockers hit:** one — `packages/daemon/src/pidfile.test.ts > pidfile > reaps a stale pidfile (dead owner)` flaked once under parallel test load on Windows. Passed in isolation and on retry. Pre-existing intermittent unrelated to 11.x; flagged for future awareness, no action taken.
- **Tests: 707 green** across 14 packages (666 → +41 this session arc: daemon +32 — 9 from 11.2 + 11 from 11.3 + 12 from 11.4; wiki +9 from 11.4 helpers). `pnpm lint` + `pnpm format:check` clean (after a prettier `--write` pass on each commit's narrow tables and long-string ternaries; same pattern as Phase 10 close). `pnpm build` clean across 14 packages + 3 apps.
- **Spend: $0** — all TS / Markdown / schema work; no LLM calls. 11.6 will be the first spend this phase (cheap — reuses an existing project for the live build smoke).
- **Memory:** new feedback memory `feedback_use_frontend_dev_skill.md` — standing rule that the operator wants the `frontend-dev` plugin/skill invoked BEFORE hand-rolling Astro markup or form designs in this repo (not just for 11.5; applies to any future factory5 SPA work). MEMORY.md index updated.
- **Steps.md note:** the 11.5 entry in `.control/phases/phase-11-web-ui-9b/steps.md` got an inline "**Note for next session:**" block reinforcing the `frontend-dev`-first rule.
- **Non-trivial finding:** the third-tier budget resolution turned out to be a one-line `??`-chain change in the CLI but a slightly larger insertion in the daemon (which had a single-tier `body.limits` lift before 11.4). Per-field independence — `body.limits?.maxUsd ?? projectDefaults?.maxUsd` for each field separately rather than `body.limits ?? projectDefaults` — was load-bearing in the daemon test "body.limits per-field overrides project-tier defaults"; the wholesale `??` would have flushed the project's `maxSteps` whenever the operator passed any `limits` object.
- **Next: 11.5 — SPA write affordances.** With the standing rule to invoke `frontend-dev` first. Then 11.6 (live val) + 11.7 (phase close).

## 2026-04-26 (session `2026-04-26T09`, end) — stale-dist gotcha investigation + session-end docs

- **Commit:** `0df2b51 docs(state): clarify stale-dist gotcha needs design, not a one-liner` (single commit on top of `1351b2f` Phase 10 close). Session-end `docs(state)` lands on top.
- **Investigation:** the Phase-9-suggested one-line fix for the stale-dist gotcha (flip `packages/{daemon,ipc,state}/package.json` `main` from `dist/index.js` to `src/index.ts`) turns out to be **incompatible with the prod runtime path**. Raw `node apps/factoryd/dist/main.js` fails with `Cannot find module .../src/brain-supervisor.js` because raw node can't transpile `.js` extensions on `.ts` source files (tsx + vitest hook the loader and rewrite; raw node doesn't).
- **Investigation #2:** explored bundling workspace deps in `apps/*/tsup.config.ts` via `skipNodeModulesBundle: true` + `noExternal: [/^@factory5\//]`. Bundles built cleanly but the raw-node runtime then failed on transitive npm deps not declared in the app's own package.json (`Cannot find package 'commander'` from apps/factory; `'zod'` from apps/factoryd). pnpm doesn't hoist transitive deps to where bundled output expects them; apps would need to enumerate every transitive npm dep explicitly. Reverted.
- **Decisions:** none new. ADR count unchanged at 26.
- **Issues:** none opened or closed.
- **Net code change:** zero — both attempted fixes reverted clean. Only carry-forward language in STATE.md + next.md updated to reflect the empirical findings, so the next session sees what was tried and why each path needs design rather than a quick fix.
- **Recommendation logged in carry-forward:** real fixes are either (a) **conditional exports** (`exports.development → src`, `exports.default → dist`) plus tooling config (`tsx --conditions=development`, vitest `resolve.conditions`), or (b) **app-side bundling** with full transitive npm deps declared in app package.json. Both deserve their own substep when next prioritised.
- **Tests:** unchanged at 666 green. `pnpm lint` + `pnpm format:check` clean. `pnpm build` clean across all 14 packages + 3 apps.
- **Next: Phase 11 — 11.1 (ADR 0027 mutation route shape).** See `.control/progress/next.md`.

## 2026-04-26 (session `2026-04-26T09`) — Phase 10 closed (10.3 + 10.5 + 10.7 + 10.8 + 10.9 across 2 sessions)

- **Phase tag:** `phase-10-assessor-tier3-closed` applied in this commit. Phase 10 entirely shipped — Node / Go / Rust runtimes all live-validated against real `factory build` runs with `gate.verify=true`.
- **Commit range this session arc:** `50bab61 feat(10.3)` (Node live val + 3 fixes) → `503da4d feat(10.8)` (init picker) → `62ee979 feat(10.5)` (Go live val + parser fix + I014 filed) → `8be8dc0 feat(10.7)` (Rust live val) → this Phase 10 close commit.
- **Live runs (the actual ground truth Phase 10 was about):** Node (`log-totals`, $3.57, 14 vitest), Go (`go-line-counter`, $5.40 across two attempts, 34 go-test), Rust (`rust-csv-summary`, $1.98, 7 cargo-test). All three closed `gate.verify=true`. Total Phase-10 LLM spend: ~$10.95 across all live runs.
- **Bugs caught + fixed in-phase (4):** (1) `--language` flag threading gap — every pre-fix build defaulted to `'python'` because `loop.ts:assess()` call had no `runtime`. Threaded through `factory build` → `directive.payload.language` → `extractRuntime` → `assess({runtime})`. (2) **I013** (RESOLVED) — `git worktree remove --force` failed with "Directory not empty" on Windows when workers left `node_modules/` inside the worktree. `prePurgeDepDirs` rimrafs heavy dep dirs first. (3) `extractJsonObject` brace-counter mis-counted `{` inside string literals (architect responses with `{` inside markdown code blocks). Added string-state tracking with `\\` escape handling. (4) Go runtime parser missed PASS/FAIL because `go test ./...` default output is package-level only — fixed by `-v -count=1` (`-v` for per-test attribution, `-count=1` to bypass Go's test cache so the assessor sees fresh subprocess output).
- **Issues:** **I013** (worker/worktree, MEDIUM) opened at 10.3 — `prePurgeDepDirs` fix landed in same commit; should be moved from OPEN to RESOLVED in `docs/issues/INDEX.md` once a verifying live run is observed (Node run was green-after-fix in 10.3 third try, so the resolution is verified now). **I014** (brain/architect, MEDIUM) opened at 10.5 — architect re-running on existing project leaves `docs/knowledge/*.md` modifications uncommitted in main, dirty-tripping `gate.verify`; targeted fix is one git commit at end of `runArchitect`. Carry-forward.
- **10.8 init project mode:** `factory init <project> [--language python|node|go|rust]` scaffolds a new project under the workspace with a language-aware CLAUDE.md and writes `.factory/project.json` with `metadata.language`. `factory build` reads that as a fallback when no `--language` flag is given. `loadOrCreateProjectMetadata` gained an `initialMetadata` option (only honoured on first creation; existing identity stays sticky per ADR 0021).
- **Helper added:** `scripts/one-shot-assess.mjs` invokes `assess()` directly against a project path. Used to verify gate state after manual cleanup without re-running the full brain pipeline (~$0 vs. ~$3 for a full rebuild). Generally useful for any "rerun assess after a manual fix" workflow.
- **Tests: 666 green** across 14 packages (642 → +24 this session arc: brain +10 extract-json, worker +4 I013, cli +8 init/build, wiki +2 initialMetadata; also picked up the assessor.go test assertion update for the `-v -count=1` change). `pnpm lint` + `pnpm format:check` clean. `pnpm build` clean across 14 packages + 3 apps.
- **`CompleteArchitecture.md`:** §22 "Pluggable runtimes (Phase 10, added post-scaffold)" added — explains the four ADR-0026 sub-decisions, the per-runtime command mapping, the failure-mode taxonomy, and the language-resolution flow (`--language flag → project.json metadata.language → undefined`).
- **Phase 11 scaffold:** `.control/phases/phase-11-web-ui-9b/{README.md,steps.md}` created. Phase 11 picks up the deferred Phase 9 mutation surface — answer pending questions from the browser, kick off builds via UI, configure per-project budget defaults. 7 sub-steps planned (11.1 ADR 0027 → 11.7 phase close). Estimated 1–2 sessions.
- **Non-trivial finding:** the seam-only Phase 10.2 / 10.4 / 10.6 tests passed cleanly but missed three bugs that only showed up in live runs — the Go runtime parser missed PASS/FAIL because it was tested against `-v` style output but the gate command didn't pass `-v`. Tests that mock subprocess output cannot catch "we're calling the subprocess wrong" — that's a real-subprocess-coverage gap. The Node runtime had `node-e2e.test.ts` precisely for this reason; Go and Rust didn't. Worth adding similar e2e tests in a future session, now that go + cargo are on this dev machine. Tracked informally; not worth a dedicated issue file.
- **Next: Phase 11 — 11.1 (ADR 0027 mutation route shape).** See `.control/progress/next.md`.

## 2026-04-24 (session `2026-04-24T13`) — Phase 10 sub-steps 10.1 + 10.2 + 10.4 + 10.6 (ADR 0026 + Node / Go / Rust runtimes)

- **Commit range:** `d493ff9` (10.1) → `0563a85` (10.6), four substantive commits. Session-end `docs(state)` commit lands on top.
- **Decisions:** one new ADR. **ADR 0026** (Accepted, ~250 lines) — Pluggable assessor runtimes, four sub-decisions in one ADR. (1) Provisioner shape = two-shape contract — env-owning (Python keeps ADR 0017's `.factory/assessor-env/` lifecycle) vs. env-assuming (Node / Go / Rust trust project manifests). Rejected single polymorphic shape because Node's `node_modules` is not analogous to a Python venv and modelling it as one would force cargo-cult metadata. (2) Verify-gate command mapping per runtime pinned: Python `pytest`; Node `pnpm install → pnpm typecheck || tsc --noEmit → pnpm test`; Go `go build ./... + go test ./...` with `go test -list` no-test detection (closes the zero-test-projects-pass-the-gate loophole); Rust `cargo test` (build + test in one). (3) Failure-mode taxonomy: new optional `AssessResult.failureMode` enum `BUILD_FAILURE | TEST_FAILURE | ENV_SETUP_FAILURE | ENV_HOST_MISSING_TOOL` with top-down precedence. Deliberately on `AssessResult`, not on `Finding`, to avoid mutating a widely-consumed schema. (4) Host-tool pre-flight via `resolveOnPath` in `assess()` before any project-subprocess; each runtime declares `hostTools: HostTool[]`. Python's empty list is special-cased (its `pickPython` handles multi-bin fallback internally).
- **Issues:** none opened, none closed. Backlog carries I009 + I012 forward from Phase 8/9 — both non-blocking.
- **10.1 ADR (`d493ff9`):** `docs/decisions/0026-pluggable-runtime-contract.md` + INDEX row. Followed the multi-decision single-ADR shape from ADR 0020 / 0024 / 0025. Implementation outline at the bottom of the ADR became the verbatim type sketch for 10.2.
- **10.2 Node runtime (`34763dc`):** meaty commit — the type refactor that 10.4 / 10.6 built on. `packages/assessor/src/types.ts` gained `Runtime` / `FailureMode` / `HostTool` / `ProvisioningRecord` / `RuntimeGateResult` / `RuntimeAssessor`. `AssessResult.provisioning` shape migrated (pythonPath → toolPath, pythonVersion → toolVersion, venvSource → envSource); Python's internal `ProvisioningReport` kept intact to avoid churning the 8+ `pytest.test.ts` assertion sites (adapter converts at the `runtimes/python.ts` boundary). `AssessOptions.testFramework` narrowed from `'auto' | 'pytest' | 'none'` to `'auto' | 'none'` ('pytest' was never used by any caller). `assess.ts` rewritten: dispatch on `AssessOptions.runtime` (defaults to `'python'`), host-tool pre-flight, `computeGateResults(runtimeResult, modules, artifacts)` signature change (collapsed tests + imports + provisioning args into the single `Pick<RuntimeGateResult>` slice). `packages/assessor/src/runtimes/python.ts` — thin adapter over the existing `pytest.ts` + `imports.ts`. `packages/assessor/src/runtimes/node.ts` — env-assuming provisioner. `node.test.ts` (+15 seam) covers parseNodeTestSummary (vitest / jest / node:test TAP) + runGate green / ENV_SETUP / BUILD / TEST / ENV_HOST_MISSING / no-test-script / package.json-missing. **`test/node-e2e.test.ts` (+1 real)** seeds a minimal TS fixture into `os.tmpdir()` and runs the whole pipeline: real `pnpm install` (warm ~7 s, cold ~25 s), `pnpm typecheck`, `pnpm test` against vitest. Verified green on Windows with pnpm 9.12.0 + Node 22.22.2. Downstream: `scripts/reassess.ts` updated for the new provisioning field names; `pytest.test.ts`'s `computeGateResults + provisioning` block rewritten for the new signature. Tests 605 → 621.
- **10.4 Go runtime (`10f2132`):** mechanical follow-on. `runtimes/go.ts` — env-assuming, no preflight (`go build ./...` transparently fetches modules). `parseGoTestSummary` counts `--- PASS:` / `--- FAIL:` lines including indented subtests (initial regex was anchored with `^` and missed `    --- PASS: Parent/sub` — fixed with `line.trimStart()` in one iteration; test already in place). `countListedGoTests` skips package-footer lines (`ok`, `FAIL`, `?`) and counts test-name lines starting with Test/Benchmark/Example/Fuzz. `go test -list '.*'` as the no-test-detection gate so zero-test projects stop falsely passing `gate.integration`. `go.test.ts` (+12 seam) covers parsers + runGate scenarios. No e2e — `cargo`/`go` absent on this machine, and 10.5 live validation covers the ground-truth side. Registered in RUNTIMES map. Tests 621 → 633.
- **10.6 Rust runtime (`0563a85`):** most compact of the three — `cargo test` subsumes build and test, so the pipeline is a single subprocess. `parseCargoTestSummary` aggregates across multiple `test result:` lines (unit + integration + doc targets each emit one). `sawResult` flag distinguishes compile failure (no summary line, `BUILD_FAILURE`) from test failure (summary line with `failed > 0`, `TEST_FAILURE`). `rust.test.ts` (+9 seam). Registered in RUNTIMES map. Tests 633 → 642.
- **Tests: 642 green** across 14 packages (+37 from session start 605). `pnpm lint` + `pnpm format:check` clean after each commit. `pnpm build` clean. All 14 packages' tsup + dts outputs regenerate green.
- **Spend: $0** — all TS / docs work; no LLM calls. 10.3 live validation will be the first spend this phase.
- **Key implementation tradeoffs made in code (worth remembering):** (a) Kept Python's internal `ProvisioningReport` field names (pythonPath / venvSource) unchanged, mapping at the `runtimes/python.ts` adapter boundary — traded one small mapping function for ~8 avoided test-edit sites. (b) `AssessResult.failureMode` is `undefined` on green gates and on artifact-only failures (missing README etc); it only fires for the four assessor-cause categories. Artifact-only failures still flip `gate.verify = false` but UI should render them as separate from the four primary failure modes. (c) Host-tool pre-flight lives in `assess()` itself via `resolveOnPath` — runtime modules also carry a safety-net probe at the top of `runGate` to survive PATH mutation between pre-flight and call. (d) Four out of four new commits passed `pnpm format:check` only after a prettier `--write` pass; the narrow tables in ADR 0026 and some long-string ternaries in `assess.ts` logger calls required reflow.
- **Dependency changes:** none. No new packages added to any `package.json`.
- **Config files touched:** none beyond `.control/phases/phase-10-assessor-tier3/steps.md` (four checkbox flips) and `docs/decisions/INDEX.md` (ADR 0026 row).
- **Non-trivial finding:** real-subprocess Node e2e ran green on Windows + pnpm 9 + Node 22 — validates the `runSubprocess` path's cross-platform + `PATHEXT` handling end-to-end for the first time since Phase 5c. The prettier `--write` pass is doing real work on complex TS + Markdown; the CI workflow should either accept this or add `prettier --write` to the commit hook to save a manual roundtrip (deferred as a Phase 11+ nicety, not worth a dedicated commit).
- **Next: 10.3 live validation** (Node end-to-end via real `factory build`), then 10.5 / 10.7 after operator installs Go + Rust, then 10.8 init picker, then 10.9 phase close + tag.

## 2026-04-23 (session `2026-04-23T21`) — Phase 9 closed (9.9 live validation + 9.10 phase close); Phase 10 kicked off

- **Commit:** single close commit `e360436 chore(phase-9): close phase 9, kick off phase 10` on top of `3313030` (prior session-end docs-state). Tag `phase-9-web-ui-closed` applied on `e360436`. Session-end `docs(state)` commit lands on top.
- **9.9 live validation:** factoryd restarted clean after a stale-dist diagnosis + rebuild (see "Non-trivial finding" below). Server-side smoke of 14 `/api/v1/*` route variants all 200 with p50 ≈ 2.5 ms against the operator's ~5 MB `factory.db` (25 directives, 13 pending questions, $69.6 across 141 calls in 5 projects, 10+ findings). Auth negatives both 401 (`UI_AUTH_REQUIRED`). Operator then opened `http://127.0.0.1:25295/app/?t=<48-hex>` in a browser and clicked through all five pages: overview / directives / questions / spend / findings — all rendered with real data. Charter criterion "every page renders with real data + <100 ms p50 latency" met with ~40× headroom. Observations captured in `docs/Phase9_Progress.md` §Live validation.
- **9.10 phase close:** `docs/Phase9_Progress.md` authored (mirrors Phase 8 shape: scope table + ADR 0025 summary + 9.9 validation + non-trivial finding + tests at close + carry-forward + done-criteria checklist). `docs/PROGRESS.md` prepended with the Phase 9 close summary. `CompleteArchitecture.md` gained new §21 "Web UI" pointing at ADR 0025. `.control/phases/phase-9-web-ui/steps.md` 9.9 + 9.10 checkboxes flipped. Phase 10 scaffolded at `.control/phases/phase-10-assessor-tier3/{README.md,steps.md}` per the forward-queue pitch in Phase 9's README (Node / Go / Rust pluggable runtimes, ~2–3 sessions; ADR 0026 opens). STATE.md fully rewritten for Phase 10. `.control/progress/next.md` rewritten as the Phase 10 kickoff prompt.
- **Non-trivial finding (captured in `docs/Phase9_Progress.md`):** stale `packages/daemon/dist/index.js` tripped 404 on `/api/v1/spend` and `/api/v1/findings` on first factoryd restart, despite all daemon tests passing. Root cause: vitest resolves `.ts` source directly; `pnpm factoryd` imports `@factory5/daemon` via its `main: "./dist/index.js"` entry and picks up stale dist. Recommended remediation (deferred to Phase 10+): flip `packages/{daemon,ipc,state}/package.json` `main` from `"./dist/index.js"` to `"./src/index.ts"`. tsx transpiles on demand; prod `pnpm build` still produces dist/ for downstream consumers.
- **Tests: 605 green** (per STATE.md per-package sum — verified this close). `pnpm lint` + `pnpm format:check` clean. `pnpm build` clean (14 packages + 3 apps + factory-web dist).
- **Spend: $0** — close work is pure docs + scaffolding; no LLM calls.
- **Issues:** none opened, none closed. I009 (MEDIUM) + I012 (LOW) carry forward unchanged from Phase 8.
- **Phase 10 next:** sub-step 10.1 — author ADR 0026 (pluggable-runtime contract). Four sub-decisions pinned in `next.md`: provisioner shape, verify-gate command mapping, failure-mode taxonomy, host-tool pre-flight.

## 2026-04-23 (session `2026-04-23T20`) — Phase 9 sub-steps 9.1 → 9.8 (ADR 025 + scaffold + four read-side routes + SPA pages)

- **Commit range:** `f71840a` (9.1) → `5190f44` (9.8), eight substantive commits back-to-back. Docs-state commit lands after this entry.
- **Decisions:** one new ADR. **ADR 0025** (Accepted, ~200 lines) — Web UI architecture: four sub-decisions in one ADR per the multi-part shape established by ADR 0020. (1) Framework = Astro MPA + Islands + `<ClientRouter />` over Vite+React / lit-vanilla — best bundle-size + dev-loop tradeoff for a read-only dashboard, matches ecosystem defaults, escape hatch via Islands for any 9b interactive surface. (2) Auth = separate `FACTORY5_UI_TOKEN` minted at factoryd startup, distributed via `?t=<48-hex>` query → `sessionStorage`, scoped distinct from `FACTORY5_WORKER_AUTH_TOKEN` so a leaked dashboard token can't impersonate workers. (3) Bundle serving = `@fastify/static` under `/app/` in prod, Astro dev server with Vite `/api/v1` proxy in dev, no SSR adapter. (4) Routing = Astro file-based MPA with `<ClientRouter />`; `/api/v1/*` URL-prefix versioning; dynamic details use `?id=` query params rather than dynamic routes (keeps output fully static).
- **Issues:** none opened, none closed. Backlog carries I009 + I012 forward from Phase 8 — both still non-blocking.
- **9.1 ADR (`f71840a`):** `docs/decisions/0025-web-ui-architecture.md` + INDEX row. Builds on ADR 0012 (daemon Fastify), ADR 0014 (HTTP + polling transport), ADR 0024 (per-startup bearer pattern). ADR's Implementation notes section laid out the 9.2–9.10 sub-step mapping before any code landed — same pattern ADR 0024 used to de-risk Phase 8's sub-steps.
- **9.2 scaffold (`b0cbf53`):** new `apps/factory-web/` (Astro 5, `@astrojs/check`) picked up automatically by `apps/*` glob in `pnpm-workspace.yaml`. `astro.config.mjs` pins port 4321 + Vite proxy `/api/v1` → `http://127.0.0.1:25295`. `src/lib/api.ts` — bearer-aware `apiFetch<T>`, `captureTokenFromUrl` (catches `?t=` into sessionStorage and strips via `history.replaceState`), typed `NoTokenError` / `ApiError`. `src/layouts/Dashboard.astro` minimal shell; `src/pages/index.astro` smokes against `/api/v1/status`. `tsconfig.json` extends `astro/tsconfigs/strict` (browser-target TS diverges from the Node-target `tsconfig.base.json`). Config-level: `.gitignore` + `.prettierignore` exclude generated `.astro/`; `eslint.config.js` ignores `**/.astro/**` and relaxes `triple-slash-reference` for `**/*.d.ts` (Astro's scaffold convention for `env.d.ts`).
- **9.3 /app/\* static + /api/v1/status bearer gate (`930b7a1`):** `DaemonOptions.webUiStaticPath?` + `DaemonOptions.uiAuthToken?` added. `buildIpcServer` became `async` to `await app.register(fastifyStatic, ...)`; 11 test call sites updated to `await buildIpcServer(...)` via replace_all; `beforeEach` in `worker-ask-user-regression.test.ts` became `async` to accommodate. Renamed `checkWorkerBearer` → `checkBearer` (no signature change) so both bearer namespaces share the constant-time compare. New `GET /api/v1/status` returns same `StatusResponse` shape as IPC `/status`, bearer-gated at the handler (handler-level check mirrors `/worker/ask-user`; ADR described a preHandler — aesthetic only). `apps/factoryd/src/main.ts` mints `FACTORY5_UI_TOKEN` via `randomBytes(24).toString('hex')` + resolves web-ui dist via `resolveWebUiStaticPath()` that walks from `import.meta.url` to `apps/factory-web/dist`; logs `ui: http://127.0.0.1:25295/app/?t=<hex>` (or a fallback message with the token alone when the SPA bundle is missing). +13 daemon tests (5 for `/api/v1/status` paths, 4 for `/app/*` serve + 404 + loopback guard + auth-not-required).
- **9.4 /api/v1/directives (`9c2d10a`):** `directivesQ.listPaged({limit, offset, status?})` added to `@factory5/state` — single query helper with Math.max/min clamping to [1, 100]. New Zod schemas in `@factory5/ipc`: `apiV1DirectivesListQuerySchema`, `apiV1DirectivesListResponseSchema`, `apiV1DirectiveDetailResponseSchema`, `apiV1InflightTaskSchema` (inline to @ipc — its audience is the web UI, no need to promote to @core). Two routes: `GET /api/v1/directives` (paged list, `?limit=&offset=&status=`, server-side limit clamp) + `GET /api/v1/directives/:id` (directive + timeline of `tasks_inflight.listByDirective` + `openForDirective` + `{totalCostUsd, callCount}` rollup). Extracted `requireUiAuth(req, token)` helper — `/api/v1/status` now uses it too. +15 tests (6 state for listPaged, 9 daemon for list/detail happy + error paths).
- **9.5 /api/v1/pending-questions (`917f4a8`):** `pendingQuestions.listPaged({limit, offset, status?, directiveId?})` with `status='open'|'answered'|'all'`. First dedicated test file for the pending-questions module (`pending-questions.test.ts` +7 tests). Two routes: list with filters + `GET /api/v1/pending-questions/:id` envelope for outbound-message deep links. +14 tests (7 state, 7 daemon). **In-step gotcha:** FK `pending_questions.directive_id → directives(id)` forced seeding a directive before each question — `makeQuestion(db, overrides)` seeds a fresh directive when `overrides.directiveId` isn't supplied; test updated to call `seedDirective` explicitly when a shared id was needed.
- **9.6 /api/v1/spend (`a5ad4d0`):** single endpoint returning all four rollups (`perProject`, `perDirective`, `perDay`, `perModel`) in one response + echoed filter. No new query logic — reuses Phase 7b's spend helpers verbatim. Schemas + types hand-written in `@factory5/ipc` matching `@factory5/state`'s `PerProjectSpend` etc. interfaces. +5 daemon tests: 401, empty-db shape, rollup of two seeded usage rows, since/until echoed + excluded, 400 on non-ISO since.
- **9.7 /api/v1/findings (`6a29f2f`):** list-only endpoint (read-only per ADR 0025; mutation in 9b) with `severity`/`status`/`project`/`advisory`/`limit` filters matching the existing `findingsRegistry.list` `ListFilter`. Schema advisory coercion: `z.enum(['true','false']).transform(v => v === 'true').optional()` since query strings arrive as strings. +8 daemon tests covering each filter isolated + 400 on invalid severity value. **In-step gotcha:** initial test used `status: 'RESOLVED'` which tripped a SQLite CHECK constraint — valid statuses are `OPEN|FIXED|VERIFIED|WONTFIX`; fixed to use `VERIFIED`.
- **9.8 SPA pages (`5190f44`):** seven Astro pages wiring the four read-side APIs to UI. `src/layouts/Dashboard.astro` grows nav + `<ClientRouter />` + a minimal design language (`.cards`, `.card`, `.filter-form`, `.empty`, `.err`). `src/lib/api.ts` adds `loadInto<T>(mount, path, render)` helper that centralises the capture-token + token-missing + try/catch pattern every page would otherwise duplicate; `el()` + `fmtUsd()` utilities. Pages: `/app/` (overview summary cards), `/app/directives/` (paged list), `/app/directives/detail?id=<ulid>`, `/app/questions/` (defaults to status=open), `/app/questions/detail?id=<ulid>`, `/app/spend/` (four rollup tables + filter form), `/app/findings/` (severity/status/project/advisory filter form). **In-step gotchas:** (a) dynamic `[id].astro` routes require a server adapter under static output — swapped to query-param pattern (`/detail?id=<ulid>`) which stays fully static. (b) `<ViewTransitions />` deprecated in Astro 5 — replaced with `<ClientRouter />` per `astro check` hints.
- **Tests: 605 green** across 14 packages (+41 from Phase 8 close 564). Per-package: +13 state (9.4 + 9.5), +38 daemon (9.3 + 9.4 + 9.5 + 9.6 + 9.7). Every commit verified with full `pnpm test` + `pnpm build` + `pnpm lint` + `pnpm format:check` (`astro check` also clean on the Astro app post-ClientRouter swap). Workspace-wide build touches 14 packages + 3 apps + factory-web's 7 static pages.
- **Spend: $0** — all TS / SQL / HTML / Astro work; no LLM calls. 9.9 will need no LLM spend either (browser testing against existing factory.db).
- **Dependency changes:** `astro ^5.0.0` + `@astrojs/check ^0.9.0` added to new `apps/factory-web`; `@fastify/static ^7.0.0` added to `@factory5/daemon`. pnpm-lock.yaml grew +3.6 KLOC from Astro's transitive dep tree (305 packages added at 9.2).
- **Dev-loop gotcha (worth remembering):** `@factory5/ipc` and `@factory5/state` both resolve via `dist/` at runtime, so editing schemas in `src/` requires a `pnpm --filter @factory5/ipc --filter @factory5/state build` before dependent tests (daemon) pick up the new exports. Ran into this at 9.4 (TypeError: undefined.parse at runtime) and applied the rebuild pattern for every subsequent state/ipc edit.
- **Config files touched this session** (beyond per-sub-step source): `.gitignore` (+`.astro/`), `.prettierignore` (+`.astro/`), `eslint.config.js` (ignore `.astro/**` + relax triple-slash for `*.d.ts`), `pnpm-lock.yaml` (two bumps for Astro + fastify-static).
- **No tags placed this session.** Phase 9 still open; `phase-9-web-ui-closed` tag waits for 9.10.
- **Next: 9.9 live validation** — operator-at-browser smoke of every page + sub-100ms p50 latency. Then 9.10 phase close.

## 2026-04-23 — Phase 8 closed; Phase 9 (Web UI) kicked off

- Phase tagged `phase-8-worker-ask-user-closed` on the `chore(phase-8)` close commit. Phase 9 scaffolded at `.control/phases/phase-9-web-ui/` with 10-step plan.
- Sub-steps 8.6 → 8.8 closed this session on top of 8.1–8.5 from the earlier session: 8.6 regression suite (`34f4ab5`), `fix(8.7)` outbound drain spam (`6c9c5ce`), 8.7 live validation (`761034a`), and this phase-close commit.
- ADR 0024 validated live: directive `01KPX1Z4RE3535H8X55E169PHR` (Telegram-initiated), $2.579 spend over 7 LLM calls; builder MCP `ask_user` fired at 11:39:27 → outbound delivered to Telegram at 11:39:28.511Z; operator's Reply-feature answer matched `maybeAnswerPendingQuestion` and wrote to `pending_questions.answer`. Directive ended `blocked` (verifier's hallucinated findings + operator `abort`); primary ADR 0024 mechanism validated end-to-end.
- Four new issues surfaced during 8.7 live run (I009–I012): two resolved this phase (I010 WONTFIX, I011 RESOLVED via shared `resolveProjectPath` helper in `@factory5/wiki`), two OPEN (I009 Telegram budget defaults MEDIUM, I012 maybeAnswerPendingQuestion FIFO matcher LOW) — both post-close follow-ups.
- Tests: **564** across 14 packages, +93 from Phase 7 close baseline. `pnpm build` + `pnpm test` + `pnpm lint` + `pnpm format:check` all clean. 2026-04-23T13:00Z close.
- Code changes in Phase 8 close commit (beyond the Phase 9 scaffold): `docs/Phase8_Progress.md` authored; `docs/PROGRESS.md` entry appended; `CompleteArchitecture.md` §11 updated with inline pointer to ADR 0024; steps.md 8.8 flipped `[x]`; STATE.md + next.md updated for Phase 9 active.
- No new factory5 self-issues opened beyond I009/I010/I011/I012 which were filed during 8.7.
- Phase 9's first concrete work: sub-step 9.1 — author ADR 025 on web-UI architecture (framework / auth / bundle / routing).

## 2026-04-23 (session `2026-04-23T06`) — Phase 8 sub-steps 8.1 → 8.5 (charter + ADR + brain RPC + worker MCP + agent registry + lifecycle migration)

- **Commit range:** `0754a69` → `37c0605` (5 substantive commits + this session-end docs commit). **Phase step range:** 8.1 (charter open) → 8.5 (lifecycle migration + brain-restart recovery).
- **Decisions:** one new ADR. **ADR 0024** (Accepted, ~360 lines) — worker-subprocess `askUser`: reverses ADR 0015's Phase-4 deferral of Shape 1. Five sub-decisions in one ADR per the multi-part shape established by ADR 0020. (1) MCP route over direct-stdio JSON-RPC — Claude CLI's official extension path; sidesteps undocumented behaviour for unknown tool names; reusable for future custom tools. (2) `max_usd` and `max_steps` paused during human wait — operators who set `--max-usd 3` get exactly that LLM-spend ceiling; wall-clock waiting on a human is not LLM money. Per-question soft deadline default 1h (`[budget.askUser] deadlineSeconds`) bounds Claude CLI seat pinning. (3) `taskId` mandatory in worker→brain envelope — closes sibling-worker crossover hazard the brain-level path didn't have to address. (4) `tasks_inflight.status='waiting_for_human'` plus brain-startup orphan-cleanup via `recoverFromHumanWaits` — task aborted with reason `'brain_restart_during_human_wait'`; late answers logged but no resume. (5) Tool whitelist limited to scaffolder/builder/fixer/investigator (the four tool-using agents that own writes/runtime decisions); brain-checkpointed agents (architect/planner/reviewer/verifier/triage) keep using existing `escalateBlocked`.
- **Issues:** none opened, none closed. Backlog empty for the fifth consecutive session.
- **8.1 charter open (`0754a69`):** authored `.control/phases/phase-8-worker-ask-user/{README.md,steps.md}` (8 sub-steps), ADR 0024, INDEX row.
- **8.2 brain RPC endpoint (`0d018f4`):** new `POST /worker/ask-user` route on the existing daemon Fastify server (not a new server — daemon already imports `@factory5/brain` per ADR 0012, so new daemon route is the lowest-coupling shape). Bearer-token-gated with constant-time compare; bearer check fires before schema parse so unauthenticated callers can't probe schema. New `workerAskUser{Request,Response}Schema` in `@factory5/ipc` (`taskId` required per ADR 0024 §3). `buildWorkerAskUserHandler` in `@factory5/daemon/index.ts` validates `(taskId, directiveId)` against `tasks_inflight` then proxies into existing brain `askUser()`. `apps/factoryd/src/main.ts` generates per-startup 24-byte hex token via `crypto.randomBytes`, exports as `FACTORY5_WORKER_AUTH_TOKEN` env var so brain-spawned workers inherit it. +18 tests.
- **8.3 worker-mcp + claude --mcp-config (`91c248b`):** new `packages/worker-mcp/` workspace package (+1 → 14 packages). `handler.ts` — HTTP layer with stub-fetch testability; typed `AskUserEnvError` (lists every missing var) + `AskUserRpcError` (httpStatus + upstream code if JSON envelope, `UPSTREAM_ERROR`/`NETWORK_ERROR` fallbacks). `mcp-config.ts` — config builder + `MCP_SERVER_NAME='factory5-ask-user'` + `ASK_USER_TOOL_NAME='mcp__factory5-ask-user__ask_user'` constants. `server.ts` — MCP server entry using `@modelcontextprotocol/sdk` over stdio; tool description teaches when to use the tool (decision-grade clarification only); `isError: true` on timeout/abort so the agent sees the failure mode and falls back. `index.ts` — `getServerScriptPath()` uses `import.meta.url` to locate `dist/server.js` portably across pnpm hoisting. `ProviderRequest.mcpConfigPath` added to `@factory5/providers`; `buildClaudeArgs` adds `--mcp-config <path>`. `runTooling` writes per-task config to `os.tmpdir()/factory5-mcp-<taskId>.json` (out of worktree so it never shows in `git status`), passes flag, unlinks in finally. Pool's `buildAskUserConfig` reads `FACTORY5_WORKER_AUTH_TOKEN` + `loadDaemonEndpoint()`. +17 tests; +68 transitive deps from MCP SDK install.
- **8.4 agent whitelist + skill (`2c1981c`):** `ASK_USER_MCP_TOOL` literal exported from `agents/registry.ts` (mirrors `ASK_USER_TOOL_NAME` to keep brain → worker-mcp dep-free). Added to `tools` arrays of scaffolder/builder/fixer/investigator. `'ask-user'` added to those agents' `defaultSkills`. New `skills/ask-user.md` with concrete heuristics: 4-condition gate to ASK (ambiguous + multiple equal options + non-trivial rework cost + answerable by operator without context the agent lacks); concrete WHEN-TO-ASK patterns; explicit anti-patterns (typos → fix; sensible defaults → use; style → match convention; verifiable from codebase → read first); how-to-ask-well guidance; timeout/abort handling. Brain-checkpointed agents (architect/planner/reviewer/verifier/triage) deliberately excluded. +5 brain tests covering both directions of the inclusion list + the canonical tool name.
- **8.5 lifecycle migration + recovery (`37c0605`):** **Migration 007** rebuilds `tasks_inflight` (SQLite-style — CHECK can't ALTER; new table → INSERT-SELECT → DROP → RENAME → reinstate indexes) widening status from 5 values to 7 (`+'waiting_for_human'`, `+'aborted'`); adds nullable `waiting_question_id` + `aborted_reason` columns; new partial index `idx_tasks_waiting_for_human(id) WHERE status='waiting_for_human'` so the orphan-cleanup scan stays cheap. `TASK_STATUSES` widened in `@factory5/core`. `tasks-inflight.ts` gains 7 helpers: `getById`, `markWaitingForHuman` (no-op unless `status='running'` — guards against downgrading aborted), `markRunningAfterAnswer` (no-op unless `'waiting_for_human'` — race-safe vs. orphan recovery), `markAborted`, `findOrphanedHumanWaits`, `isTerminalStatus`, `TERMINAL_STATUSES`. `pending-questions.ts` gains `detectOrphanedAnswer()` returning `{taskId, taskStatus} | undefined` for the late-answer path; treats missing task row as orphaned (forensic-safe default). `ask-user.ts` gains `onQuestionResolved?: (questionId) => void` callback firing on new-question or rehydrated-open paths (skipped on rehydrated-already-answered since there's no poll-loop to protect). Daemon's handler wires `onQuestionResolved → markWaitingForHuman` so the wait state is staged BEFORE polling begins; `markRunningAfterAnswer` in finally. New `recoverFromHumanWaits()` runs at daemon startup alongside the existing reconcile-orphaned-directives pass; aborts orphans with reason `'brain_restart_during_human_wait'`. Discord + Telegram collectors call `detectOrphanedAnswer` after `pendingQuestions.answer`; warn-log if linked task is terminal (answer row preserved either way for forensic value). Migration count assertions in 003/004/006 idempotency tests bumped from 6 → 7. +24 state tests.
- **Tests: 535 green** (was 471 at Phase 7 close; +64 across this session: +9 ipc / +9 daemon at 8.2; +15 worker-mcp / +2 providers at 8.3; +5 brain at 8.4; +24 state at 8.5). All green on Windows; `pnpm lint` + `pnpm format:check` clean throughout. Per-package: core 14, **logger 13**, **ipc 14 (+9)**, **providers 39 (+2)**, **state 116 (+24)**, assessor 42, wiki 39, channels 60, events 3, worker 24, **brain 64 (+5)**, **daemon 37 (+9)**, cli 55, **worker-mcp 15 (+15, NEW)**.
- **Spend: $0** — all internal TS / SQL / vitest work; no LLM calls. Live validation (8.7) deferred to a future session due to operator-presence requirement.
- **Dependency changes:** `@modelcontextprotocol/sdk ^1.0.0` added to `@factory5/worker-mcp` (new package); `@factory5/worker-mcp` workspace dep added to `@factory5/worker`. pnpm-lock.yaml +619 lines from MCP SDK transitive tree (~68 packages).
- **No tags placed this session.** Phase 8 still open; tag `phase-8-worker-ask-user-closed` waits for 8.8.
- **In-step adjustments worth recording:** (a) 8.3 type fix — initial `WorkerAskUserHandler` used an inline `req` shape that didn't satisfy `exactOptionalPropertyTypes: true` for `options?: string[]`; resolved by importing `WorkerAskUserRequest` from `@factory5/ipc` and using it as the handler signature. (b) 8.5 idempotency assertions — migration tests in 003/004/006 had hard-coded `expect([1,2,3,4,5,6])`; bumped to `[1,2,3,4,5,6,7]` after adding migration 007. Expected forward-maintenance, not a real surprise.
- **Next: Phase 8 sub-step 8.6 — regression tests** (4 scenarios: happy path, brain-restart mid-wait, two-workers correlation, late-answer no-op). Then 8.7 live validation (needs operator presence — real LLM build with synthetic ambiguity, Telegram answer roundtrip, ~$2-3 spend). Then 8.8 close + tag.

## 2026-04-22 (session `2026-04-22T19`) — Phase 7c + Phase 7 + addendum-onboarding all closed

- **Commit range:** `74ad146` → `7ce70e7` (14 substantive commits + this session-end docs commit). **Phase step range:** 7c.1 (HALT clearance) → 7c.7 (Phase 7c + Phase 7 close) → addendum-onboarding (5 commits + close) → Control-discipline invariant.
- **Decisions:** two new ADRs. **ADR 0022** — Telegram long-polling lives inside `TelegramChannel`, mirroring Discord's websocket-inside-plugin pattern; closes 7c.3 as a no-op; reversible if future Telegram signals need `Event` rather than `Directive` treatment. **ADR 0023** — repo-local factory instances via cwd-walk discovery; `dataDir()` precedence is `FACTORY5_DATA_DIR` env → walk up from cwd for `.factory/config.toml` → `~/.factory/` fallback; `.factory/` is the instance-marker name, disambiguated from per-project `.factory/project.json` (ADR 0021) by the `config.toml` predicate; partially supersedes ADR 0004's storage-location claims.
- **Issues:** none opened, none closed. Backlog empty for the fourth consecutive session (I008 resolved in 7b.1).
- **Phase 7c (Telegram channel; commits `74ad146` → `7906099`):** six sub-steps. 7c.1 HALT cleared by operator providing `@Factory5_bot` token + chat-id `1225367797`, persisted to config.toml and stale `/start` update consumed. 7c.2 `packages/channels/src/telegram.ts` implements `ChannelPlugin` via raw `fetch` (no SDK) with `TelegramApi` test seam, private-chat-every-message + group-only-if-mentioned scoping, reply-to-message-id threading, pending-question answer routing; 29 unit tests (54 channels / 457 workspace). 7c.3 closed as no-op per ADR 0022. 7c.4 formalised `[channels.telegram]` config shape + added `--telegram-*` flags to `factory init`, `probeTelegram` + `--skip-telegram` to `factory doctor`, daemon auto-wiring via `hasStringField` gate; live `factory doctor` against real token returned `getMe: ok (token accepted)`. 7c.5 `packages/channels/src/telegram-roundtrip.test.ts` feeds realistic Telegram Update fixtures through the plugin to a real SQLite db via daemon-style `onInbound`; 6 tests asserting persisted directive rows (60 channels / 463 workspace). 7c.6 `scripts/telegram-smoke.ts` — live round-trip against `@Factory5_bot` captured operator reply 22s after kickoff as directive `01KPV4AQVDSPA24ZMRP944QYDG`, echoed back with `reply_to_message_id=6`; zero LLM spend. 7c.7 phase close: docs/Phase7_Progress.md 7c section authored with plugin design / onboarding wiring / done criteria, docs/PROGRESS.md entry, steps.md checkboxes flipped, tags `phase-7c-telegram-channel-closed` and `phase-7-closed` placed on the close commit.
- **Pre-Phase-8 onboarding addendum (commits `8669925` → `17c393d`):** five substantive commits. `8669925` gitignores `.factory/` at repo root before any tokens land. `a55e201` rewrites `packages/logger/src/paths.ts#dataDir()` with env → cwd-walk → `~/.factory/` fallback + 8 new tests in `paths.test.ts` (env-var wins, cwd-walk hit + deep descendant, ignore non-instance, homedir fallback, logsDir). **Migration executed out-of-band** (files gitignored): `%LOCALAPPDATA%\factory5\config.toml` + `factory.db` copied to `G:\Projects\Large-Projects\factory\factory5\.factory\`, header rewritten, old dir deleted; `factory doctor --skip-call --skip-discord` and `factory spend` both re-probed cleanly — DB migrated byte-for-byte ($63.1666 / 116 calls preserved). `e4a1c42` authored ADR 0023 + `config.example.toml` at repo root + full `docs/ONBOARDING.md` (prerequisites, Discord + Telegram bot creation, multi-instance via `cd`, troubleshooting). `3103449` reshaped `factory init` into three modes: template-copy (default, no existing config; locates `config.example.toml` via `import.meta.url` walk to `pnpm-workspace.yaml`), validate (existing config, no `--force`; zod-parse + probes), flag-driven (`--force` or any flag; legacy/CI behaviour extracted). `0628bc7` added `[daemon].host/port` to configSchema + new `loadDaemonEndpoint()` helper exported from `@factory5/brain`, wired into `apps/factoryd/src/main.ts` and the three CLI callsites (`build`, `chat`, `daemon status`); `@factory5/brain` added as runtime dep of `apps/factoryd`. Addendum closed with `17c393d` + tag `addendum-onboarding-closed`.
- **Control-discipline invariant (commits `7ce70e7` factory5, `d07d6a3` Control source):** one-line add to CLAUDE.md's Invariants section requiring the assistant to state the next Control command explicitly after every commit / tag / step-close / phase-or-addendum close. Mirrored into the shipped Control CLAUDE.md template so new projects inherit it. Motivating UX moment: user asked "from a control perspective what do I run next?" after the addendum close — deterministic answer (`/session-end`) existed but wasn't surfaced proactively. The complementary user-callable `/control-next` skill is queued as Improvement 7 in `G:\Projects\Small-Projects\Control\improvement.md` for post-v1.4.0 ship; this session ships the assistant-side half (labelled 7a in the improvement doc).
- **Tests: 471 green** (was 428 at Phase 7b close; +35 across this session: +29 at 7c.2 TelegramChannel unit suite, +6 at 7c.5 round-trip fixtures, +8 at addendum for `paths.test.ts`; net +43 over 7b close). All green on Windows; `pnpm lint` + `pnpm format:check` clean throughout. Per-package: core 14, **logger 13 (+8)**, ipc 5, providers 37, state 92, assessor 42, wiki 39, **channels 60 (+35)**, events 3, worker 24, brain 59, daemon 28, cli 55.
- **Spend: ~$0.** Telegram's API is pure HTTP (no LLM surface); live-smoke at 7c.6 exercised real bot endpoints only. No `factory build` / triage calls this session.
- **Dependency changes:** `@factory5/cli` gained `@factory5/channels` (for `factory doctor`'s Telegram probe reaching the shared api factory). `apps/factoryd` gained `@factory5/brain` (for `loadDaemonEndpoint()` at bind time). `scripts` gained `@factory5/brain` (for `telegram-smoke.ts`'s config load). `.prettierignore` created (excluding auto-generated `pnpm-lock.yaml` + `dist/` + `node_modules/`).
- **Tags placed this session (3):** `phase-7c-telegram-channel-closed`, `phase-7-closed`, `addendum-onboarding-closed` — all on commits within this session.
- **Next: Phase 8 charter — no HALT.** Operator picks between Web UI (3–5 sessions), assessor tier-3 (2–3 sessions), worker-subprocess `ask_user` (2–3 sessions), or names a different problem. After charter, author `.control/phases/phase-8-<name>/README.md` + `steps.md` and begin.

## 2026-04-22 (session `2026-04-21T22`) — Phase 7b closed (cross-session spend dashboard shipped)

- **Commit range:** `beb540a` → `ecce6ef` (4 substantive commits + this session-end docs commit). **Phase step range:** 7b.2 → 7b.5 (phase close).
- **Decisions:** none new. 7b.2–7b.5 are implementation of the ADR 0021 foundation laid down in 7b.1 last session. No new ADRs; no rescopes.
- **Issues:** none opened, none closed. Open backlog remains empty (I008 closed in 7b.1). Third consecutive session with zero open blockers.
- **Schema additions:** none. 7b runs entirely against existing tables (`model_usage` joined through `directives.project_id → projects.id`, all populated since migration 006).
- **Query layer (7b.2):** `packages/state/src/queries/spend.ts` — four aggregations (`perProject`, `perDirective`, `perDay`, `perModel`) over `model_usage`, joined through `directives.project_id`. Shared `SpendFilter { since, until, projectId }`. Exported `formatProjectDisplay(name, id)` canonises ADR 0021 §5 label rule. LEFT-JOIN semantics so orphan / project-less rows collapse into `(unassigned)` rather than vanishing. +23 tests including the ADR 0021 regression (two projects sharing basename surface distinctly).
- **CLI surface (7b.3):** `packages/cli/src/commands/spend.ts` — `factory spend` with `--group-by {project|directive|day|model}` (default project), `--since`/`--until` accepting relative (`7d`/`24h`/`30m`) or strict ISO8601, `--project` accepting ULID/name/suffix with case-insensitive LIKE + ambiguity exit-2 path, `--json` NDJSON, `--limit` clamped 1–1000. Pure `runSpend` handler + Commander wrapper mirroring findings.ts. Bare numeric strings rejected in the date parser (the `Date.parse('5')` year-5 v8 quirk). +24 tests.
- **Round-trip regression (7b.4):** `packages/cli/src/commands/spend-roundtrip.test.ts` — two tmp workspaces both basename `example`, `loadOrCreateProjectMetadata` writes distinct-ULID identity files, directives + `model_usage` seed each, `runSpend` driven directly. Six assertions covering distinct on-disk files, distinct dashboard rows, rollup-matches-raw ground truth, `--project <ulid>` / `--project <suffix>` isolation, `--project example` ambiguity. If any layer reverts to basename-keying one of the six fails immediately. +6 tests.
- **Live validation (7b.3 smoke):** `factory spend` against the real local DB — migration 006 auto-ran on first touch, dashboard rendered 2 projects (`example (…SG6H)` + `parallel-example (…9PR3)`) + 2 `(unassigned)` calls, totalling $63.17 across 116 rows.
- **Tests: 428 green** (was 375 at 7b.1 close; +53 across the 7b arc: +28 at 7b.1 prior session, +23 at 7b.2, +24 at 7b.3, +6 at 7b.4). All green on Windows; `pnpm lint` + `pnpm format:check` clean. Per-package at close: core 14, logger 5, ipc 5, providers 37, state 92, assessor 42, wiki 39, channels 25, events 3, worker 24, brain 59, daemon 28, cli 55.
- **Spend: $0** — pure local TS + SQLite + vitest work; no LLM calls. (Live-smoke invoked `factory spend` which is a read-only DB query, not an LLM call.)
- **Minor polish (not its own step):** tightened `parseWindowArg` ISO fallback to require a `T` separator so bare numeric strings don't silently parse as ancient years (test caught `Date.parse('5')` → year 5). Dropped an unused `ULID_RE` constant + `isUlid` export from `spend.ts` before the first lint run to keep the module minimal.
- **Phase 7b tagged** `phase-7b-spend-dashboard-closed` on commit `ecce6ef`.
- **Next: 7c (Telegram channel) — blocked on operator input.** Step 7c.1 is an explicit [HALT] for a Telegram bot token + target chat-id. After secrets land, 7c.2 implements `packages/channels/src/telegram.ts` as the third `ChannelPlugin` (Discord is the reference).

## 2026-04-21 (session `2026-04-21T21`) — Phase 7b.1 shipped (first-class project identity; I008 resolved)

- **Commit range:** `db87e97` → `1999a14` (6 commits in factory5 + 1 in Control source `cee27a1`). **Phase step range:** session-internal Control-fix → 7b.1 close.
- **Decisions:** ADR 0021 — first-class project identity via `<project>/.factory/project.json` (ULID). Stable across path moves; explicit at fork (delete file before next build). Mirrors how git, npm, uv claim per-project identity. Five-part decision: file shape, helper resolve rules, schema migration, backfill, CLI display. Supersedes both prior framing options for I008's collision (per-path PK and per-directive-only fuzzy view) — neither would have given identity that survives a path move.
- **Issues:** none opened. **I008 RESOLVED** (commit `1999a14`) with regression-test references in 4 files: migration 006 shape + backfill, project-metadata helper, state.projects two-projects-same-name test, cli/findings backfill identity-required test. Open backlog now empty.
- **Schema additions:** migration 006 — `projects.id TEXT PRIMARY KEY NOT NULL` (rebuilt from name-PK via table-rebuild + post-hook backfill), `directives.project_id TEXT` (no FK to keep migration tractable; helper owns lifecycle), `findings_registry.project_id` semantics shift basename → ULID via post-hook UPDATE, `learnings.source_project` similarly translated. Migration interface gained optional `post: (db) => void` hook (runs inside the same transaction as `up` SQL) and `runMigrations(db, { maxId })` so tests can stage data at a specific schema version before triggering a later migration's backfill.
- **Helper:** `wiki.loadOrCreateProjectMetadata(projectPath, name, opts?)` reads-or-creates `<project>/.factory/project.json`. Throws `ProjectMetadataCorruptError` on present-but-unparseable rather than silent re-tag (which would lose project history). Companion `readProjectMetadata` for inspection-only flows.
- **Integration end-to-end:** CLI `build.ts` + `resume.ts` populate `directive.projectId` (resume inherits from parent directive or falls back to `findByName`); brain `pool.ts` derives projectId from the directive (one SELECT per task) for the FindingRegistryBinding; wiki `mirrorToRegistry` skips the registry write if no projectId rather than fall back to `basename(projectPath)` (the I008 trap); cli `findings.ts` backfill uses `readProjectMetadata`, skips projects without an identity file with a clear operator hint to run `factory build` once. Brain `pool.ts` switched off `basename` import; wiki `findings.ts` same.
- **Control-framework hygiene (factory5 + Control source).** Session-start protocol gains step 5b: when `next.md` surfaces a `## Decisions awaiting your input` section or STATE.md flags an open design choice, expand each option in full at bootstrap rather than as labeled footnotes — caught from this session itself wasting a turn on (a)/(b) shorthand. Mirrored across factory5's `.claude/commands/session-start.md` + `.control/runbooks/session-start.md` + the embedded copies in `.control/PROJECT_PROTOCOL.md`. Same change in Control source repo (`cee27a1`). next.md template gains the matching `## Decisions awaiting your input` slot.
- **Tests: 375 green** (was 347 at Phase 7a close; +28 across 7b.1: 11 migration 006 shape + backfill, 11 project-metadata helper, 3 state.projects rewrite for id-keyed CRUD + two-projects-same-name regression, 1 cli backfill skip-on-missing-identity, 2 wiki dual-write `projectId` propagation).
- **Phase 7b re-scope.** Original 4-step plan grew to 5 steps. New 7b.1 is data-model prep (this session); existing query → 7b.2, CLI → 7b.3, round-trip → 7b.4, close → 7b.5. README est. sessions bumped from 1 to 1–2.
- **Format hygiene.** Prettier format-pass on 24 previously-unformatted control & docs files (commit `786698a`, separate from substantive work). Pure whitespace; `pnpm format:check` is now clean across the workspace.
- **Spend: $0** — pure local TS + SQLite + Node fs work; no LLM calls.
- **Follow-up noted, not blocking:** Brain `pool.ts` does one extra `directives.getById` SELECT per task to fetch `projectId`. Could be optimized by passing projectId through PoolOptions instead, but the current shape keeps PoolOptions stable. No measured impact; revisit if pool throughput becomes a bottleneck. **`docs/PROGRESS.md` and `docs/Phase7_Progress.md` 7b.1 row** not updated this session — non-blocking; can land alongside 7b.2 or as standalone hygiene.
- 7b.2 (spend aggregation queries) opens next session on a clean foundation. No [HALT] gates remain in 7b.

## 2026-04-21 (session `2026-04-21T17`) — Phase 7a closed (budget enforcement shipped)

- **Commit range:** `d295dd3` → close commit. **Phase step range:** 7a.1 → 7a.9.
- **Decisions:** ADR 0020 (pre-call budget enforcement; rolling average from `model_usage` per `(category, mode)` with cold-start defaults; `assertBudget` wrapper in `@factory5/brain/src/budget.ts`; `budget_exceeded_*:` prefix on `directives.blocked_reason`; per-directive scope — not per-session, not cumulative).
- **Issues:** none opened, none closed. Open backlog unchanged: {I008 MEDIUM, findings-registry project-id collision — may surface in Phase 7b if grouping by project_id}.
- **Schema additions:** migration 004 (`model_usage.mode` nullable TEXT), migration 005 (`directives.max_usd REAL` + `max_steps INTEGER` nullable). Both additive; absent = unlimited (pre-ADR-0020 behaviour).
- **Call-site integration:** triage / architect / planner call `assertBudget` before the registry provider call; pool invokes it pre-dispatch using `isToolUsingAgent(task.agent)` to pick `'stream'` vs `'call'`; `loop.runInline` catches `BudgetExceededError` at the outer boundary, flips directive to `blocked` via `formatBlockedReason`, queues outbound escalation with a `factory resume --max-usd <higher>` hint. Providers + worker untouched — they stay dumb about budgets.
- **CLI + config:** `--max-usd <n>` / `--max-steps <n>` flags on `factory build` (7a.5); `[budget.defaults]` section in `~/.factory5/config.toml` (7a.6). Explicit flag wins over config default; both absent = unlimited.
- **Regression test:** `packages/brain/src/budget-regression.test.ts` — 3 scenarios covering maxUsd trip (pre-seeded spend against $3 ceiling), maxSteps trip (3rd call against maxSteps=2), happy path (limits well above estimate). Asserts on error kind, directive blocked_reason, `tasks_inflight` emptiness, and `readPlan` persistence as `abandoned`.
- **Live validation passed.** `factory build example --max-usd 3` against fresh workspace. Tripped cleanly at builder-2 dispatch: `spentSoFar=$1.9151 + estimatedCost=$2.00 > ceiling=$3.00`. Directive `01KPRHNEX1T3VR3S4ZTTSJ8F0M` ended `blocked` with the expected `budget_exceeded_usd:` blocked_reason. 5 `model_usage` rows recorded with correct mode values. Phase 6c's silent $7.71-over-$4-6 overshoot is not reproducible; $1.08 headroom at the halt.
- **Tests: 347 green** (was 309 at Phase 6 close; +38 across 7a: 3 migration 004 shape, 14 model-usage queries, 3 migration 005 shape, 12 budget unit, 3 budget integration regression, 2 config budget-defaults, with one existing migration-idempotency assertion updated).
- **Spend:** $1.9151 (live validation only) — right at the bottom of the $2–4 envelope the user scoped for the session.
- **Minor polish.** Pool.ts's `running.size === 0` branch now labels pending tasks with the budget reason (not "deadlock") when `budgetError` is set. Doesn't affect directive-level state; improves per-task blocked_reason readability if anyone ever surfaces it.
- **Follow-up noted, not blocking:** CLI build-summary omits partial task results when the budget catches (returns `taskResults: []` from the catch arm); `InlineResult.triage` is optional now but the CLI still falls back cleanly when missing.
- Phase tagged `phase-7a-budget-enforcement-closed` on this close commit. Phase 7b (spend dashboard) kicks off next session; next step is 7b.1 — spend-aggregation queries in `@factory5/state`.

## 2026-04-21 (session `2026-04-21T16`) — Phase 6 closed (6c + 6a shipped; 6b dropped per ADR 0019)

- **Commit range:** `c780180` → `47cf160` (4 commits). **Phase step range:** 6b.1 → phase-6-close.
- **Decisions:** ADR 0019 (drop GitHub integration; durable doctrine: factory's effects are operator-directed per-directive, not pattern-driven).
- **Issues:** none opened, none closed. Open backlog unchanged: {I008 MEDIUM, findings-registry project-id collision}.
- Phase tagged `phase-6-closed` on commit `47cf160`. Phase 7 (Operator-control + budget discipline) kicked off; next step is 7a.1 — ADR for pre-call cost estimate approach.
- **Phase 6b dropped wholesale.** Session opened Phase 6b cleanly (6b.1 commit `c780180` recorded PAT + test repo scaffolding). At 6b.2 — the event-source design ADR — the session surfaced that (a) the charter had silently pivoted GitHub from event-source (scaffold intent) to channel (Phase 6b charter) without justification, and (b) neither framing earned its keep for a solo dev-box operator: channel duplicates the CLI; observer needs factory's outputs to live on GitHub first, which no phase has built. Drop was wholesale.
- **ADR 0019 records the decision + durable doctrine.** Commit `c39ef8f`. Three decisions in one doc: no GitHub channel; no GitHub observer; future output-to-GH (if and when it ships) is operator-directed per-directive, not pattern-driven. This last principle generalises — factory's side-effects in the world happen because a directive asks for them, not because a daemon observer or channel plugin silently decides.
- **Code + doc prune.** Commit `ee85efd`. Pruned: `'github'` + `'webhook'` from `CHANNEL_IDS`; three `github.*` event kinds from `eventBodySchema`; github narrative from `CompleteArchitecture.md`, `docs/ARCHITECTURE.md`, `docs/CONTRACTS.md`, `README.md`; `github-poll` + `webhook-server` stub mentions from `packages/events/README.md`; "GitHub polling" phrase from `packages/daemon/README.md` + `apps/factoryd/package.json` description; "GitHub event description" from `prompts/agents/triage.md`. Migration 001's CHECK constraints intentionally left in-place (SQLite cannot ALTER a CHECK; stricter-TS-over-wider-DB is harmless; comment added pointing to ADR 0019). Tests re-pointed at `fs.changed`. 309 tests green (no delta).
- **Charter amendment.** Phase 6 exit criterion #2 ("accept at least one non-CLI trigger live — a real GitHub issue...") struck through per the charter's "scope is flexible" clause, replaced with "factory accepts at least one non-CLI trigger — Discord (shipped Phase 4)."
- **Phase 7c dependency updated.** Telegram (7c) no longer depends on "patterns locked by 6b"; Discord is the reference channel.
- **`.control/phases/phase-6b-github-channel/` deleted in full.** Scaffolded Phase 7 at `.control/phases/phase-7-budget-discipline/` with README + placeholder steps.md covering 7a (9 steps) + 7b + 7c.
- **Operator follow-up (out-of-band).** Revoke the `env:GITHUB_TOKEN` PAT, delete `momobits/factory5-6b-smoke`, clear the env var. Documented in STATE.md + next.md. Non-blocking for Phase 7.
- **Spend: $0** — a second consecutive zero-LLM-spend session. Phase 6 closed cheap.

## 2026-04-21 — Phase 6b kickoff (6b.1 — `[HALT] secret_needed` resolved)

- PAT reference + test repo recorded: `env:GITHUB_TOKEN` stored in `HKCU\Environment` (persistent user env, classic PAT, `public_repo` scope); test repo `momobits/factory5-6b-smoke` (public, issues enabled, default branch `main`). Full caveats + rollback in `.control/phases/phase-6b-github-channel/config.md`. **No secret value** committed — only the reference shape.
- Pre-commit verification confirmed the repo is reachable (HTTP 200 unauthenticated) and the env var is present in `HKCU\Environment`. The current Claude Code bash does not see `$GITHUB_TOKEN` (parent-process env frozen before `setx`) — factoryd spawned after `setx` will inherit it, so the 6b.6+ code paths are unblocked even though this session's bash cannot directly exercise the token.
- No code touched, no tests changed, $0 spend. Next step **6b.2** — ADR on the event source (webhook vs polling vs hybrid); the decision gates expansion of the placeholder 6b.3–6b.9 sub-step bodies.
- Commit: `chore(6b.1): record github test repo + PAT ref`.

## 2026-04-21 — Phase 6a closed (cross-project findings registry)

- Phase tagged `phase-6a-findings-registry-closed` on the `chore(phase-6a)` close commit. Phase 6b (GitHub channel) kicked off; paused at 6b.1 pending user PAT + test repo URL.
- Shipped: `findings_registry` SQLite table (composite PK on `(project_id, finding_id)`, `advisory` column mirroring ADR 0018); `wiki.addFinding` + `updateFindingStatus` gain optional `FindingRegistryBinding` for best-effort dual-write; worker + brain pool wire the binding end-to-end; `factory findings list | show | backfill` CLI surface complete with filters, glob, NDJSON output, per-project dedup.
- Tests: 309 green (was 262 at Phase 6c close; +9 state migration shape, +8 state registry queries, +6 wiki dual-write, +24 CLI handlers — CLI gained its first test file).
- Spend: $0 — first factory5 session since Phase 3 that did no LLM calls. Validation was local SQL + filesystem only.
- Issue opened: **I008** (MEDIUM, state/findings-registry) — `project_id = basename(path)` collides across workspaces; 6a's backfill against the v5f/v6c corpora overwrote v5f's F001 on composite-PK conflict. Per-project `findings.json` files untouched; registry-only representation limit. Candidate fix: PK on `(project_path, finding_id)`. Deferred to Phase 7+.
- Mid-session Control discipline addition (commit `87ea1c0`): CLAUDE.md now mandates flipping the matching `- [ ]` in `.control/phases/<phase>/steps.md` to `- [x]` in the same commit as the sub-step closer. Proposal filed as Improvement 6 in `G:/Projects/Small-Projects/Control/improvement.md` for v1.3.1 / v1.4.0 inclusion.
- Commits: 6a.1 `5d81fe2` → 6a.8 close commit; docs-side close at `fd3837e`. Phase-6a tag lands on this phase-close commit.

## 2026-04-21 — Phase 6c closed (verifier advisory-only)

- Phase tagged `phase-6c-verifier-overhaul-closed` on commit `a24f883`. Phase 6a kicked off.
- ADR 0018 decided advisory path: Finding schema gains optional `advisory`; verifier source defaults to true; `brain.loop` log splits open findings into blocking vs advisory; verifier prompt rewritten (6-line stub → 90-line brief with anti-hallucination rule).
- Live validation (directive `01KPQK61F9967TT8JZWCMCV3NW`, 2026-04-21) ended `complete` with gate all-true, 119 pytest green, two verifier findings both `advisory:true` and non-contradictory. F001-class defect (CRITICAL absence hallucination contradicting green gate) not reproducible.
- Tests: 262 green (was 255 at Phase 5 close; +2 core schema, +3 wiki addFinding, +2 worker F001 regression).
- Spend overrun: live run cost $7.71 vs $4–6 envelope. Carry-forward concern for 6a/7a.
- Commits: 6c.1 `c35681a` → 6c.8 `a24f883`; phase-close commit appends Phase 6a scaffold + STATE + next.md + this journal entry.
- No new factory5 self-issues opened; `docs/issues/INDEX.md` Open list still empty.

## 2026-04-21 — Control instantiated for Phase 6

- Control framework v1.3.0 installed (commit `6494766`, tag `protocol-initialised`). Installer preserved factory5's existing `CLAUDE.md`, `docs/`, `CompleteArchitecture.md`.
- Content-vs-operational split documented in `CLAUDE.md`: long-form content (ARCHITECTURE, CONTRACTS, SKILLS, AGENTS, PROGRESS, ADRs, issues, Phase\*\_Progress) stays under `docs/`; Control only owns the operational cursor (`.control/progress/`) and per-phase checklists (`.control/phases/`).
- `.control/architecture/overview.md` rewritten as pointer-only into `docs/`.
- `.control/architecture/phase-plan.md` populated: Phases 0–5 closed pre-Control, Phase 6 active with sub-phases 6c → 6a → 6b in execution order.
- Scaffolded `.control/phases/phase-6c-verifier-overhaul/` (detailed steps), `phase-6a-findings-registry/` (stub), `phase-6b-github-channel/` (stub).
- STATE.md set to Phase 6, sub-phase 6c, step 6c.1. Next action: author F001 regression-reproducer test.
- No implementation work yet. Next session begins step 6c.1.
