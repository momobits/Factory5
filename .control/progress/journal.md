# Journal

Append-only, newest on top. One entry per session, short. Minor fixes land here as one-line entries (see Issue flow in `.control/PROJECT_PROTOCOL.md`).

## 2026-05-05 — 3.6 follow-ups + ADR 0029 promotion + Claude Code hooks cwd-anchor (3.7 not started)

- Three commits this session: `00d2bc4` (3.6 follow-up auth fix) → `e5ec723` (hooks cwd-anchor) → `79474b1` (ADR 0029 promotion). All four `pnpm` gates green at every commit and at session end. No new tests added (auth fix in factory-web has no test harness; hooks change is config; ADR is markdown). Workspace test count unchanged.
- **2026-05-05 phase-3 live smoke landed (Decision 1 = smoke-first path from last session's session-start prompt).** Single factoryd-up window covered: 3.6 cancel-button acceptance ✓, ADR 0029 promotion ✓, plus surfaced two real bugs and one styling backlog confirmation. Total smoke spend ~$1.67 across two directives (first cancelled at architect-askUser at $0.43, second cancelled mid-builder at $1.24 after directing the smoke-demo CLAUDE.md and answering two askUser questions via API to get past `assisted` autonomy gates). Browser-side verification covered Overview / Directives / Chat / Questions pages.
- **Live-smoke ADR 0029 scorecard:** 4 of 5 functional event types verified live in browser (`task.started` / `task.completed` / `spend.updated` / `log.line`) plus `directive.completed` via cancel-button round-trip. `finding.created` not exercised — directed smoke-demo project produced no findings (no verifier issues to register). ADR 0029 promotion accepted with the gap documented in its "Live verification" section: the brain emit code path has 4 unit tests in `pool.test.ts` from `f990323`, and the wire-side plumbing is identical in shape to the 4 verified event types, so risk of inflight breakage is low. Phase-3 acceptance smoke at `/phase-close` will close the gap on a substantive build.
- **Bug 1 — `directives/detail.astro` was missing `captureTokenFromUrl()` on init.** Surfaced when the operator opened the detail page directly with `?id=...&t=...` in a fresh tab and got "missing FACTORY5_UI_TOKEN — reopen the URL logged by factoryd". Every other auth-gated page calls `captureTokenFromUrl()`; detail.astro was the lone outlier. One-line fix in `00d2bc4 fix(3.6): bootstrap UI token in directives/detail.astro` — import `captureTokenFromUrl` alongside the other lib/api imports, call it as the first executable statement in the script body. Mirrors the canonical pattern. Minor fix, no regression test possible without an FE harness which factory-web doesn't have. Logged here per the journal-line convention for minor fixes.
- **Bug 2 — Claude Code hook commands failed intermittently with "No such file or directory".** Root cause: `.claude/settings.json` Stop / SessionStart / SessionEnd / PreCompact entries used `bash .claude/hooks/<name>.sh` (relative path); Claude Code launches hooks from whatever cwd the most recent Bash tool call left the harness in, so a `cd packages/state` mid-session broke every subsequent Stop hook fire. Confirmed via the claude-code-guide skill: `$CLAUDE_PROJECT_DIR` is the canonical project-root env var Claude Code provides per the hooks spec. Fix in `e5ec723 chore(install): cwd-anchor Claude Code hooks via $CLAUDE_PROJECT_DIR` wraps each hook command with `bash -c 'cd "$CLAUDE_PROJECT_DIR" && exec bash .claude/hooks/<name>.sh'`. Verified end-to-end by running the new Stop hook command from `packages/state/` (the original drift cwd that triggered the bug) — exit 0, snapshot written, internal relative paths resolved. Same fix applied in the upstream Control framework repo at `G:\Projects\Small-Projects\Control` (`tools/cli.js` settings template generator + Control's own `.claude/settings.json`); operator-facing call to commit + bump 2.2.2 → 2.2.3 + npm publish was deferred — those edits are uncommitted in the Control repo pending operator's go.
- **ADR 0029 promotion (`79474b1 docs(adr): ADR 0029 — directive-stream protocol`).** Distills six architectural decisions from `UPGRADE/specs/sse-directive-stream.md` into `docs/decisions/0029-directive-stream-protocol.md` (168-line ADR). Sections: Context (3 forcing functions: Phase-3 charter / chat-streaming / cancel-button latency); Decision (six numbered subsections matching the spec's structure); Live verification (the 2026-05-05 smoke scorecard with the `finding.created` gap explicitly documented); Consequences (positive 6-bullet, negative 6-bullet, reversible-yes-layered); Alternatives considered (8 bullets including WebSocket / long-poll / gRPC / single-event-type / no-backfill / per-directive-process / synchronous-finding-emit). INDEX.md row added. The carry-forward "where the 3.6 auth bug surfaced" disambiguation is in the ADR's Live verification so a future reader doesn't conflate the bug with a protocol issue.
- **Bug 3 — Submit button invisible in question-detail page.** Surfaced when the operator tried to answer the askUser question via FE; the `Submit answer` button had `class="btn btn-primary"` with `color: Canvas` rendering identical to the page background under the operator's color scheme. Operator could only locate the button via Ctrl+A. Fix is one-line CSS adjustment (swap `color: Canvas` for an explicit foreground that holds against any color scheme, or add an explicit background fill that contrasts) but not landed this session — flagged in next-session notes as a minor `fix(3.x)` follow-up that could fold into the PageShell + Dashboard `<style is:global>` migration if that lands first. Workaround for the smoke: operator answered the questions via direct API POST (`/api/v1/pending-questions/:id/answer`) bypassing the FE entirely.
- **Decision NOT to start step 3.7 this session.** Three commits already landed; the smoke + debugging consumed significant operator focus; 3.7 spans 3+ commits with frontend-design skill discipline required for the FE page. Cleaner cut to /session-end with 3.7 as a fresh session's primary work. Decision 2 from last session (Option A — extract createProject into @factory5/wiki) is locked in and ready to execute first thing next session.
- ADRs decided: ADR 0029 promoted (this session). Counts now: 0029 latest accepted, 0028 + 0027 unchanged.
- Issues opened / closed: none formally; bug 3 (Submit button) flagged in next-session notes as a fix(3.x) follow-up rather than a separate issue file (severity = minor per Control conventions, fix is one-line CSS).
- Minor fixes: prettier touched up table-column spacing in `0029-directive-stream-protocol.md` after authoring (single auto-format pass before commit); INDEX.md was already prettier-clean.
- Blockers hit: none. The smoke surfaced bugs but they were either fixed mid-session (auth bootstrap, hooks) or routed to next-session notes (Submit button, smoke teardown). The `assisted` autonomy parking at each phase transition (architect→planning, planning→execution) was a documented characteristic, not a bug — operator-bypassable via direct API POST when the FE was unstyled enough that the question-answer flow wasn't smooth.
- All four `pnpm` gates green at session end. Per-package test counts unchanged from last session: state 152, channels 175, daemon 167, brain 101, worker 38, worker-sandbox 86 (+ 3 skipped Windows/Linux branches), assessor 79, wiki 64, cli 82, providers 39, ipc 28, events 3, worker-mcp 15, core 14, logger 20.
- Smoke residue carried forward: two cancelled directives (`01KQW3D4CZ84ZFHFKYEP7BWQBX`, `01KQWDWRQD08X3BEYEQNAD6M23`), real `smoke-demo` project (`01KQW30T5274QGSEHHVZTRQ953`) at `C:\Users\Momo\factory5-workspace\smoke-demo` with scaffolder + builder output written. Daemon (PID `serve-41196` on `127.0.0.1:25295`) and astro dev (`127.0.0.1:4321`) still running at session end. All optional cleanups, none block 3.7.
- Next: step 3.7 — `/app/projects/new` per Decision 2 = Option A locked in last session. Three-commit plan in next-session notes: (a) `refactor(3.7): extract createProject into @factory5/wiki`, (b) `feat(3.7): POST /api/v1/projects route + schemas`, (c) `feat(3.7): /app/projects/new page` (frontend-design skill required for c). Acceptance smoke ideally combined with phase-3 acceptance to also close ADR 0029's `finding.created` live-verification gap.

## 2026-05-03 — Step 3.6 shipped (cancel button on directive detail; pause + 3.x follow-ups deferred)

- Step range 3.6 across `01686e1..0f5775a` (1 step commit + 1 close commit), preceded by `288603e` (session-opening drift reconcile) and `f990323` (3.1b deferred-work — finding.created emission). Four commits in the session.
- Session-start drift reconcile (`288603e`) — fifth occurrence of the post-session-end self-reference drift. Operator chose option (a): STATE.md "Last commit" caught up from `2dfd25f` (3.5 close) to HEAD's `96f5883` (the prior session-end docs commit); "Current step" narrowed from "cancel + pause" to "cancel button" reflecting Decision 2 = option C made at the same session-start prompt. Reconcile commit body documents the steady-state lag-by-1 the runbook accepts. The `/session-end` skill fix flagged in 3.4 + 3.5 journal entries (track "last work commit" rather than HEAD, or amend STATE.md post-commit) remains worth doing — still unaddressed across 5 occurrences now.
- **Two design decisions resolved at session-start before any code:**
  - **Decision 1 — cancel route prefix = option (a):** add `POST /api/v1/directives/:id/cancel` gated by `requireUiAuth`, keep the existing `POST /directives/:id/cancel` for CLI-loopback use. CLI cancel was confirmed to hit the legacy path via HTTP daemon-client (`packages/cli/src/commands/cancel.ts:82` → `client.cancelDirective` → `packages/ipc/src/client.ts:88-96` → `POST /directives/${id}/cancel`); CLI auth is loopback-only, so moving the route under `requireUiAuth` would break CLI without a parallel auth shift. Two route registrations sharing one closure-scoped `handleCancel` keeps the contract surface unchanged for CLI while lining up the SPA namespace.
  - **Decision 2 — pause primitive = option C (defer):** phase-3-web-ui/steps.md line 8 conflated cancel + pause into one step but no pause primitive exists today. Cancel solves the primary operator-pain case (a build going wrong needs to die, not pause-then-think); pause is the kind of feature worth designing once a real workflow demands it. Steps.md line 8 rewritten as "Cancel button on directive detail"; a new `## Deferred follow-ups` section at the end of phase-3-web-ui/steps.md captures three explicit `- [ ]` bullets — pause primitive, PageShell + Dashboard global migration, pre-3.5 baseline live-smoke — none of which gate any 3.x step.
- **Session ships 4 commits implementing cancel + closing 3.6 + extending the SSE wire:**
  - `288603e` `docs(state): reconcile commit cursor after session-end` — drift reconcile per option (a). Single-file STATE.md update; mirrors `d7a366c`'s shape from the prior session.
  - `f990323` `feat(3.1b): emit finding.created from brain pool` — wires the 5th of 5 ADR 0029 events. New `emitFindingCreated` synchronous helper in `packages/brain/src/pool.ts` that takes a fully-loaded `Finding` so I/O stays in the caller; integration call site in `executeTask` does one `listFindings(plan.projectPath)` per task to feed every emit (avoiding N+1 disk reads); emission happens after `task.completed` so subscribers see the completion signal first; wrapped in try/catch so a corrupt findings.json never fails the task. +4 unit tests in `pool.test.ts` mirror `loop.test.ts` shape: emitter-undefined-silent, well-formed-event-with-advisory-false-default, advisory-true-pass-through, one-event-per-call. Brain test count 97 → 101. ADR 0029 promotion now blocks on a single operator-driven live-smoke (kick off a real build, observe all 5 event types over the wire).
  - `01686e1` `feat(3.6): cancel button on directive detail` — server: extracts the existing inline cancel handler into a closure-scoped `handleCancel`, keeps `/directives/:id/cancel` as the CLI path, adds `/api/v1/directives/:id/cancel` gated by `requireUiAuth`. FE: adds `cancelInflight` + `cancelError` to `PageState`; `buildHeader` emits a pill button next to the live-pip when status is running/pending OR a cancel is in flight OR a cancelError is showing; click disables and morphs to "Cancelling" with a blinking trailing dot echoing the existing `live-pip-blink` keyframe; ApiError handling distinguishes ALREADY_TERMINAL / UI_AUTH_REQUIRED / UI_DISABLED. New CSS uses the warm-red `#c24` palette mirroring the existing log-level-error treatment, color-mix(currentColor) borders + background to stay in the same visual family as the live-pip. +6 daemon route tests cover 401 UI_AUTH_REQUIRED without bearer (DB row unchanged on auth fail) / 503 UI_DISABLED when uiAuthToken is undefined / happy path with bearer (status flips, abortFired=true, handle aborted) / 404 NOT_FOUND for unknown id post-auth / 409 ALREADY_TERMINAL post-auth / legacy CLI path still works without bearer. Daemon test count 161 → 167. Frontend-design skill invoked before authoring per saved feedback.
  - `0f5775a` `refactor(3.6): close step 3.6 — cancel-only; pause + 3.x follow-ups deferred` — flips `[ ] 3.6` → `[x] 3.6` in `phase-3-web-ui/steps.md` and `UPGRADE/ROADMAP.md`; rewrites the 3.6 line to "Cancel button on directive detail" wording; adds a `## Deferred follow-ups` section at the end of phase-3-web-ui/steps.md capturing pause primitive (Option A vs B vs longer-defer), PageShell + Dashboard global migration, and pre-3.5 baseline live-smoke as explicit `- [ ]` bullets with no acceptance dependency from any 3.x step.
- **Decision NOT to autonomously land Backlog A (PageShell + Dashboard global migration) this session:** the migration touches all 11 pages plus the `Dashboard.astro` layout, with high visual-regression surface that an autonomous gate-only run can't verify (no browser harness in `factory-web`; saved feedback says "if you can't test the UI, say so explicitly rather than claiming success"). Documented under steps.md "Deferred follow-ups" with explicit guidance: land in a session where the operator can spot-check pages in a browser as they convert.
- ADRs decided: none. ADR 0029 (directive-stream protocol) is now one operator smoke away from promotion — all 5 event types flow end-to-end after this session's 3.1b commit; the only remaining gate is "kick off a real build, observe all 5 event types in `/app/directives/detail` and `/app/chat`."
- Issues opened / closed: none.
- Minor fixes: `prettier --write` auto-fixed formatting on `packages/daemon/src/server.ts` after the cancel-route extraction edit (single-line change, non-content).
- Blockers hit: none. The cancel-route auth design call surfaced last session as a 3.6 design decision rather than a 3.5 blocker; the resolved-at-session-start pattern meant no rollback or rework happened mid-step.
- All four `pnpm` gates green at every commit and at session end. Workspace test count went +10 (brain +4 from `pool.test.ts`, daemon +6 from cancel-route tests). Workspace total now ~1063.
- Two deferred items remain on the 3.x backlog (no 3.7+ acceptance dependency): (i) PageShell + Dashboard global migration — operator-required for browser verification; (ii) Pause primitive — design when a workflow signal demands it. The third (`finding.created` brain emission, flagged as deferred in 3.5's journal entry) shipped this session in `f990323`. The pre-3.5 baseline live-smoke remains pinned for phase-3 acceptance.
- Loose ends still open from prior sessions (not blocking 3.7): synthetic smoke directive `01KQPDMQE6QTQZ3QMDD69019YK` + `demo-project` in DB; factoryd PID 32436 from a prior session may still be running.
- Next: step 3.7 — `/app/projects/new` page (browser mirror of `factory init <project>`). Two design decisions to resolve before code: (1) operator live-smoke first to close 3.6 acceptance + promote ADR 0029, or after; (2) form-submit route reuse vs new — depends on whether `factory init` server-side already exposes an HTTP path. Recommendation: smoke first (5 min, unblocks ADR 0029 + verifies cancel button in real conditions), then start 3.7 with the route audit.

## 2026-05-03 — Step 3.5 shipped (`/app/chat` page wired end-to-end with brain `log.line` emission)

- Step range 3.5 across `10efe20..2dfd25f` (4 step commits) plus a session-start drift reconcile (`d7a366c`).
- Session-start drift reconcile (`d7a366c`) — fourth occurrence of the post-session-end self-reference drift (`cce7065`/`db61baf`/`54c0f20` were the prior three). Operator chose option (b) at the session-start prompt: STATE.md "Last commit" caught up from `dfd1a07` (3.4 close) to HEAD's `1a845f5` (the prior session-end docs commit); reconcile commit body documents the steady-state lag-by-1 the runbook accepts and doesn't re-attempt the self-amend that corrupted the `54c0f20` reconcile last session. The `/session-end` skill fix flagged in step 3.4's journal entry remains worth doing — track "last work commit" rather than HEAD, or amend STATE.md post-commit — but stays unaddressed in 3.5 (out of step scope).
- **Step 3.5 spans 4 commits implementing chat end-to-end against a real factoryd:**
  - `10efe20` `feat(3.5): emit log.line from brain chat-path` — adds `emitLogLine` helper alongside `emitDirectiveCompleted` in `packages/brain/src/loop.ts` (same fire-and-forget shape, silent when no emitter wired); `runInline`'s chat branch emits a `log.line` event with `component: 'brain.chat'`, `msg: replyText`, `attrs: {intent, confidence}` before `directive.completed`. New `loop.test.ts` pins the emission shape (silent without emitter / well-formed event with emitter / attrs pass-through / attrs-undefined-omits-key). Brain test count 93 → 97.
  - `7fcbd10` `feat(3.5): POST /api/v1/chat/messages route + IPC schemas` — `apiV1ChatMessageRequestSchema { message: 1..8192, conversationId?: ulid }` + `apiV1ChatMessageResponseSchema { directive }` in `@factory5/ipc`; route in `packages/daemon/src/server.ts` mints `intent=chat` directive with `source='cli'` (loopback parity with `/api/v1/builds`), `principal='web-ui'` (audit-log distinction without expanding the channel-id enum), `channelRef='web-ui-${reqId}'` (multi-tab distinctness). +9 tests cover 401 / 503 UI_DISABLED / 400 SCHEMA_VALIDATION_FAILED on missing/empty/>8KB message / 400 on malformed conversationId / happy path with directive persistence + doorbell ring / conversationId stored on payload / multi-tab channelRef distinctness. Daemon test count 152 → 161.
  - `1fa06fa` `feat(3.5): /app/chat page — composer + history + markdown + /cmd shortcuts` — `apps/factory-web/src/pages/chat.astro`. Free-form messages POST `/api/v1/chat/messages` and subscribe to the directive's SSE stream rendering one bubble per `log.line` event filtered by `component === 'brain.chat'`. Slash-prefixed input dispatches client-side to existing `/api/v1/{status,spend,findings}` GETs (annotated `direct` rather than `via stream` on the bubble). Hand-rolled markdown for fenced code (extracted first to survive HTML escape) / inline code / bold / italic / paragraphs from blank lines / line-breaks from single newlines — ~30 LOC, no new dep. Auto-scroll-with-pause-on-user-scroll mirrors `directives/detail.astro:284-336` log-tail pattern. ⌘/Ctrl+Enter submits (textarea Enter remains for multi-line). Connection pip in the header reuses the directive-detail design language. Per-turn stream lifecycle: each user message closes any prior `apiStream`, opens a fresh one against the new directive, listens for log.line events; `astro:before-swap` teardown prevents leaked EventSources. Frontend-design skill invoked before authoring per saved feedback. Chat link added to dashboard nav between Build and Directives.
  - `2dfd25f` `refactor(3.5): close step 3.5 — /app/chat lands; CLI 120s gate stays` — flips `[ ] 3.5` → `[x]` in `phase-3-web-ui/steps.md` and `UPGRADE/ROADMAP.md`; documents the chat-page patterns (bubble layout, auto-scroll-pin, hand-rolled markdown subset, /cmd shortcut path, per-turn stream lifecycle) in `apps/factory-web/src/components/README.md` "Patterns introduced by /app/chat (3.5)" section so the next streaming-page author has a reference. Carried-forward 2.6 (`factory chat` per-turn timeout) closes implicitly: WEB chat page subscribes to SSE and sees per-token progress so the original false-timeout pain is addressed for the recommended chat surface. CLI's `TURN_TIMEOUT_MS=120_000` stays as-is intentionally — the cheap-bump path tier-2 §2.6 offered is no longer load-bearing now that the WEB surface exists, and removing the gate outright would regress to "CLI hangs on brain crash."
- ADRs decided: none. ADR 0029 (directive-stream protocol) still gated on FE consumer being smoke-tested end-to-end against a real factoryd; the chat-page surface in 3.5 is the second consumer of the SSE schemas after `directives/detail.astro` — the protocol is now exercised by two distinct page paths, which strengthens the case for promotion once the live smoke confirms wire fidelity.
- Issues opened / closed: none.
- Minor fixes: none.
- Blockers hit: none. The chat-route auth move from `/directives/:id/cancel` (no `/api/v1/` prefix) to a UI-token-gated namespace surfaced as a 3.6 design call rather than a 3.5 blocker — phase-3 steps.md line 8 frames cancel as hitting `POST /api/v1/directives/:id/cancel` but the existing route is at `/directives/:id/cancel`; deferred to 3.6 with the option ladder spelled out in STATE.md "Notes for next session." Pause is a similar deferred design call: phase-3 steps.md frames it as "a status flip via existing route" but no pause primitive exists today — STATE.md surfaces three options (extend directiveStatus, reuse markBlocked, defer pause) for 3.6's session start.
- All four `pnpm` gates green at every commit and at session end. Workspace test count went +13 (brain +4 from `loop.test.ts`, daemon +9 from chat-route tests). `factory-web` still has no vitest harness — chat.astro is integration-tested only by the daemon's chat-route tests + manual smoke. Visual-regression / live-smoke against a running factoryd is pinned as part of phase-3 acceptance, not a 3.5 prerequisite.
- Three deferred items remain on the 3.x backlog (no 3.6+ acceptance dependency): (i) PageShell + Dashboard `<style is:global>` follow-up (3.4 carryover); (ii) `finding.created` brain emission — the 3.1 deferred-work counterpart to `log.line` which shipped in `10efe20`; (iii) pre-3.5 baseline live-smoke against a running factoryd (open a real browser to `/app/directives/detail?id=…` AND `/app/chat` during a build run).
- Loose ends still open from prior session (not blocking 3.6): synthetic smoke directive `01KQPDMQE6QTQZ3QMDD69019YK` + `demo-project` in DB; factoryd PID 32436 may still be running.
- Next: step 3.6 — cancel + pause buttons on directive detail. Cancel is straightforward (existing IPC route from 2.4, FE handler, UI-token-gate move from `/directives/:id/cancel` → `/api/v1/directives/:id/cancel`). Pause is the open design call — STATE.md spells out three options (extend directiveStatus / reuse markBlocked / defer); recommendation is defer-pause-ship-cancel-only.

## 2026-05-03 — Step 3.4 shipped (all 10 pages converted to component library; el() retired)

- Step range 3.4 across `54c0f20..dfd1a07` (8 commits including a session-start drift reconcile).
- Session-start drift reconcile (`54c0f20`) — third occurrence of the post-session-end self-reference drift (`cce7065`/`db61baf` were the prior two): STATE.md "Last commit" pointed at `94b8b71` (the 3.3 feat) but actual HEAD was the `4466078` session-end docs commit. First reconcile attempt amended the SHA, which changed the commit's SHA and reproduced the drift; soft-reset and recommitted as a single non-self-referencing entry matching the established `db61baf` shape ("Last commit" → the session-end commit; "State reconcile" entry → the prior reconcile, not itself). Worth a small `/session-end` skill fix (track "last work commit" rather than HEAD, or amend STATE.md post-commit) to break the recurrence — flagged in the reconcile commit body, not addressed here.
- Step 3.4 spans 7 page-conversion commits + this final closing commit:
  - `32bdfb6` index.astro → `<Card>` × 5; first `id?` extension on a 3.3 component (the runtime-fetch placeholder pattern that turned out to be load-bearing for every list page).
  - `d55c41d` findings/index.astro + Table extension (`id?` + `loading?`); chrome rendered server-side, script replaces tbody on resolve, empty result → single colspan'd `<td class="empty">` row.
  - `a876608` projects/questions/spend list pages; projects empty-state hits dedicated `<Alert kind="info">` per migration map; spend page's four sub-tables share a per-page `fillTable<T>` helper.
  - `e849aa7` directives list + project/question detail pages; introduces the hidden-Alert-placeholder pattern for dynamic conflict/success swapping (one Alert per kind, server-rendered hidden, script reveals via `hidden=false` and writes textContent into inner h4/p).
  - `58d4584` build.astro → `<Form>` + `<Field>` + `<Submit>` (the primary form use case); project select `options={[]}` server-side, script appends one `<option>` per fetched project then rewrites the `Loading projects…` hint paragraph.
  - `a405556` directives/detail.astro inlines `el()` as a per-page helper (the live SSE render path's per-page DOM helper exception per the migration map).
  - `dfd1a07` retires `el()` and `loadInto()` from `lib/api.ts`; flips `[ ] 3.4` → `[x] 3.4` in steps.md and ROADMAP.md; documents the Dashboard-CSS scoping discovery + deferred PageShell decision in components/README.md.
- ADRs decided: none (refactor-only step).
- Issues opened / closed: none.
- Minor fixes: README format issue auto-fixed by `prettier --write` after the components/README.md edit; soft-reset of the failed self-amend reconcile attempt.
- Blockers hit: self-reference in the reconcile commit (described above). Found mid-conversion that Astro's scoped CSS doesn't propagate `data-astro-cid-*` to slot content — Dashboard's class-based rules (`.cards`, `.card`, `.empty`, `.err`, `.btn*`, `.alert*`, `.form-*`, table-base) only match elements rendered directly inside Dashboard's own template (just the `<header class="shell">` chrome and the inner `<h2>`). Decided not to prune those rules during 3.4 (they're already inert for slot content; pruning is safe but the future `<style is:global>` follow-up is what would actually make them load-bearing); recorded in components/README.md.
- All four `pnpm` gates green at every commit and at session end. Test count holds at the prior baseline (state 152, channels 175, daemon 152, brain 93, worker 38, worker-sandbox 86 + 3 skipped, assessor 79, wiki 64, cli 82, providers 39, ipc 28, events 3 — same as last STATE.md baseline of 1040 across 73 files). 3.4 added zero test files (page conversions; `factory-web` still has no vitest harness).
- Two items deferred during 3.4, filed as 3.x backlog rather than 3.5 blockers: (i) `<PageShell>` adoption across all 10 pages — coupled to removing Dashboard's inner `<h2>{title}</h2>` so we don't get double `<h2>`s; (ii) Dashboard primitives (`.cards`, `.empty`, `.err`, `.btn*`, etc.) → `<style is:global>` so they actually apply to slot-level elements. Both fit cleanly in one focused follow-up commit.
- Step 3.1 deferred work (still open): `finding.created` and `log.line` brain emission. 3.5 specifically needs `log.line` (one bubble per agent message in the chat page); pin `log.line` emission as part of 3.5's scope OR a 3.5-prerequisite mini-step.
- Loose ends from prior sessions still open (not blocking 3.5): synthetic smoke directive `01KQPDMQE6QTQZ3QMDD69019YK` + `demo-project` in DB; factoryd PID 32436 may still be running.
- Next: step 3.5 — `/app/chat` page (browser mirror of `factory chat`); reuses Phase 2's `command-handlers.ts` for `/cmd` shortcut path; SSE stream consumed via `apiStream` helper from 3.2.

## 2026-05-03 — Steps 3.2 + 3.3 shipped (SSE FE wiring + Astro component library)

- Step range 3.2-3.3 across `db61baf..94b8b71` (3 commits including a session-start drift reconcile).
- Session-start drift reconcile (`db61baf`) — same post-session-end pointer drift as `cce7065` last session: STATE.md "Last commit" pointed at `772f9f3` (the 3.1 feat) but actual HEAD was the `15bbad3` session-end docs commit. Caught by the SessionStart hook's `[control:drift] commit-mismatch` block; reconciled by updating STATE.md's Git state pointer + adding a new "Recently completed" entry for `15bbad3`. The reconcile commit itself reproduces the same one-behind shape — structural to the no-amend protocol.
- Step 3.2 (`998e7d8`) — `feat(3.2): wire directive detail page to SSE stream`. New `apiStream<T>(path, callbacks)` helper in `apps/factory-web/src/lib/api.ts`: token-auths via `?t=<UI_TOKEN>` query param (EventSource cannot set custom headers), Zod-validates each payload via `directiveStreamEventSchema`, surfaces a six-state connection machine (connecting → live → reconnecting → polling | disconnected → completed), degrades to a 5 s polling fallback when EventSource gives up retrying. `directives/detail.astro` rewritten: incremental `Map<taskId, Task>` render, atomic spend swap, log tail panel rendered only after the first `log.line` arrives, connection-state pip in the header (subtle pulse on green when live, blink amber when reconnecting, dashed grey when completed). Polling-fallback merge rule prefers existing live-event Map entries over snapshot rows so a brief polling window can't clobber a finer-grained `task.completed` status with a stale `running` row.
- New `./sse` sub-export on `@factory5/ipc` keeps `undici` / `@factory5/logger` out of the FE bundle: package.json gains the sub-export entry; tsup build script gains `src/sse.ts` as a second entry; emits `dist/sse.js` + `dist/sse.d.ts` alongside existing `index.js`. `apps/factory-web` adds `@factory5/ipc: workspace:*` to dependencies.
- Step 3.3 (`94b8b71`) — `feat(3.3): astro component library — Card / Table / EmptyState / Alert / Form / PageShell`. Eight new server-rendered components under `apps/factory-web/src/components/` (Card, Table, EmptyState, Alert, Form, Field, Submit, PageShell). Each ships with a typed `interface Props`, scoped `<style>` mirroring `Dashboard.astro`'s `color-mix(in srgb, currentColor X%, transparent)` palette + form/btn/alert/card class styles, and TSDoc'd intent. Component README documents conventions + per-component usage examples + the 3.4 migration map.
- 3.3 is **library-only** — no page consumes the components yet. Visual is unchanged because each component duplicates the matching CSS from `Dashboard.astro` under Astro's auto-scoping. The Dashboard-level CSS gets gutted in 3.4 once every page has migrated.
- Tests: 0 new tests this session. Workspace test count holds at 1040 across 73 files (the prior STATE.md's "76" was a slight miscount — actual is 73; the 3.1 session added `sse.test.ts` + `directive-stream-route.test.ts` for +2 files vs. earlier baseline, putting us at 73). The SSE FE wiring relies on the existing 152 daemon SSE-route tests for wire shape; `factory-web` has no vitest harness yet (deferred — adding jsdom + EventSource polyfill is a multi-file change for one test file's worth of value).
- ADRs decided: none. ADR 0029 (directive-stream protocol) still gated on FE consumer being smoke-tested end-to-end against a real factoryd, which lands as part of 3.4's acceptance smoke.
- Issues opened / closed: none.
- Minor fixes: textarea whitespace handling in `Field.astro` — Astro preserves whitespace inside `<textarea>` so a multi-line expression body would leak indent as the field's initial value; switched to `set:html` for the textarea body.
- Blockers hit: 11 ts(18047) "mount is possibly null" errors in `detail.astro` after the rewrite — TypeScript narrowing didn't propagate `if (mount === null) throw` into closure-scoped function bodies. Fixed by introducing a typed non-null local `const mount: HTMLElement = mountEl` after the throw guard. Single-line README format issue auto-fixed by `prettier --write`.
- All four `pnpm` gates green at every commit: build / test (1040 unchanged) / lint / format:check.
- Step 3.1 deferred work (still open): `finding.created` brain emission and `log.line` forwarder. The FE now has the listener wiring in place via 3.2 (Map mutation + log tail append, both no-op until events flow). Either land in a 3.x sub-step or defer to a follow-up tier — neither blocks 3.4.
- Loose ends still open from prior session (not blocking 3.4): synthetic smoke directive `01KQPDMQE6QTQZ3QMDD69019YK` + `demo-project` in DB; factoryd PID 32436 may still be running.
- Next: step 3.4 — convert all 10 pages in `apps/factory-web/src/pages/` to consume the component library; retire `el()` from `lib/api.ts`; gut the duplicated CSS from `Dashboard.astro`.

## 2026-05-03 — Step 3.1 shipped (SSE backend + brain emission)

- Step range 3.1 across `cce7065..772f9f3` (2 commits).
- Session-start drift reconcile (`cce7065`) — STATE.md "Last commit" pointed at the prior post-phase-2 docs commit (`f7c78ce`) but actual HEAD was the `733ce5a` session-end bookkeeping commit. Caught by the SessionStart hook's `[control:drift] commit-mismatch` block; reconciled by updating STATE.md's Git state pointer (the new commit reproduces the same one-behind shape, but it's structural — every STATE.md-only commit lands one behind itself unless you `--amend`, which CLAUDE.md forbids).
- Step 3.1 (`772f9f3`) — `feat(3.1): SSE on /api/v1/directives/:id/stream`. Six-event protocol pinned in `UPGRADE/specs/sse-directive-stream.md`. Six Zod event schemas + discriminated union in new `packages/ipc/src/sse.ts`. `DirectiveStreamHub` in `packages/daemon/src/directive-stream.ts` (subscribe / emit / closeDirective / shutdown; synchronous dispatch so a slow consumer can't backpressure the producer). Fastify SSE handler in `packages/daemon/src/directive-stream-route.ts` via `reply.hijack()` over `reply.raw` — auth via either `Authorization: Bearer …` or `?t=<token>` query param (browsers' EventSource cannot set headers); 404 / 401 / 503 pre-stream errors; backfill on connect synthesizes `task.started` (+ `task.completed` for terminal tasks) plus a baseline `spend.updated`; already-terminal directives short-circuit with one `directive.completed` and stream close; 15 s heartbeats; cleanup on `request.raw.on('close')`. Brain emission via new `BrainOptions.emitDirectiveEvent` callback typed as `DirectiveEventEmitter` from `@factory5/ipc` — daemon's `brain-supervisor` wires it to `hub.emit`; threaded through `runServe` → `defaultRunOne` → inline `runBrain` so both inline and serve paths emit. Pool emits `task.started` / `task.completed` / `spend.updated`; loop emits `directive.completed` at all six terminal-status branches (chat / non-build / architect-abort / planner-abort / normal / budget-exceeded).
- **Deferred from 3.1:** `finding.created` and `log.line` brain emission. Spec carries the event shapes, route forwards them, schemas + tests cover them. `finding.created` is a small follow-up (one event per entry in `outcome.result.findingsRaised` from `pool.ts`); `log.line` is more invasive (selective pino-stream tap) and may defer indefinitely. FE will refresh findings on each `task.completed` until `finding.created` is wired.
- Tests: 14 new schema cases in `packages/ipc/src/sse.test.ts` + 15 new end-to-end SSE route cases in `packages/daemon/src/directive-stream-route.test.ts` driving a real bound socket via Node's streaming `fetch` body (auth, 404, backfill on connect for in-flight + terminal tasks, already-terminal short-circuit + `blockedReason` forwarding, live `hub.emit` round-trip, directive scoping (events for one don't leak to another), heartbeat after idle with a small heartbeat interval, listener cleanup on client disconnect via `hub.listenerCount`).
- Workspace test count went from prior baseline to 1040 across 76 files (+29 new tests this session). Daemon 137 → 152; ipc 14 → 28. All four `pnpm` gates green.
- ADRs decided: none. Spec lives at `UPGRADE/specs/sse-directive-stream.md`; promotion to **ADR 0029 — directive-stream protocol** held until 3.2 ships and the FE consumer validates the contract end-to-end.
- Issues opened / closed: none. Step 3.1 was infrastructure-only.
- Minor fixes: added `.serena/` to `.gitignore` (Serena MCP server's `activate_project` drops a per-operator project descriptor there; not shared, not part of the build).
- Blockers hit: none. Two ESLint `prefer-const` errors during the lint gate from a `let unsubscribe` / `let heartbeatTimer` pattern in the SSE route handler — refactored to a `const subscription = { unsubscribe?: () => void }` holder so the late-bound subscription handle could be assigned without a `let` binding, and made `heartbeatTimer` `const` from the start.
- Step 2.6 (`factory chat` per-turn timeout) carryforward: infrastructure ready via this SSE stream; full resolution lands in 3.5 when `/app/chat` routes chat directives through it. Until then the 120 s false-timeout could still bite a chat directive that takes longer than 120 s for the first turn — same state as before this session.
- Loose ends still open from prior session (not blocking 3.2): synthetic smoke directive `01KQPDMQE6QTQZ3QMDD69019YK` + `demo-project` in DB; factoryd PID 32436 still running.
- Next: step 3.2 — wire `directives/detail.astro` to the SSE stream + add `apiStream<T>(path)` helper to `lib/api.ts`.

## 2026-05-03 — Session-end after phase-2 close + post-close docs polish

- Single small commit landed after `/phase-close`: `f7c78ce` `docs(2): document factory/factoryd PATH setup in ONBOARDING`. Closes a doc gap surfaced when operator asked "where do I run `factory daemon stop`?" — the existing onboarding doc said "once factory5 is on your `$PATH`" without ever explaining how to put it there.
- Working tree clean at session-end. HEAD = `f7c78ce`. Phase-2 tag on `081b832`.
- Synthetic smoke data still resident in DB; `packages/state/smoke-cleanup.mjs` available. Daemon (PID 32436) still running.
- No blockers. Next session opens at Phase 3.1 (SSE on `/api/v1/directives/:id/stream`).

## 2026-05-03 — Phase 2 closed (tag `phase-2-channel-parity-closed` on `081b832`); Phase 3 kicked off

- Steps 2.3, 2.4, 2.5 all shipped through prior-session work; Step 2.6 deferred to Phase 3 (folded into SSE streaming).
- This session: completed live-smoke checklist for `/phase-close` — Discord+Telegram slash command surfaces verified, free-form chat re-routing verified on both, `factory cancel` IPC route paths verified (NOT_FOUND/ALREADY_TERMINAL/OK), CLI exit codes 0/2/3 verified.
- Out-of-step UX fix: `fix(2.2): show project name in /status output` (`081b832`) — operator complaint that the recent-directives table showed only directive IDs, not which project they belonged to. Added a project column to Discord embed, Telegram HTML reply, and CLI table; new shared `makeProjectNameLookup` helper.
- Tag annotated with full phase-2 shipping summary.
- Phase 3 scaffold landed: `.control/phases/phase-3-web-ui/{README.md, steps.md}` with carry-forward of Step 2.6 in "Why this phase exists".
- Synthetic smoke directive remains in DB (id `01KQPDMQE6QTQZ3QMDD69019YK`, status=failed/cancelled). `packages/state/smoke-cleanup.mjs` available if user wants to reap; permission-gated via Bash settings.
- Gates: build / test (175 channels, 82 cli, full workspace ≥ 938) / lint / format:check all green.
- Next: Step 3.1 — SSE on `/api/v1/directives/:id/stream`.

## 2026-05-02 — Phase 2 session 2a — slash + setMyCommands

- Step range 2.1-2.2 across `8ea8e4a..22e0e54` (2 commits + this session-end).
- Step 2.1 (`8ea8e4a`) — Discord slash commands. New `discord-commands.ts` (single `/factory` command with seven subcommands: status / spend / findings / resume / cancel / budget / build). New `setProjectBudget` callback on `ChannelContext` + `SetProjectBudgetError` sentinel; daemon binds it over `wiki.updateProjectMetadata`. 23 unit tests added covering every subcommand, allow-list gate, and major error paths. Closes structural piece of U011 (live-smoke still required at phase-close).
- Step 2.2 (`22e0e54`) — Extracted transport-agnostic `command-handlers.ts` (each handler returns either typed data or `CommandResult<T>` for user-visible failures with a stable `code`). Refactored `discord-commands.ts` to delegate. Telegram side: added `setMyCommands` to `TelegramApi` (optional in contract; default HTTP factory provides), `parseMode` on `sendMessage`, slash dispatcher replacing the old `/build`-only parser, HTML-mode formatter with `<pre>` blocks for tabular reads. 10 new Telegram tests. Closes structural piece of U012 (live-smoke pending). Side effect: `payload.text` is no longer set on build directives — confirmed unused, dropped one roundtrip-test assertion.
- ADRs decided: none (judgement calls within tier-2 plan, not architectural).
- Issues opened / closed: U011 + U012 partially addressed (structural pieces shipped; live-smoke acceptance held until `/phase-close`).
- Minor fixes: `.claude/hooks/regenerate-next-md.ps1` UTF-8 round-trip fix — read with `-Encoding utf8`, write without BOM via `UTF8Encoding $false` for parity with the bash sibling. Logged as a known bug at end of step 2.0; fixed during the 2.2-2.3 idle window per the "Phase 2 idle" note in last STATE.md.
- Blockers hit: none.
- Gates: build / test (938 total, 103/103 channels) / lint / format:check all green.
- Next: step 2.3 — pending-question button affordances.

## 2026-05-02 — Phase 1 closed; Phase 2 kicked off
- Phase 1 (doc-sweep) closed: tag `phase-1-doc-sweep-closed` on `10e400a`; close commit `1384ae8`.
- Step range 1.1-1.8 across `91541a9..10e400a` (9 commits, doc-only).
- Issues closed: U001, U002, U003, U014, U015, U016, U017 (7 of 23 catalogued; all Tier-1).
- ADRs decided: none (doc-only phase, as anticipated).
- Minor fixes: orphan `factory inspect` reference removed from `packages/logger/README.md` while sweeping for broken refs.
- Blockers hit: none. Hook drift at session start (commit-mismatch + tag-mismatch) reconciled via `91541a9` before any step work began.
- All four `pnpm` gates green throughout (build, test 876p/3s, lint, format:check).
- Next: step 2.1 — wire Discord slash commands.

## 2026-05-02 — Session bootstrap
- Control framework v2.2.1 installed (commit `e94393e`); `/bootstrap` populated SPEC + phase plan + Phase 1 scaffold.
- Next: Phase 1 doc-sweep.
