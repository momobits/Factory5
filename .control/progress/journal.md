# Journal

Append-only, newest on top. One entry per session, short. Minor fixes land here as one-line entries (see Issue flow in `.control/PROJECT_PROTOCOL.md`).

## 2026-05-07 (latest) — Phase 5 closed; sub-steps 5.1 → 5.8 + /phase-close in one session; upgrade arc complete (again)

- Single-session continuation that picked up post-compact at HEAD `120d8ed` (the prior session-end of the kickoff). Operator path: `/session-start` narrate drift (commit-mismatch was the well-known lag-by-1) → "go" → 5.1 → 5.2 → 5.3 → 5.4 → 5.5 → 5.6 → 5.7 → 5.8 → /phase-close. Eight work commits + this docs(state)-shaped phase-close commit.
- **Per-step commit shas:** 5.1 `8fb3b29` chore(5.1) open U024+U025 · 5.2 `e08f062` docs(5.2) prompts/agents/README.md sweep (closes U024) · 5.3 `27dc6c7` docs(5.3) ONBOARDING §5.4 sweep (closes U025) · 5.4 `21bf980` docs(5.4) reviewer.md factory5-native body · 5.5 `839c2c1` docs(5.5) fixer.md (branch 3 prose-only) · 5.6 `ae47147` docs(5.6) investigator.md · 5.7 `005e75b` docs(5.7) builder.md flesh-out · 5.8 `59a684f` chore(5.8) retire factory logs stub · /phase-close `[this commit's sha]` chore(phase-5) close.
- **Pre-write homework, both branches resolved without ADRs.**
  - 5.4 reviewer findings policy: grepped `packages/wiki/src/findings.ts:130` → `resolveAdvisory` auto-defaults `advisory: true` only for `source: 'verifier'`; every other source (reviewer included) defaults to blocking. Runtime is unambiguous; no ADR 0030 needed. Operational caveat captured in the prompt body: brain's `hadFailures` (`packages/brain/src/loop.ts:435-438`) is gated on assessor `gate.verify` + task exit codes, not finding count, so "blocking" is operator-visibility distinction rather than auto-stop.
  - 5.5 fixer output contract: grepped `packages/brain/src/` + `packages/worker/src/` for any agent-output → `updateFindingStatus` parser path. Result: **NO PARSER EXISTS**. The wiki API (`packages/wiki/src/findings.ts:196`) is only invoked from tests. No CLI `factory findings mark` command either. Branch 3 chosen (prose-only); `RESOLUTION <FID> (FIXED|VERIFIED|WONTFIX)` marker grammar pinned as future-parser lock-on shape; commit type stayed `docs(5.5)`. Tier 6 candidate: wire the parser.
- **Path B chosen for 5.8** (`factory logs` retire) per plan default + auto-mode "make reasonable assumptions on routine decisions". Reasoning preserved in commit body: feature work mid-tier was the kind of scope creep the plan's "Single-session scope" risks-and-decisions called out. Deleted `packages/cli/src/commands/stubs.ts` (single-purpose file), removed `registerStubCommands` from `cli.ts`, dropped row from CLI README, dropped `'logs'` from completion vocab. ADR 0002 footnote about `factory logs` flagged but unedited (CLAUDE.md "do not edit accepted ADRs"); supersede-via-new-ADR is over-engineering for one footnote.
- **Doc drift surfaced and flagged for follow-up (not in scope of any 5.x step):**
  - `docs/ONBOARDING.md` §6.4 says outbound_messages reach the SPA via "polling fetch"; chat.astro consumes SSE for token-by-token reply rendering today (post-Tier 3.5).
  - `docs/ONBOARDING.md` §6.1 says U005's 120s chat timeout "may be extended in Tier 2 or Tier 4"; both shipped without addressing it. U005 itself still Open (re-tier candidate).
  - ADR 0027 §1's pinned route table doesn't list the `POST /api/v1/projects` route that Tier 3's projects/new actually uses. Route exists at `packages/daemon/src/server.ts:923`; ADR-amend candidate.
- **builder.md byte-for-byte preservation verified** via `git diff prompts/agents/builder.md | grep ^-` — only 4 removed lines: 3 from old frontmatter description (rewritten) + the `> **Phase 1 stub.**` marker. Both load-bearing sections (BUILD.md prohibition + Python venv discipline) preserved.
- **Tier 5 line counts after prettier:** reviewer.md 156 / fixer.md 158 / investigator.md 140 / builder.md 185. All satisfy plan acceptance "comparable in depth to verifier.md (97) or architect.md (79); the substantive criteria above are the gate, not a line count." Substantive content (skill references, advisory-vs-blocking explanations, OK/NOT-OK Bash example lists, marker grammar with parser citations) accounts for the over-runs.
- **No new ADRs.** Pre-write homework for 5.4 + 5.5 both confirmed runtime contracts that didn't need pinning (verifier-vs-reviewer policy is unambiguous in `findings.ts:130`; fixer parser path simply doesn't exist).
- **Issues opened / closed:** opened U024 + U025 in 5.1; closed both as part of 5.2/5.3 work. ISSUES.md "Open" now contains only U005 again. Sha-backfill for U024 → `e08f062` and U025 → `27dc6c7` landed in this phase-close commit (per Tier 5 plan acceptance "marked Resolved with this commit's sha"; the commits themselves landed under the lag-by-1 convention with date+step refs only, then backfilled at /phase-close).
- All four `pnpm` gates green pre-commit at every step's close. Workspace held at **1135 + 3 skipped** throughout — Tier 5 had no test deltas (5.1–5.3 doc-only; 5.4–5.7 markdown-only; 5.8 deleted untested code).
- **14th `/session-end`-shape lag-by-1 occurrence** lands at this phase-close commit. The pattern continues unchanged: STATE.md inside the commit references `59a684f` (the prior 5.8 commit) while HEAD will move to this phase-close commit's sha. Worth noting that phase-close commits trigger lag-by-1 just like session-ends and kickoffs do (any docs(state)-shaped commit; see Phase 4 close commit `28c0188` for the self-referential variant where STATE.md inside the commit references its own sha — the alternative pattern).

## 2026-05-07 — Phase 5 (agent-prompts) kicked off post-arc; README rewrite + scaffold bundled

- Single-session post-arc continuation that picked up at HEAD `f3fd6ed`. Operator path: fullscan vs README → README rewrite → agent-prompt audit → Tier 5 plan → 4 plan revisions → scaffold + kickoff bundle → /session-end. One work commit + this docs(state) commit.
- **`c0869d6 chore(phase-5): scaffold phase 5 + refresh README + state cursor`** — 8 files changed, +727/-121. Bundled the README rewrite into the kickoff commit because the commit-msg hook rejected `(readme)` as a parens scope (only `[0-9]+\.[0-9]+`, `phase-N`, `adr|issues|state|spec|install`, `[A-Z]\.[0-9]+`, `reconcile` allowed). Worth noting for future operators landing non-phase-scoped doc/refactor work.
- **README day-1 rewrite (~145 lines, 2 mermaid diagrams).** Replaced the 73-line "build the binaries" entry-point. Architecture diagram shows SQLite as the bus across CLI / Discord / Telegram / Web; operator-flow tree covers the 4 canonical loops. Quick start gets reader to a real first build (`pnpm factory doctor` → `pnpm factory build hello-cli`). Surfaces & permutations table for 5 channels / 3 autonomy modes / 4 runtimes / 5 providers / 2 storage modes. ONBOARDING + WORKFLOWS pulled to top of doc index.
- **Agent-prompt audit findings.** Of 9 prompts in `prompts/agents/`: **5 substantive** factory5-native (triage / architect / planner / scaffolder / verifier — README falsely flags them all as "stub"); **1 hybrid** (builder — has factory5-native venv discipline + a stale "Phase 1 stub" marker); **3 pure stubs** (reviewer / fixer / investigator, 10 lines each). Plus stale docs: `prompts/agents/README.md` status table; `docs/ONBOARDING.md` §5.4 ("read-once" + "no SPA project creation" — both shipped past in Tier 3). Plus one CLI stub-by-design: `factory logs` prints a hint and exits.
- **Tier 5 plan + scaffold landed.** `UPGRADE/plans/tier-5-agent-prompts.md` (9 sub-tasks; runtime-contract verification branches per step where contracts were unverified); `UPGRADE/ROADMAP.md` Tier 5 section (7 deliverable rows + Tier 6 skills-rewrite candidate noted in Out-of-scope); `.control/architecture/phase-plan.md` Tier 5 row + summary; `.control/phases/phase-5-agent-prompts/{README.md,steps.md}`. User directive "build new for factory5, don't port from factory2" pinned in plan goal + each prompt-write step's framing.
- **Operator pushback caught 4 plan corrections** before scaffolding: (1) drop the prompts README status column entirely instead of "fixing" it (post-Tier-5 the column carries no signal and invites stale-tracking); (2) 5.5 fixer needs a three-branch verification preamble — initial draft asserted a `RESOLUTION` marker the brain doesn't actually parse; only `wiki/findings.ts:189`'s `markFinding` API is real; 5.5 must verify the agent-output → `markFinding` parser path before writing; (3) 5.6 investigator markers reframed "parsed by the brain" → "operator-readable structural conventions" (no parser exists; don't promise runtime persistence); (4) loosen line-count floors to depth-comparison ("comparable to `verifier.md`/`architect.md`"). Plus on second pass: 5.1 trimmed to just "open U024 + U025" — ROADMAP rows belong to scaffold-time per Tier 4 convention.
- **Runtime contracts grepped during plan revisions** — `pool.ts:111-132` propagates `finding.advisory` and `finding.source` as-set on insert (default `advisory: false` = blocking); `wiki/findings.ts:189` `markFinding` API is real but no agent-output → `markFinding` parser path was located (open homework for 5.5). Confirmed `legacy/code-reviewer.md` + `legacy/test-runner.md` not loaded by `packages/brain/` (registry uses bare filenames; no `legacy/` paths anywhere in code).
- **No new ADRs.** ADR 0030 likely candidate if 5.4's reviewer-finding-policy reading shows the contract is genuinely ambiguous.
- **Issues opened / closed: none.** U024 (prompts/agents/README.md status table is stale) and U025 (docs/ONBOARDING.md §5.4 read-once claim is stale post-Tier-3) are queued for sub-step 5.1 to open in the next session.
- All four `pnpm` gates green pre-commit at `f3fd6ed` and at `c0869d6`. Workspace held at **1135 + 3 skipped** throughout — kickoff bundle is docs/scaffold only; no test deltas.
- **13th `/session-end`-shape lag-by-1 occurrence** lands at this commit. The kickoff `c0869d6` was the 12th — the first non-/session-end commit to trigger lag-by-1 (same root cause: a docs(state)-shaped commit's STATE.md references the previous HEAD while landing the new HEAD). Worth noting for the structural fix: kickoff commits hit the same pattern as session-ends.

## 2026-05-06 — Session end after Phase 4 close (final upgrade-arc session)

- Single-session continuation that picked up post-compact at HEAD `a1cacfa`, advanced through 4.7 → 4.8 → /phase-close (4.9) on operator "proceed" signals. Three work commits + this session-end docs commit.
- **Step 4.7 (`4902480`):** `packages/cli/README.md` refresh adding the five new (or newly-verified) commands shipped in the prior session — cancel, ask, budget set, project (list/show/delete), completion — plus a top-level Tab completion section, a WORKFLOWS.md cross-reference, and exit-code blocks aligned with the implementations.
- **Step 4.8 (`1d1f6a9`):** U018 / U019 / U020 / U021 moved Open → Resolved with full Resolution lines pointing at this arc's commits (`91eebca` / `9340cfd` / `9da25ba` / `fa28e6d`). Tier 4 ROADMAP rows already ticked in the per-step work commits — no edits there, just verification.
- **Step 4.9 (`28c0188`):** `/phase-close` on Phase 4. All 11 done-criteria green; no `phase:4-blocker` issues; all four `pnpm` gates clean. No Phase 5 scaffolded — `phase-plan.md` defines only Phases 1-4. Annotated tag `phase-4-cli-completion-closed` at the close commit. **Final upgrade-arc tag.**
- All four `pnpm` gates re-verified post-4.7 (build/lint/format/test), post-4.8 (format), and at /phase-close (format/build/lint/test). Workspace test count held at **1135 + 3 skipped** throughout — 4.7 / 4.8 / 4.9 are docs/state-only with no test deltas.
- This session-end commit creates the **11th occurrence** of the documented `/session-end` skill lag-by-1 (STATE.md references `28c0188` while HEAD will move to this docs commit). The phase-close commit was self-referential (STATE.md inside `28c0188` references `28c0188`), so it didn't itself trigger lag-by-1 — only this trailing session-end commit does.
- No new ADRs.
- Issues opened / closed: closed U018, U019, U020, U021. UPGRADE/ISSUES.md "Open" now contains only U005 (`factory chat` 120 s turn timeout — out-of-upgrade-arc scope).
- **Carry-forward at session/arc end:** Pause primitive (defer-until-signal); PageShell + `<style is:global>` migration (1-commit sweep); brain-side `log.line` forwarder (ADR 0029 future-work); chat-page click-test; U005 chat 120 s timeout; Control framework 2.2.3 publish; `/session-end` skill structural fix. None gating any current work.
- **Auto-mode session shape worth recording.** The session ran in auto mode after the operator gave one initial "proceed" — three steps closed without further per-step prompting, including the destructive-feeling /phase-close (annotated tag). The runbook clarity made this safe: each step had explicit acceptance criteria, the gates were re-verified at the right boundaries, and the close commit's done-criteria check was 11/11 before tagging. The pattern works for end-of-arc steps where the cursor is mechanical; it would not have been right for any step that needed operator judgement (a new ADR, a UX call, a destructive cleanup).
- Working tree clean post session-end commit. Kickoff prompt at `.control/progress/next.md` regenerated for the post-arc parking state (three options: open a new arc; promote a carry-forward item; park).

## 2026-05-06 — Phase 4 closed (tag: `phase-4-cli-completion-closed`); upgrade arc complete (no Phase 5 scheduled)

- `/phase-close` ran on Phase 4 work. All nine sub-steps shipped (4.1 → 4.8 in this session and the prior, plus this 4.9 close); steps.md fully ticked.
- Done-criteria verification: 11/11 green. All four `pnpm` gates clean; no `phase:4-blocker` issues open; new commands all unit-tested (ask 7 / budget 15 / project 22 / completion 9 / cancel 7 + help-coverage 2); tab completion produces valid bash/zsh/pwsh scripts (gated by `completion.test.ts`); every `factory <cmd> --help` shows worked examples (gated by `help-coverage.test.ts`); U018-U021 already moved to Resolved in 4.8 (`1d1f6a9`); README refreshed in 4.7 (`4902480`); tree clean; commit shapes all match `<type>(4.<step>): <subject>`.
- **No Phase 5 scaffolded.** `phase-plan.md` defines four phases (doc-sweep / channel-parity / web-ui / cli-completion); the upgrade arc is complete and STATE.md transitions to "all phases complete". Phase 4 README's `## Deferred to Phase 5 (or later)` section uses the `<item>` placeholder verbatim, so no carry-forward bullets get seeded into a non-existent Phase 5 README.
- **Issues moved Open → Resolved across the upgrade arc:** Tier 1 (U001, U002, U003, U014, U015, U016, U017); Tier 2 (U004, U011, U012, U013, U023); Tier 3 (U006, U007, U008, U009, U010, U022); Tier 4 (U018, U019, U020, U021). Twenty-three issues total. ISSUES.md "Open" now contains only **U005** (`factory chat` REPL turn timeout 120s — out of upgrade scope, sized as a future Tier 2/4 follow-up if a demand signal surfaces).
- Annotated tag `phase-4-cli-completion-closed` at the close commit. Final upgrade-arc tag.
- Tier 4 ROADMAP rows already ticked alongside per-step work commits — `/phase-close` had nothing to do there; the close itself is the wrap.
- All four `pnpm` gates green at /phase-close verification. Workspace test count unchanged from end-of-4.6: 1135 passing + 3 skipped (4.7 / 4.8 / 4.9 are docs/state-only).
- **Carry-forward items deferred to operator follow-up** (none load-bearing): Pause primitive on directive detail; PageShell + `<style is:global>` migration (11-page sweep absorbing filter-form Apply / "Clear all defaults" + inline-style audit); brain-side `log.line` forwarder (ADR 0029 future-work); chat-page click-test (final piece of Phase 3.5 baseline); U005 chat 120s timeout; Control framework 2.2.3 publish; `/session-end` skill structural fix for the lag-by-1 (now **11 occurrences** counting this close commit).
- **Lesson worth recording:** the upgrade-arc finale lands cleanly when the per-step disciplines stayed true throughout — every step had its commit, ROADMAP tick happened in the work commit (not deferred to a sweep at phase close), and most issues moved Open → Resolved in the per-step close commits as each was earned. The 4.8 verification step found nothing to fix structurally, which is the marker of a healthy arc. Phase-3's close commit had to absorb six unmoved issues (U006-U010 + U022); Phase-4's didn't, because the discipline carried.
- **Session shape:** auto-mode session that picked up post-compact at HEAD `a1cacfa`, executed 4.7 → 4.8 → 4.9 in sequence on operator "proceed" without further per-step prompts. Three commits: `4902480` (4.7) + `1d1f6a9` (4.8) + this phase-close commit. Each ran the four pnpm gates; format/lint clean throughout; build + test re-verified at 4.9.
- Next: `/session-end` to record the operator-side handoff, append the final session entry to UPGRADE/LOG.md ("Phase 4 closed; upgrade arc complete"), and regenerate next.md as the post-arc parking pointer. Then operator parks, opens a new arc, or promotes a carry-forward item.

## 2026-05-06 — Phase 4 steps 4.1 → 4.6 closed (six sub-steps in one session)

- Step range 4.1 → 4.6 across `7a970b5..91eebca` (7 commits + this session-end docs commit). Six functional/docs commits + 1 follow-up reflow + 6 step-close edits + this docs commit. All four `pnpm` gates green at every commit. **CLI test count 78 → 133** (+55 across the session); workspace total **1080 → 1135 + 3 skipped**.
- **Session-start drift:** `commit-mismatch` (STATE.md said `5fbcfb1` but HEAD was `ce55404`, the prior session-end docs commit). 9th occurrence of the documented post-session-end self-reference lag-by-1. Steady-state lag-by-1 maintained — this session-end will create the 10th occurrence.
- **Step 4.1 — verify `factory cancel` (`7a970b5`):** Smoke against live factoryd (pid 41888, uptime 16h) confirmed: bogus ULID → `not found` exit 2; complete directive → `is already complete` exit 3; `--reason` accepted. **Doc reconciliation:** the implementation has 4 exit codes (0/1/2/3 = OK/GENERIC/NOT_FOUND/ALREADY_TERMINAL); steps.md and tier-4 plan said 3 codes (collapsed). Tightened both docs to the live 4-code surface, parallels `factory ui-token`'s shape. Filed two carry-forwards in the commit body: README.md gap (no `cancel` row) → handled in 4.7; `--help` sonic-boom-on-exit error → handled in 4.6.
- **Step 4.2 — `factory budget set <project>` (`fa28e6d`):** New `packages/cli/src/commands/budget.ts` reusing `updateProjectMetadata` (same code path as the web's PUT). **Per-field merge** (CLI semantics, differs from web's full-doc replacement) — `--max-steps 100` keeps an existing `maxUsd`; idempotent. Project resolution: name → full ULID; ambiguous → exit 2 with disambiguation list. 15 tests covering merge in both directions, idempotence, both Wiki error classes, full-ULID resolution. Validation via `projectBudgetDefaultsSchema`.
- **Step 4.3 — `factory project list/show/delete` (`9da25ba`):** Three subcommands. `list` walks projectsQ.listAll + on-disk language + most-recent build. `show` resolves by name/ULID, pretty-prints registry + project.json + last build (with `(unset)` vs `(unavailable)` distinction). `delete` defaults to interactive y/N + unregister-only; `--force` skips prompt; `--purge` adds typed-name double-confirm + rm -rf. Order on `--purge`: unregister first then rm -rf. New typed `projectsQ.remove(db, id)` in state package — no FK cascade by design (migration 006 §implementation note: directives.project_id has no FK, historical spend/findings rows survive as ledger). 22 tests; tmpdir-rooted workspaces; canned-response prompt stubs cover approve/decline/wrong-name paths.
- **Step 4.4 — `factory ask "<question>"` (`e07c7a0` + reflow `caba5d5`):** Refactored `chat.ts` to extract `submitOneDirective` helper (mint + notify + reply-poll cycle). Chat REPL now loops over the helper; ask calls it once. New `packages/cli/src/commands/ask.ts` mints one chat directive, awaits the brain's reply, prints, exits. `--json` emits `{ directive, reply, status[, directiveStatus] }` for scripting. 7 tests cover plain + --json crossed with reply / timeout / terminal-no-reply. **Notify-injection trick:** the test's notify hook either enqueues a matching outbound row (reply path) or flips the directive's status (terminal path) — avoids any racy concurrent-task setup.
- **Step 4.5 — tab completion (`9340cfd`):** New `factory completion <shell>` command emitting self-contained completion scripts for bash / zsh / pwsh. Static surface — 19 top-level commands + 7 nested groups. Single source of truth (`TOP_LEVEL_COMMANDS` + `NESTED_SUBCOMMANDS`) drives all three templates. 9 tests; live smoke confirmed bash output + unknown-shell exit 2.
- **Step 4.6 — rich `--help` (`91eebca`):** Every leaf command's `--help` now ends with `Examples:` (and Exit codes: where applicable). Top-level `factory --help` `addHelpText('afterAll', ...)` points at `docs/WORKFLOWS.md` + lists `factory <cmd> --help` as the per-command surface + tab-completion install one-liners. New help-coverage gate at `packages/cli/src/help-coverage.test.ts` (2 tests) walks the Commander tree via `cmd.outputHelp()` with a captured writer (since `helpInformation()` alone misses event-driven addHelpText content). **Sonic-boom-on-help flush race fixed** (filed in 4.1's commit body): `apps/factory/src/main.ts` argv-sniffs `-h`/`--help`/`-V`/`--version` and inits the logger with `noFile: true, noConsole: true` on those paths so the on-exit hook's flush is a no-op. Live smoke: `factory --help` / `factory build --help` / `factory cancel --help` all exit 0 cleanly (were exit 1 with a sonic-boom error trailer).
- **Patterns recorded for future sessions** (in STATE.md "Attempts that didn't work"): `helpInformation()` doesn't include `addHelpText` content (use `outputHelp()` with a captured writer); pino's sonic-boom isn't ready synchronously (synchronous-exit paths need `noFile:true,noConsole:true` to avoid the flushSync throw).
- **No new ADRs.** Tier 4 plan flagged three likely candidates (tab-completion scope; project delete semantics; ask JSON output shape) — each was a sane-default decision matching the plan; none rose to ADR-worthy. Recorded inline in commit bodies.
- **Phase 4 progress:** 6 of 9 sub-steps closed. Remaining: 4.7 (cli/README refresh), 4.8 (issues U018-U021 + ROADMAP), 4.9 (`/phase-close`). All three fit comfortably in a single short follow-up session (~1 hour). 4.7 is a single-file doc edit; 4.8 is mechanical issue moves; 4.9 is `/phase-close`.

## 2026-05-06 — Session end after Phase 3 close

- Single-purpose session: ran `/phase-close` on the Phase 3 (web-ui) work that the prior session left ready (steps 3.1 → 3.10 all closed; 3.11 was `/phase-close` itself).
- Verification surfaced one gap: issues U006-U010 + U022 were still in ISSUES.md "Open" — folded the resolution-marking into the close commit alongside the structural close work.
- Phase 3 closed cleanly: tag `phase-3-web-ui-closed` (annotated) at `5fbcfb1`. Phase 4 (cli-completion) scaffolded.
- One Prettier hiccup during verification: ADR 0029's edits needed a `prettier --write` reflow for table column alignment; reformatted in-place, format:check then clean.
- All four `pnpm` gates green at /phase-close verification. Workspace test count unchanged at 1080 + 3 skipped.
- Working tree clean post-close. This session-end commit will create the 9th occurrence of the documented post-session-end self-reference lag-by-1 (STATE.md will reference `5fbcfb1` while HEAD moves to the session-end commit).

## 2026-05-06 — Phase 3 closed (tag: `phase-3-web-ui-closed`); Phase 4 (cli-completion) kicked off

- `/phase-close` ran on Phase 3 work. All ten sub-steps shipped (3.1 → 3.10); 3.11 was `/phase-close` itself.
- Six issues moved Open → Resolved in `UPGRADE/ISSUES.md` (U006, U007, U008, U009, U010, U022) — all Tier 3 web-UI issues now in the Resolved section.
- ADR 0029 promoted past gated state — Live verification table now ✅ for all six event types; unit-test-only carve-out for `finding.created` retired (closed in 3.7's `node-sse-smoke` smoke); Negative-consequence bullet for the gap removed; future-work list trimmed.
- Phase 3 README's `## ADRs decided in this phase` populated (0027 / 0028 / 0029); `## Deferred to Phase 4 (or later)` populated with three carry-forward items (Pause primitive; PageShell + `<style is:global>` migration; brain-side `log.line` forwarder). Carry-forward block auto-seeded into Phase 4 README's `## Why this phase exists`.
- Phase 4 scaffolded at `.control/phases/phase-4-cli-completion/{README.md,steps.md}` per `phase-plan.md` Phase 4 entry + `tier-4-cli-completion.md`. Sub-step list: 4.1 verify `factory cancel`; 4.2 `factory budget set`; 4.3 `factory project list/show/delete`; 4.4 `factory ask`; 4.5 tab completion (bash/zsh/pwsh, static); 4.6 rich `--help` examples; 4.7 README refresh; 4.8 resolve U018-U021 + tick Tier 4 ROADMAP; 4.9 `/phase-close`.
- All four `pnpm` gates green at /phase-close verification. Workspace test count unchanged from end-of-Phase-3 baseline: 1080 passing + 3 skipped.
- Tier 3 ROADMAP boxes were already fully ticked through step-3.10 close — `/phase-close` had nothing to do there; the Tier 3 close itself is the wrap.
- Annotated tag `phase-3-web-ui-closed` at the close commit.
- One verification-stage discovery: issues U006-U010 + U022 were still in ISSUES.md "Open" at phase-close time (sub-step closes had not folded in the resolution-marking). Fixed in the close commit. Lesson worth recording: marking issues resolved is best done in the per-sub-step close commits as each issue's work completes; folding into `/phase-close` is acceptable but increases the close commit's diff size.

---

## 2026-05-05 — Steps 3.8 + 3.9 + 3.10 closed in one session (spend charts + mobile-responsive nav + logout + connection pip)

- Step range 3.8 → 3.10 across `bf19b2b..80b9bec` (7 commits + this session-end docs commit). Six functional commits + 1 follow-up fix + 3 close commits + this docs commit. All four `pnpm` gates green at every commit. Workspace test count 1075 → **1080 + 3 skipped** (state +5 for `perDayPerProject` coverage in 3.8; 3.9 + 3.10 are layout-only, no test deltas).
- **Session-start drift:** `commit-mismatch` (STATE.md said `06e7460` but HEAD was `8d13c26`, the prior session-end docs commit). 7th occurrence of the documented post-session-end self-reference lag-by-1; STATE.md itself documented it. Operator was given the narrative + verbose block per the runbook; no reconciliation action needed (the lag is structural, not real divergence). Steady-state lag-by-1 maintained — this session's session-end commit will create the 8th occurrence.
- **Step 3.8 — spend page charts (2 commits, +5 tests).**
  - `bf19b2b feat(3.8): perDayPerProject rollup + /api/v1/spend extension` — new `spend.perDayPerProject(db, filter?)` query helper in `packages/state/src/queries/spend.ts` returning `{date, projectId, projectName, display, totalUsd, callCount}[]` ordered `(date DESC, totalUsd DESC)`; same `SpendFilter` + LEFT-JOIN-through-projects pattern as the other four helpers so orphan rows roll up into per-date `(unassigned)` buckets. +5 tests in `spend.test.ts`. New `perDayPerProjectSpendSchema` in `packages/ipc/src/schemas.ts` extending `apiV1SpendResponseSchema` (additive — older clients ignore the field). `packages/daemon/src/server.ts` `/api/v1/spend` route extended to call the new helper + include in response + add `dayProjectCells` to debug log; existing `server.test.ts` cases extended to assert the new field on empty + populated bodies. State 152 → 157.
  - `61dd29a feat(3.8): spend page sparkline + 30-day stacked bar` — `apps/factory-web/src/pages/spend/index.astro` gains two charts above the existing four tables. Per-project sparkline (240×28 SVG, last 14 days, one row per project, 3-column grid label·sparkline·USD-total, discrete segments + dots so zero-spend days render as visible gaps not lines connecting through zero); 30-day stacked bar (720×180 SVG, segments per project, native `<title>` tooltips on bars + segments, per-day invisible hover target so empty days surface tooltips, y-max + first/last date axis labels, legend with color swatches below). Vanilla SVG only (no chart-library dep). Color encoding: deterministic hue from `projectId` hash → `hsl(<hue>, 50%, 50%)` mid-luminance for theme-independent stable colors; `(unassigned)` gets neutral `color-mix(currentColor 35%, transparent)`. Page-side `<style is:global>` block with `.sparklines` / `.stacked-bar` / `.legend` namespacing reaches dynamically-injected SVG content. Frontend-design skill invocation per saved feedback before authoring.
  - `df1a351 refactor(3.8): close step 3.8 — spend page charts live` — flipped `[x] 3.8` + ROADMAP tick. Live smoke against fresh factoryd PID 21776 (post-mid-session-restart): real `perDayPerProject` data populated with two projects on 2026-05-05 (`node-sse-smoke` $1.93, `smoke-demo` $1.67) — both charts render through the deterministic-hue palette; visual verification by operator passed (light + dark, no console errors).
- **Step 3.9 — mobile-responsive nav (1 feat + 1 close, no test deltas).**
  - `5a15b1a feat(3.9): mobile-responsive nav with hamburger drawer` — Dashboard.astro's header gains a `<details>`-based hamburger drawer at ≤768px (zero JS, native semantics, keyboard- and screen-reader-friendly, auto-closes on link nav). Hamburger glyph is three currentColor bars; animates into a × via 120ms `transform` when drawer is open; 44×44px tap target per Apple HIG. `@media (max-width: 768px)` hides inline nav + reveals the drawer + tightens header/main padding (1.5rem → 1rem). `@media (max-width: 640px)` stacks paired-column `.form-row` to single column so two-up form fields don't compress at phone widths. `Table.astro` wraps the `<table>` in a `.table-wrap` div with `overflow-x: auto` so wide data-dense tables (e.g., 6-column perDirective) can h-scroll inside their container at narrow viewports without forcing the whole page to h-scroll.
  - `8364e75 refactor(3.9): close step 3.9 — mobile-responsive nav live` — flipped `[x] 3.9` + ROADMAP tick. Operator visual verification across desktop / ≤768px / 375px (iPhone SE) / <640px breakpoints + keyboard nav + light/dark.
  - **Plan-vs-steps.md numbering offset surfaced.** The tier-3 plan numbers 3.8 = mobile-responsive nav and 3.9 = logout/connection pip; steps.md numbers 3.9 = mobile-responsive nav and 3.10 = logout/connection pip (steps.md added 3.4 "convert all pages" the plan didn't enumerate, then swapped 3.3/3.4 + 3.5/3.6). Documented in the close commit body. STATE.md tracks steps.md as cursor.
- **Step 3.10 — logout + connection-status pip (1 feat + 1 follow-up fix + 1 close, no test deltas).**
  - `d544192 feat(3.10): explicit logout + connection-status pip in header` — Dashboard.astro gains a layout-level heartbeat that drives a colored status pip + dual logout buttons (one in desktop header, one inside the hamburger drawer at mobile, both with `data-logout` for a single click handler). Heartbeat: 30s poll on `/api/v1/status` with bearer; state machine 0 failures → green Connected / 1-2 → amber Reconnecting / 3+ → red Disconnected; no token in store → straight to red labeled "Signed out". Logout flow: `clearToken()` → redirect to `/app/?logged-out=1`. Logged-out banner rendered hidden in the layout; JS unhides on `?logged-out=1` URL param then strips it via `history.replaceState`. Intentional theme-independent green/amber/red colors (`#2a8` / `#d80` / `#c24`) so traffic-light semantics read in both light + dark via muscle memory. `aria-live="polite"` on the pip wrapper announces state transitions to assistive tech. At ≤768px only the colored dot remains in the header; pip-label + desktop logout hide and the drawer's logout button takes over (separated from nav links by a 1px top border for visual rhythm).
  - `3cecb72 fix(3.10): surface stale-token case + name the recovery command` — operator smoke surfaced the gap: post-daemon-restart, the page's stored bearer goes stale (token rotates per startup per ADR 0025), and the heartbeat's generic 3-failure cycle was misleading because polling can't recover from 401. Short-circuit on `err instanceof ApiError && err.status === 401` directly to red `disconnected` with a terse visible label "Session expired" and a verbose hover tooltip naming the recovery command — `Daemon restarted — run \`factory ui-token\` for a fresh URL (or paste a new URL from the daemon stdout).` Generalized via `setStatus(state, label, hover?)` so the visible chrome stays terse while the actionable detail lives in the tooltip — pattern reusable for any future labeled-with-action UI. ONBOARDING.md §6 troubleshooting entry for "Dashboard token rejected (401)" extended to mention the in-UI signal alongside the DevTools 401 cue. Operator confirmed the rotation flow + the troubleshooting docs (`docs/ONBOARDING.md` §5.2 "Recover the URL", §6 troubleshooting entry, `packages/cli/README.md` `factory ui-token` section, `apps/factory-web/README.md` token lifecycle blurb) — well-covered, no further docs amendment needed beyond §6.
  - `80b9bec refactor(3.10): close step 3.10 — logout + connection pip live` — flipped `[x] 3.10` + ROADMAP tick. Live smoke against factoryd PID 21776 + post-restart fresh PID/token: pip cycled green → amber Reconnecting → red Disconnected on daemon stop, recovered to green on restart-with-fresh-URL, signed-out flow lands on the banner.
- **Frontend-design judgement calls worth carrying forward to /phase-close + Phase 4:** smart defaults read better than empty states (3.8's chart windows always render anchored on today UTC, gaps render visibly); native HTML beats custom widgets when semantics align (`<details>` for the 3.9 hamburger); theme-independent intentional colors for status semantics (3.10's traffic-light pip); error-class differentiation when recovery paths differ (3.10's 401 short-circuit vs. generic retry); visible-label vs. hover-title separation (3.10's `setStatus(state, label, hover?)`); root-cause CSS fixes over global rewrites + inherit-don't-invent + hint-copy-teaches-consequence + in-context-affordance-vs-nav (carried from prior sessions, all still apply).
- **Mid-session daemon restart cycle.** Factoryd PID 37148 was loaded with the pre-3.8 build; restarted to PID 21776 mid-session for the 3.8 daemon-side build. Astro dev on 4321 hot-reloaded throughout. Operator stop/start round-trips during 3.10 smoke also rotated PID/token several times. Live URL via `factory ui-token`.
- **Three deferred follow-ups remain in `phase-3-web-ui/steps.md`** (none block /phase-close): pause primitive on directive detail (defer until workflow signal demands it); PageShell adoption + Dashboard `<style is:global>` migration (absorbs the carry-forward "Clear all defaults" + 4× filter-form Apply buttons unstyled-button issue + inline-style audit pass); pre-3.5 baseline live-smoke chat-page click-test (30-second remaining piece — pin as part of the /phase-close smoke).
- ADRs decided: none. ADR 0029 promotion past gated state pinned to /phase-close per the ADR's Live verification section — gap was closed in 3.7's smoke (six event types confirmed live), the structural promotion (retire the unit-test-only carve-out via amendment) happens at phase close.
- Issues opened / closed: none.
- Minor fixes: the `3cecb72` 401 short-circuit is minor by Control's severity gating; logged here per the journal-line convention rather than a separate issue file.
- Blockers hit: none. The 3.10 smoke surfaced one real UX gap (stale-token after daemon restart) and was fixed in-flight without rolling back the smoke; the close commit landed cleanly afterward.
- All four `pnpm` gates green at every commit. Per-package counts: state 152 → **157** (+5), other packages unchanged.
- **Smoke residue accumulated** (no new projects this session, but the 3.10 daemon-restart cycle didn't clean up residue from 3.7/3.8 sessions — 2 projects + 3 directives + workspace dirs persist). Optional cleanup deferred. Daemon + astro dev still running at session end.
- **`/session-end` skill structural fix** for the "Last commit" lag-by-1 drift remains unaddressed across **8 occurrences** now (counting this session-end). Two structural options unchanged from prior journal entries.
- Next: **step 3.11 — `/phase-close`**. Tags `phase-3-web-ui-closed`; appends to UPGRADE/LOG.md; promotes ADR 0029 past gated state via amendment retiring unit-test-only carve-out; scaffolds Phase 4 (cli-completion) per `phase-plan.md`. One smoke remaining: chat-page click-test for Phase 3.5's pre-existing work.

## 2026-05-05 (later) — Step 3.7 closed (live-smoke acceptance + ADR 0029 finding.created verified live + 2 in-flight CSS/router fixes)

- Step range 3.7 across `561da90..06e7460` (2 fix commits + 1 close commit). Three commits this session, all four `pnpm` gates green at every commit. Workspace test count unchanged at 1075 + 3 skipped — both fix commits were CSS / layout-only, no test deltas.
- **Session-start drift:** `commit-mismatch` (STATE.md said `53e4e98` but HEAD was `561da90`). Anticipated post-session-end lag-by-1 — STATE.md itself documented it ("same pattern as `1c6eeaf` / `288603e` predecessors"). Operator chose to fold the reconcile into the upcoming step-close commit rather than emit a standalone `317d94b`-shape. Worked: the 3.7 close commit (`06e7460`) updated STATE.md's "Last commit" line to `e3aca18` (the prior commit immediately before the close), restoring lag-by-1 cleanly.
- **Daemon restart prerequisite handled at session-start.** Stale factoryd PID 41196 on `127.0.0.1:25295` was loaded with the pre-3.7 build (verified: GET /api/v1/projects 401 = bearer-gated route exists, POST /api/v1/projects 404 = new route missing). Stop+start round-trip via the actual binary (`node apps/factory/dist/main.js daemon stop && ... start` — see Tooling discovery below). Fresh PID 37148 came up at `T+5s` with cli/discord/telegram channels ready; POST /api/v1/projects then 401 (route present, no token) — confirming commit (b)'s route loaded.
- **Tooling discovery worth recording:** `node packages/cli/dist/index.js daemon <…>` produced no output and exited 0. `packages/cli/dist/index.js` is the **library exports** for `@factory5/cli` (`buildCli` + types — confirmed by reading `packages/cli/src/index.ts` which only re-exports `buildCli`), not a runnable CLI binary. The actual `factory` binary is `apps/factory/dist/main.js` per the `factory` package's `bin: { factory: "./dist/main.js" }` field. Use that path until a global pnpm link lands.
- **Live-smoke acceptance for step 3.7 (operator-driven):** opened `/app/projects/new`, submitted `node-sse-smoke` (language=node, optional CLAUDE.md asking the architect for a verifier-flagged feature). POST /api/v1/projects returned `{id: 01KQWT6T6STXT4BFB5MC9QF9E6, path: …}` shape; redirect to `/app/projects/detail?id=…` fired. Scaffolded files at `C:\Users\Momo\factory5-workspace\node-sse-smoke\` confirmed (`CLAUDE.md`, `.factory/project.json` with id/name/language/budgetDefaults intact, `package.json`, `src/`, `tests/`, `docs/`, etc. — architect did meaningful authoring work). Project visible at `/app/projects/`. Build kicked at `/app/build`; `directives/detail` SSE produced events including `finding.created` — assessor surfaced **F001 LOW OPEN** (source `builder`, target `docs/knowledge/testing.md`): the wiki documents `npm test` → `node --test tests/` which fails on Node 22.22.2 (Windows) with `ERR_MODULE_NOT_FOUND` resolving the bare directory; builder patched `package.json` to `node --test tests/*.test.js` and surfaced the docs divergence as a finding. Visible at `/app/findings/`.
- **ADR 0029 promotion gate cleared.** The `finding.created` live-verification gap pinned in the ADR's Live verification § is **closed** by F001's live emission. All six event types now confirmed end-to-end across the 2026-05-05 sessions (4 functional + cancel round-trip from prior session, plus this session's `finding.created`). The ADR's unit-test-only carve-out can be retired in a docs amendment when `/phase-close` (3.11) lands.
- **Two in-flight bugs surfaced + fixed during smoke (separate commits, not part of 3.7's primary scope):**
  - `3ff0ea0 fix(3.7): button text invisible on filled-accent buttons (CanvasText, not currentColor)` — three sites had the same self-referential bug: `background: color-mix(in srgb, currentColor 85%, transparent); color: Canvas;` — `color: Canvas` rebinds `currentColor` for that element to `Canvas`, so the same rule's background also collapses onto Canvas → both fg and bg evaluate to (almost) the system canvas colour, button text invisible against same-colour fill. Fixed in `Submit.astro` (`.btn-primary` — affects Submit on all forms + "Save defaults" on `/projects/detail`), `EmptyState.astro` (`.empty-state-cta` — affects every empty-state CTA), `Dashboard.astro` (`.btn-primary` — currently scoped/dead, included for the deferred `<style is:global>` migration). Reference is `CanvasText` (the system pair to Canvas, guaranteed contrast under `color-scheme: light dark`). Two-character change × 3 sites. The "Clear all defaults" + 4× filter-form Apply buttons render as user-agent default `<button>` because Dashboard.astro's scoped styles don't reach slotted page content — separate concern, folds into the deferred PageShell migration.
  - `e3aca18 fix(3.7): drop <ClientRouter /> so page setup runs on every nav` — Astro 5 view-transitions router was preventing page-setup scripts from re-running on nav. Astro module scripts dedupe across swaps and run once at first page load; none of the 11 page scripts wrap their DOM-setup (querySelector + event wiring + initial fetch) in an `astro:page-load` listener, so navigating between pages via the header nav left the new page's setup never executed — the static markup rendered, but no data was fetched and no handlers attached. F5 forced a full reload that bypassed the router and worked. Smaller blast radius to drop the router than to refactor 11 page scripts; zero current pages use ClientRouter-specific events (the `apps/factory-web/src/components/README.md:272` mention of `astro:before-swap` for SSE cleanup is forward-looking, no consumers exist). One-import + one-element removal in `Dashboard.astro`.
- **Step 3.7 close (`06e7460 refactor(3.7): close step 3.7 — /app/projects/new live, ADR 0029 finding.created verified`)** — flipped `[ ] 3.7` → `[x] 3.7` in `phase-3-web-ui/steps.md` and `UPGRADE/ROADMAP.md`; folded the STATE.md "Last commit" pointer reconcile in (vs. the standalone `317d94b`-shape last cycle); regenerated next.md absorbed.
- **Frontend-design skill invocation** (per saved feedback) before the CSS fixes — skill content surfaced as greenfield-design-oriented; honored the "inherit, don't invent" rule from STATE.md and stuck to narrow root-cause fixes (`currentColor` → `CanvasText`, drop `<ClientRouter />`) without redesigning anything. Two-character × 3 sites + 2-line removal.
- ADRs decided: none. ADR 0029's Live verification gap closed (no ADR amendment yet — defer to /phase-close).
- Issues opened / closed: none.
- Minor fixes: the two CSS / router fixes above are minor by Control's severity gating; logged here per the journal-line convention rather than as separate issues.
- Blockers hit: none. The smoke surfaced two real bugs and both were fixed in-flight without rolling back the smoke; the close commit landed cleanly afterward. The session-start `commit-mismatch` drift was anticipated and reconciled during the close commit per operator preference (vs. standalone reconcile).
- All four `pnpm` gates green at every commit. Per-package counts unchanged from prior session: state 152, channels 175, daemon 173, brain 101, worker 38, worker-sandbox 86 + 3 skipped, assessor 79, wiki 74, cli 78, providers 39, ipc 28, events 3, core 14, logger 20, worker-mcp 15. Workspace total **1075 + 3 skipped**.
- **Smoke residue accumulated:** this session's `node-sse-smoke` project (id `01KQWT6T6STXT4BFB5MC9QF9E6`) at `C:\Users\Momo\factory5-workspace\node-sse-smoke\` plus build artifacts (BUILD.md, plan.json/plan.md, findings.json, worktrees/) added to the prior session's residue (smoke-demo project + 2 cancelled directives). Optional cleanup deferred. Daemon (PID 37148) and astro dev (4321) still running at session end.
- **`/session-end` skill structural fix** for the "Last commit" lag-by-1 drift remains unaddressed across **7 occurrences** now (counting this session's `06e7460` folded reconcile). Two structural options: track "last work commit" rather than HEAD (semantic shift), or amend STATE.md post-commit (hook complexity). Worth filing as ergonomic infrastructure work in the next quiet session.
- Next: step 3.8 — spend page charts (per-project sparkline + 30-day stacked bar; vanilla SVG, no chart-lib dep) per `tier-3-web-ui-live-and-complete.md` §3.8. First-step investigation: read `pages/spend/index.astro` + spend query/route to determine if `/api/v1/spend` already has the daily/project decomposition or if 3.8 needs new daemon work. Frontend-design skill invocation required before authoring.

## 2026-05-05 — Step 3.7 code-complete (createProject extraction → POST route → /app/projects/new page; close commit + live-smoke acceptance pending)

- Step range 3.7 across `317d94b..53e4e98` (1 reconcile + 3 step commits). Four commits this session, all four `pnpm` gates green at every commit. Workspace test count 1063 → 1075 (+12 net).
- **Session-start drift reconcile (`317d94b`)** — sixth occurrence of the post-session-end self-reference drift (`cce7065` / `db61baf` / `54c0f20` / `d7a366c` / `288603e` were the prior five). Operator chose path 1 from the session-start narrative: tiny `docs(state): reconcile STATE.md last-commit pointer to current HEAD` mirroring `288603e`-shape — STATE.md "Last commit" caught up from `79474b1` (ADR 0029) to `1c6eeaf` (the prior session-end docs commit); "Recently completed[0]" prepended with the session-end entry; "Last updated" header documents the reconciliation. Steady-state lag-by-1 accepted in the commit body. The `/session-end` skill fix that would close this drift class structurally (track "last work commit" rather than HEAD, or amend STATE.md post-commit) remains unaddressed across 6 occurrences now — worth filing as ergonomic infrastructure work in the next quiet session.
- **Step 3.7 spans three commits implementing browser-side project creation end-to-end:**
  - `d118e1c` `refactor(3.7): extract createProject into @factory5/wiki` — new `wiki.createProject({projectPath, name, language, claudeMd?}) → {id, path, claudeMdPath}` containing the body of `runProjectInit` (refuse-overwrite-existing-identity, refuse-existing-CLAUDE.md, mkdirSync, writeFileSync(scaffold), loadOrCreateProjectMetadata). New `CreateProjectAlreadyExistsError` with `reason ∈ {existing-metadata | existing-claude-md}` + `existingProjectId?` so CLI maps to exit 2 messages and daemon (commit b) maps to HTTP 409. `scaffoldClaudeMd` relocated from CLI to wiki — single source of truth, wiki owns project layout per ADR 0003. CLI's `runProjectInit` collapses to ~30-LOC thin wrapper (path resolution + try/catch + stdout). `packages/cli/src/commands/init.test.ts` deleted; its 4 scaffold tests reproduced verbatim in `wiki/src/create-project.test.ts` alongside 6 new createProject tests (fresh-create, claudeMd override, refuse-existing-identity, refuse-existing-claude-md, identity-stable, each-language scaffold). Wiki 64 → 74, CLI 82 → 78.
  - `50e8b33` `feat(3.7): POST /api/v1/projects route + schemas` — new `apiV1CreateProject{Request,Response}Schema` in `@factory5/ipc` (request: `name (≥1) / language (enum) / claudeMd?`; response: `{ id (ULID), path }`). New route in `packages/daemon/src/server.ts` adjacent to the existing GET counterparts; bearer-gated via `requireUiAuth`; pipeline parses body → joins `(opts.workspace ?? defaultWorkspace())` + name → `wiki.createProject` (maps `CreateProjectAlreadyExistsError` → 409 ALREADY_EXISTS) → `projectsQ.upsert` registry row → respond `{id, path}`. New `IpcServerOptions.workspace?` opt for test override (also future-proofs the prod path for `cfg.general.workspace` once that wiring lands; POST /api/v1/builds has the same gap, deferred). +6 route tests in `server.test.ts` mirroring the build route's 401/503/400-missing-name/400-bad-language/happy-path/409-already-exists shape. Daemon 167 → 173.
  - `53e4e98` `feat(3.7): /app/projects/new page` — `apps/factory-web/src/pages/projects/new.astro` modeled on `build.astro`'s `<Form>+<Field>+<Submit>` shape. Fields: name (required text, hint teaches workspace-join + refuse-overwrite), language (required select, python default per CLI back-compat, hint names the assessor-runtime consequence), claudeMd (optional textarea, 12 rows, placeholder previews scaffold's section structure). On 200 redirects to `/app/projects/detail?id=<id>`; on error, hidden-Alert-placeholder pattern with `ALREADY_EXISTS` / `SCHEMA_VALIDATION_FAILED` / `UI_AUTH_REQUIRED` / `UI_DISABLED` headings. `captureTokenFromUrl()` first executable statement (per saved-feedback memory + lesson from `00d2bc4`). Empty `claudeMd` field sends nothing (operator falls through to per-language scaffold), not `''` (which would override the scaffold with empty). Frontend-design skill invoked before authoring per saved feedback. `+ New project` outline-button affordance added to `projects/index.astro`; empty-state copy updated to surface the new web flow alongside the CLI fallback. Top nav left at 8 items intentionally — adding "New project" as a 9th would crowd; the contextual affordance + empty-state deep link cover discoverability without nav clutter (intentional deviation from plan's nav-link recommendation, in the lighter direction; recorded in the commit body).
- **Frontend-design judgement calls during commit (c)** worth recalling for 3.8/3.9/3.10: inherit, don't invent (apply skill principles within the existing utilitarian aesthetic, not introduce a new palette / typography); hint copy teaches the consequence not the shape ("drives the per-language scaffold and the assessor runtime on subsequent builds" beats "pick a language"); empty submit fields ≠ explicit empty (don't conflate "not specified" with "explicitly cleared"); skipping the global nav addition when an in-context affordance covers discoverability is the cleaner UX (skip > clutter when both work).
- **Step 3.7 is code-complete but NOT closed.** The `- [ ] 3.7` checkbox in `phase-3-web-ui/steps.md` and the matching tick in `UPGRADE/ROADMAP.md` are intentionally not flipped yet — they land in a separate `refactor(3.7): close step 3.7` commit alongside the live-smoke acceptance, matching the pattern set by `dfd1a07` (3.4 close) and `0f5775a` (3.6 close). The acceptance smoke is the operator's call to schedule; ideally combined with the still-open phase-3 acceptance smoke that closes ADR 0029's `finding.created` live-verification gap on a substantive build.
- **Background processes carried forward from prior session.** `factoryd` on `127.0.0.1:25295` is **stale** (loaded the pre-3.7 build, will 404 the new POST /api/v1/projects route — verified via curl) — needs `factory daemon stop && factory daemon start` before live-smoke. `astro dev` on `127.0.0.1:4321` is hot-reloading correctly and already serving the new `/app/projects/new` page (verified via curl).
- ADRs decided: none. ADR 0029 still has the `finding.created` live-verification gap pinned to phase-3 acceptance.
- Issues opened / closed: none.
- Minor fixes: none mid-step (all four commits landed cleanly on first attempt; closest to a course-correction was tightening `let result;` → `let result: CreateProjectResult;` in CLI's thin wrapper for TS strict-mode flow analysis, decided pre-build).
- Blockers hit: none. Smoke residue from prior session still in DB + filesystem (two cancelled directives + smoke-demo project at `C:\Users\Momo\factory5-workspace\smoke-demo`); optional cleanup, doesn't block 3.7 close or 3.8.
- All four `pnpm` gates green at every commit. Per-package test deltas: wiki 64 → 74 (+10), cli 82 → 78 (-4 scaffold-tests-moved), daemon 167 → 173 (+6 route tests). Workspace total 1063 → 1075 + 3 skipped.
- Loose ends still open from prior sessions (not blocking 3.7 close or 3.8): Submit button invisible (`Submit.astro` `.btn-primary` `color: Canvas`) — will repro on the new `/app/projects/new` form too; same `<style is:global>` follow-up will fix all dashboard buttons in one sweep. Control framework repo at `G:\Projects\Small-Projects\Control` still has uncommitted upstream patches matching local `e5ec723` — operator's go on 2.2.3 publish.
- Next: live-smoke 3.7 against a restarted factoryd (combined with the still-open phase-3 acceptance smoke for `finding.created`) → close commit `refactor(3.7): close step 3.7`. Then step 3.8 — spend page charts (sparkline per project + 30-day stacked bar, vanilla SVG) per `tier-3-web-ui-live-and-complete.md` §3.8.

## 2026-05-05 — 3.6 follow-ups + ADR 0029 promotion + Claude Code hooks cwd-anchor (3.7 not started)

- Three commits this session: `00d2bc4` (3.6 follow-up auth fix) → `e5ec723` (hooks cwd-anchor) → `79474b1` (ADR 0029 promotion). All four `pnpm` gates green at every commit and at session end. No new tests added (auth fix in factory-web has no test harness; hooks change is config; ADR is markdown). Workspace test count unchanged.
- **2026-05-05 phase-3 live smoke landed (Decision 1 = smoke-first path from last session's session-start prompt).** Single factoryd-up window covered: 3.6 cancel-button acceptance ✓, ADR 0029 promotion ✓, plus surfaced two real bugs and one styling backlog confirmation. Total smoke spend ~$1.67 across two directives (first cancelled at architect-askUser at $0.43, second cancelled mid-builder at $1.24 after directing the smoke-demo CLAUDE.md and answering two askUser questions via API to get past `assisted` autonomy gates). Browser-side verification covered Overview / Directives / Chat / Questions pages.
- **Live-smoke ADR 0029 scorecard:** 4 of 5 functional event types verified live in browser (`task.started` / `task.completed` / `spend.updated` / `log.line`) plus `directive.completed` via cancel-button round-trip. `finding.created` not exercised — directed smoke-demo project produced no findings (no verifier issues to register). ADR 0029 promotion accepted with the gap documented in its "Live verification" section: the brain emit code path has 4 unit tests in `pool.test.ts` from `f990323`, and the wire-side plumbing is identical in shape to the 4 verified event types, so risk of inflight breakage is low. Phase-3 acceptance smoke at `/phase-close` will close the gap on a substantive build.
- **Bug 1 — `directives/detail.astro` was missing `captureTokenFromUrl()` on init.** Surfaced when the operator opened the detail page directly with `?id=...&t=...` in a fresh tab and got "missing FACTORY5_UI_TOKEN — reopen the URL logged by factoryd". Every other auth-gated page calls `captureTokenFromUrl()`; detail.astro was the lone outlier. One-line fix in `00d2bc4 fix(3.6): bootstrap UI token in directives/detail.astro` — import `captureTokenFromUrl` alongside the other lib/api imports, call it as the first executable statement in the script body. Mirrors the canonical pattern. Minor fix, no regression test possible without an FE harness which factory-web doesn't have. Logged here per the journal-line convention for minor fixes.
- **Bug 2 — Claude Code hook commands failed intermittently with "No such file or directory".** Root cause: `.claude/settings.json` Stop / SessionStart / SessionEnd / PreCompact entries used `bash .claude/hooks/<name>.sh` (relative path); Claude Code launches hooks from whatever cwd the most recent Bash tool call left the harness in, so a `cd packages/state` mid-session broke every subsequent Stop hook fire. Confirmed via the claude-code-guide skill: `$CLAUDE_PROJECT_DIR` is the canonical project-root env var Claude Code provides per the hooks spec. Fix in `e5ec723 chore(install): cwd-anchor Claude Code hooks via $CLAUDE_PROJECT_DIR` wraps each hook command with `bash -c 'cd "$CLAUDE_PROJECT_DIR" && exec bash .claude/hooks/<name>.sh'`. Verified end-to-end by running the new Stop hook command from `packages/state/` (the original drift cwd that triggered the bug) — exit 0, snapshot written, internal relative paths resolved. Same fix applied in the upstream Control framework repo at `G:\Projects\Small-Projects\Control` (`tools/cli.js` settings template generator + Control's own `.claude/settings.json`); operator-facing call to commit + bump 2.2.2 → 2.2.3 + npm publish was deferred — those edits are uncommitted in the Control repo pending operator's go.
- **ADR 0029 promotion (`79474b1 docs(adr): ADR 0029 — directive-stream protocol`).** Distills six architectural decisions from `UPGRADE/specs/sse-directive-stream.md` into `docs/decisions/0029-directive-stream-protocol.md` (168-line ADR). Sections: Context (3 forcing functions: Phase-3 charter / chat-streaming / cancel-button latency); Decision (six numbered subsections matching the spec's structure); Live verification (the 2026-05-05 smoke scorecard with the `finding.created` gap explicitly documented); Consequences (positive 6-bullet, negative 6-bullet, reversible-yes-layered); Alternatives considered (8 bullets including WebSocket / long-poll / gRPC / single-event-type / no-backfill / per-directive-process / synchronous-finding-emit). INDEX.md row added. The carry-forward "where the 3.6 auth bug surfaced" disambiguation is in the ADR's Live verification so a future reader doesn't conflate the bug with a protocol issue.
- **Bug 3 — Submit button invisible in question-detail page.** Surfaced when the operator tried to answer the askUser question via FE; the `Submit answer` button had `class="btn btn-primary"` with `color: Canvas` rendering identical to the page background under the operator's color scheme. Operator could only locate the button via Ctrl+A. Fix is one-line CSS adjustment (swap `color: Canvas` for an explicit foreground that holds against any color scheme, or add an explicit background fill that contrasts) but not landed this session — flagged in next-session notes as a minor `fix(3.x)` follow-up that could fold into the PageShell + Dashboard `<style is:global>` migration if that lands first. Workaround for the smoke: operator answered the questions via direct API POST (`/api/v1/pending-questions/:id/answer`) bypassing the FE entirely.
- **Decision NOT to start step 3.7 this session.** Three commits already landed; the smoke + debugging consumed significant operator focus; 3.7 spans 3+ commits with frontend-design skill discipline required for the FE page. Cleaner cut to /session-end with 3.7 as a fresh session's primary work. Decision 2 from last session (Option A — extract createProject into @factory5/wiki) is locked in and ready to execute first thing next session.
- ADRs decided: ADR 0029 promoted (this session). Counts now: 0029 latest accepted, 0028 + 0027 unchanged.
- Issues opened / closed: none formally; bug 3 (Submit button) flagged in next-session notes as a fix(3.x) follow-up rather than a separate issue file (severity = minor per Control conventions, fix is one-line CSS).
- Minor fixes: prettier touched up table-column spacing in `0029-directive-stream-protocol.md` after authoring (single auto-format pass before commit); INDEX.md was already prettier-clean.
- Blockers hit: none. The smoke surfaced bugs but they were either fixed mid-session (auth bootstrap, hooks) or routed to next-session notes (Submit button, smoke teardown). The `assisted` autonomy parking at each phase transition (architect→planning, planning→execution) was a documented characteristic, not a bug — operator-bypassable via direct API POST when the FE was unstyled enough that the question-answer flow wasn't smooth.
- All four `pnpm` gates green at session end. Per-package test counts unchanged from last session: state 152, channels 175, daemon 167, brain 101, worker 38, worker-sandbox 86 (+ 3 skipped Windows/Linux branches), assessor 79, wiki 64, cli 82, providers 39, ipc 28, events 3, worker-mcp 15, core 14, logger 20.
- Smoke residue carried forward: two cancelled directives (`01KQW3D4CZ84ZFHFKYEP7BWQBX`, `01KQWDWRQD08X3BEYEQNAD6M23`), real `smoke-demo` project (`01KQW30T5274QGSEHHVZTRQ953`) at `C:\Users\Momo\factory5-workspace\smoke-demo` with scaffolder + builder output written. Daemon (PID `serve-41196` on `127.0.0.1:25295`) and astro dev (`127.0.0.1:4321`) still running at session end. All optional cleanups, none block 3.7.
- Next: step 3.7 — `/app/projects/new` per Decision 2 = Option A locked in last session. Three-commit plan in next-session notes: (a) `refactor(3.7): extract createProject into @factory5/wiki`, (b) `feat(3.7): POST /api/v1/projects route + schemas`, (c) `feat(3.7): /app/projects/new page` (frontend-design skill required for c). Acceptance smoke ideally combined with phase-3 acceptance to also close ADR 0029's `finding.created` live-verification gap.

## 2026-05-03 — Step 3.6 shipped (cancel button on directive detail; pause + 3.x follow-ups deferred)

- Step range 3.6 across `01686e1..0f5775a` (1 step commit + 1 close commit), preceded by `288603e` (session-opening drift reconcile) and `f990323` (3.1b deferred-work — finding.created emission). Four commits in the session.
- Session-start drift reconcile (`288603e`) — fifth occurrence of the post-session-end self-reference drift. Operator chose option (a): STATE.md "Last commit" caught up from `2dfd25f` (3.5 close) to HEAD's `96f5883` (the prior session-end docs commit); "Current step" narrowed from "cancel + pause" to "cancel button" reflecting Decision 2 = option C made at the same session-start prompt. Reconcile commit body documents the steady-state lag-by-1 the runbook accepts. The `/session-end` skill fix flagged in 3.4 + 3.5 journal entries (track "last work commit" rather than HEAD, or amend STATE.md post-commit) remains worth doing — still unaddressed across 5 occurrences now.
- **Two design decisions resolved at session-start before any code:**
  - **Decision 1 — cancel route prefix = option (a):** add `POST /api/v1/directives/:id/cancel` gated by `requireUiAuth`, keep the existing `POST /directives/:id/cancel` for CLI-loopback use. CLI cancel was confirmed to hit the legacy path via HTTP daemon-client (`packages/cli/src/commands/cancel.ts:82` → `client.cancelDirective` → `packages/ipc/src/client.ts:88-96` → `POST /directives/${id}/cancel`); CLI auth is loopback-only, so moving the route under `requireUiAuth` would break CLI without a parallel auth shift. Two route registrations sharing one closure-scoped `handleCancel` keeps the contract surface unchanged for CLI while lining up the SPA namespace.
  - **Decision 2 — pause primitive = option C (defer):** phase-3-web-ui/steps.md line 8 conflated cancel + pause into one step but no pause primitive exists today. Cancel solves the primary operator-pain case (a build going wrong needs to die, not pause-then-think); pause is the kind of feature worth designing once a real workflow demands it. Steps.md line 8 rewritten as "Cancel button on directive detail"; a new `## Deferred follow-ups` section at the end of phase-3-web-ui/steps.md captures three explicit `- [ ]` bullets — pause primitive, PageShell + Dashboard global migration, pre-3.5 baseline live-smoke — none of which gate any 3.x step.
- **Session ships 4 commits implementing cancel + closing 3.6 + extending the SSE wire:**
  - `288603e` `docs(state): reconcile commit cursor after session-end` — drift reconcile per option (a). Single-file STATE.md update; mirrors `d7a366c`'s shape from the prior session.
  - `f990323` `feat(3.1b): emit finding.created from brain pool` — wires the 5th of 5 ADR 0029 events. New `emitFindingCreated` synchronous helper in `packages/brain/src/pool.ts` that takes a fully-loaded `Finding` so I/O stays in the caller; integration call site in `executeTask` does one `listFindings(plan.projectPath)` per task to feed every emit (avoiding N+1 disk reads); emission happens after `task.completed` so subscribers see the completion signal first; wrapped in try/catch so a corrupt findings.json never fails the task. +4 unit tests in `pool.test.ts` mirror `loop.test.ts` shape: emitter-undefined-silent, well-formed-event-with-advisory-false-default, advisory-true-pass-through, one-event-per-call. Brain test count 97 → 101. ADR 0029 promotion now blocks on a single operator-driven live-smoke (kick off a real build, observe all 5 event types over the wire).
  - `01686e1` `feat(3.6): cancel button on directive detail` — server: extracts the existing inline cancel handler into a closure-scoped `handleCancel`, keeps `/directives/:id/cancel` as the CLI path, adds `/api/v1/directives/:id/cancel` gated by `requireUiAuth`. FE: adds `cancelInflight` + `cancelError` to `PageState`; `buildHeader` emits a pill button next to the live-pip when status is running/pending OR a cancel is in flight OR a cancelError is showing; click disables and morphs to "Cancelling" with a blinking trailing dot echoing the existing `live-pip-blink` keyframe; ApiError handling distinguishes ALREADY_TERMINAL / UI_AUTH_REQUIRED / UI_DISABLED. New CSS uses the warm-red `#c24` palette mirroring the existing log-level-error treatment, color-mix(currentColor) borders + background to stay in the same visual family as the live-pip. +6 daemon route tests cover 401 UI_AUTH_REQUIRED without bearer (DB row unchanged on auth fail) / 503 UI_DISABLED when uiAuthToken is undefined / happy path with bearer (status flips, abortFired=true, handle aborted) / 404 NOT_FOUND for unknown id post-auth / 409 ALREADY_TERMINAL post-auth / legacy CLI path still works without bearer. Daemon test count 161 → 167. Frontend-design skill invoked before authoring per saved feedback.
  - `0f5775a` `refactor(3.6): close step 3.6 — cancel-only; pause + 3.x follow-ups deferred` — flips `[ ] 3.6` → `[x] 3.6` in `phase-3-web-ui/steps.md` and `UPGRADE/ROADMAP.md`; rewrites the 3.6 line to "Cancel button on directive detail" wording; adds a `## Deferred follow-ups` section at the end of phase-3-web-ui/steps.md capturing pause primitive (Option A vs B vs longer-defer), PageShell + Dashboard global migration, and pre-3.5 baseline live-smoke as explicit `- [ ]` bullets with no acceptance dependency from any 3.x step.
- **Decision NOT to autonomously land Backlog A (PageShell + Dashboard global migration) this session:** the migration touches all 11 pages plus the `Dashboard.astro` layout, with high visual-regression surface that an autonomous gate-only run can't verify (no browser harness in `factory-web`; saved feedback says "if you can't test the UI, say so explicitly rather than claiming success"). Documented under steps.md "Deferred follow-ups" with explicit guidance: land in a session where the operator can spot-check pages in a browser as they convert.
- ADRs decided: none. ADR 0029 (directive-stream protocol) is now one operator smoke away from promotion — all 5 event types flow end-to-end after this session's 3.1b commit; the only remaining gate is "kick off a real build, observe all 5 event types in `/app/directives/detail` and `/app/chat`."
- Issues opened / closed: none.
- Minor fixes: `prettier --write` auto-fixed formatting on `packages/daemon/src/server.ts` after the cancel-route extraction edit (single-line change, non-content).
- Blockers hit: none. The cancel-route auth design call surfaced last session as a 3.6 design decision rather than a 3.5 blocker; the resolved-at-session-start pattern meant no rollback or rework happened mid-step.
- All four `pnpm` gates green at every commit and at session end. Workspace test count went +10 (brain +4 from `pool.test.ts`, daemon +6 from cancel-route tests). Workspace total now ~1063.
- Two deferred items remain on the 3.x backlog (no 3.7+ acceptance dependency): (i) PageShell + Dashboard global migration — operator-required for browser verification; (ii) Pause primitive — design when a workflow signal demands it. The third (`finding.created` brain emission, flagged as deferred in 3.5's journal entry) shipped this session in `f990323`. The pre-3.5 baseline live-smoke remains pinned for phase-3 acceptance.
- Loose ends still open from prior sessions (not blocking 3.7): synthetic smoke directive `01KQPDMQE6QTQZ3QMDD69019YK` + `demo-project` in DB; factoryd PID 32436 from a prior session may still be running.
- Next: step 3.7 — `/app/projects/new` page (browser mirror of `factory init <project>`). Two design decisions to resolve before code: (1) operator live-smoke first to close 3.6 acceptance + promote ADR 0029, or after; (2) form-submit route reuse vs new — depends on whether `factory init` server-side already exposes an HTTP path. Recommendation: smoke first (5 min, unblocks ADR 0029 + verifies cancel button in real conditions), then start 3.7 with the route audit.

## 2026-05-03 — Step 3.5 shipped (`/app/chat` page wired end-to-end with brain `log.line` emission)

- Step range 3.5 across `10efe20..2dfd25f` (4 step commits) plus a session-start drift reconcile (`d7a366c`).
- Session-start drift reconcile (`d7a366c`) — fourth occurrence of the post-session-end self-reference drift (`cce7065`/`db61baf`/`54c0f20` were the prior three). Operator chose option (b) at the session-start prompt: STATE.md "Last commit" caught up from `dfd1a07` (3.4 close) to HEAD's `1a845f5` (the prior session-end docs commit); reconcile commit body documents the steady-state lag-by-1 the runbook accepts and doesn't re-attempt the self-amend that corrupted the `54c0f20` reconcile last session. The `/session-end` skill fix flagged in step 3.4's journal entry remains worth doing — track "last work commit" rather than HEAD, or amend STATE.md post-commit — but stays unaddressed in 3.5 (out of step scope).
- **Step 3.5 spans 4 commits implementing chat end-to-end against a real factoryd:**
  - `10efe20` `feat(3.5): emit log.line from brain chat-path` — adds `emitLogLine` helper alongside `emitDirectiveCompleted` in `packages/brain/src/loop.ts` (same fire-and-forget shape, silent when no emitter wired); `runInline`'s chat branch emits a `log.line` event with `component: 'brain.chat'`, `msg: replyText`, `attrs: {intent, confidence}` before `directive.completed`. New `loop.test.ts` pins the emission shape (silent without emitter / well-formed event with emitter / attrs pass-through / attrs-undefined-omits-key). Brain test count 93 → 97.
  - `7fcbd10` `feat(3.5): POST /api/v1/chat/messages route + IPC schemas` — `apiV1ChatMessageRequestSchema { message: 1..8192, conversationId?: ulid }` + `apiV1ChatMessageResponseSchema { directive }` in `@factory5/ipc`; route in `packages/daemon/src/server.ts` mints `intent=chat` directive with `source='cli'` (loopback parity with `/api/v1/builds`), `principal='web-ui'` (audit-log distinction without expanding the channel-id enum), `channelRef='web-ui-${reqId}'` (multi-tab distinctness). +9 tests cover 401 / 503 UI_DISABLED / 400 SCHEMA_VALIDATION_FAILED on missing/empty/>8KB message / 400 on malformed conversationId / happy path with directive persistence + doorbell ring / conversationId stored on payload / multi-tab channelRef distinctness. Daemon test count 152 → 161.
  - `1fa06fa` `feat(3.5): /app/chat page — composer + history + markdown + /cmd shortcuts` — `apps/factory-web/src/pages/chat.astro`. Free-form messages POST `/api/v1/chat/messages` and subscribe to the directive's SSE stream rendering one bubble per `log.line` event filtered by `component === 'brain.chat'`. Slash-prefixed input dispatches client-side to existing `/api/v1/{status,spend,findings}` GETs (annotated `direct` rather than `via stream` on the bubble). Hand-rolled markdown for fenced code (extracted first to survive HTML escape) / inline code / bold / italic / paragraphs from blank lines / line-breaks from single newlines — ~30 LOC, no new dep. Auto-scroll-with-pause-on-user-scroll mirrors `directives/detail.astro:284-336` log-tail pattern. ⌘/Ctrl+Enter submits (textarea Enter remains for multi-line). Connection pip in the header reuses the directive-detail design language. Per-turn stream lifecycle: each user message closes any prior `apiStream`, opens a fresh one against the new directive, listens for log.line events; `astro:before-swap` teardown prevents leaked EventSources. Frontend-design skill invoked before authoring per saved feedback. Chat link added to dashboard nav between Build and Directives.
  - `2dfd25f` `refactor(3.5): close step 3.5 — /app/chat lands; CLI 120s gate stays` — flips `[ ] 3.5` → `[x]` in `phase-3-web-ui/steps.md` and `UPGRADE/ROADMAP.md`; documents the chat-page patterns (bubble layout, auto-scroll-pin, hand-rolled markdown subset, /cmd shortcut path, per-turn stream lifecycle) in `apps/factory-web/src/components/README.md` "Patterns introduced by /app/chat (3.5)" section so the next streaming-page author has a reference. Carried-forward 2.6 (`factory chat` per-turn timeout) closes implicitly: WEB chat page subscribes to SSE and sees per-token progress so the original false-timeout pain is addressed for the recommended chat surface. CLI's `TURN_TIMEOUT_MS=120_000` stays as-is intentionally — the cheap-bump path tier-2 §2.6 offered is no longer load-bearing now that the WEB surface exists, and removing the gate outright would regress to "CLI hangs on brain crash."
- ADRs decided: none. ADR 0029 (directive-stream protocol) still gated on FE consumer being smoke-tested end-to-end against a real factoryd; the chat-page surface in 3.5 is the second consumer of the SSE schemas after `directives/detail.astro` — the protocol is now exercised by two distinct page paths, which strengthens the case for promotion once the live smoke confirms wire fidelity.
- Issues opened / closed: none.
- Minor fixes: none.
- Blockers hit: none. The chat-route auth move from `/directives/:id/cancel` (no `/api/v1/` prefix) to a UI-token-gated namespace surfaced as a 3.6 design call rather than a 3.5 blocker — phase-3 steps.md line 8 frames cancel as hitting `POST /api/v1/directives/:id/cancel` but the existing route is at `/directives/:id/cancel`; deferred to 3.6 with the option ladder spelled out in STATE.md "Notes for next session." Pause is a similar deferred design call: phase-3 steps.md frames it as "a status flip via existing route" but no pause primitive exists today — STATE.md surfaces three options (extend directiveStatus, reuse markBlocked, defer pause) for 3.6's session start.
- All four `pnpm` gates green at every commit and at session end. Workspace test count went +13 (brain +4 from `loop.test.ts`, daemon +9 from chat-route tests). `factory-web` still has no vitest harness — chat.astro is integration-tested only by the daemon's chat-route tests + manual smoke. Visual-regression / live-smoke against a running factoryd is pinned as part of phase-3 acceptance, not a 3.5 prerequisite.
- Three deferred items remain on the 3.x backlog (no 3.6+ acceptance dependency): (i) PageShell + Dashboard `<style is:global>` follow-up (3.4 carryover); (ii) `finding.created` brain emission — the 3.1 deferred-work counterpart to `log.line` which shipped in `10efe20`; (iii) pre-3.5 baseline live-smoke against a running factoryd (open a real browser to `/app/directives/detail?id=…` AND `/app/chat` during a build run).
- Loose ends still open from prior session (not blocking 3.6): synthetic smoke directive `01KQPDMQE6QTQZ3QMDD69019YK` + `demo-project` in DB; factoryd PID 32436 may still be running.
- Next: step 3.6 — cancel + pause buttons on directive detail. Cancel is straightforward (existing IPC route from 2.4, FE handler, UI-token-gate move from `/directives/:id/cancel` → `/api/v1/directives/:id/cancel`). Pause is the open design call — STATE.md spells out three options (extend directiveStatus / reuse markBlocked / defer); recommendation is defer-pause-ship-cancel-only.

## 2026-05-03 — Step 3.4 shipped (all 10 pages converted to component library; el() retired)

- Step range 3.4 across `54c0f20..dfd1a07` (8 commits including a session-start drift reconcile).
- Session-start drift reconcile (`54c0f20`) — third occurrence of the post-session-end self-reference drift (`cce7065`/`db61baf` were the prior two): STATE.md "Last commit" pointed at `94b8b71` (the 3.3 feat) but actual HEAD was the `4466078` session-end docs commit. First reconcile attempt amended the SHA, which changed the commit's SHA and reproduced the drift; soft-reset and recommitted as a single non-self-referencing entry matching the established `db61baf` shape ("Last commit" → the session-end commit; "State reconcile" entry → the prior reconcile, not itself). Worth a small `/session-end` skill fix (track "last work commit" rather than HEAD, or amend STATE.md post-commit) to break the recurrence — flagged in the reconcile commit body, not addressed here.
- Step 3.4 spans 7 page-conversion commits + this final closing commit:
  - `32bdfb6` index.astro → `<Card>` × 5; first `id?` extension on a 3.3 component (the runtime-fetch placeholder pattern that turned out to be load-bearing for every list page).
  - `d55c41d` findings/index.astro + Table extension (`id?` + `loading?`); chrome rendered server-side, script replaces tbody on resolve, empty result → single colspan'd `<td class="empty">` row.
  - `a876608` projects/questions/spend list pages; projects empty-state hits dedicated `<Alert kind="info">` per migration map; spend page's four sub-tables share a per-page `fillTable<T>` helper.
  - `e849aa7` directives list + project/question detail pages; introduces the hidden-Alert-placeholder pattern for dynamic conflict/success swapping (one Alert per kind, server-rendered hidden, script reveals via `hidden=false` and writes textContent into inner h4/p).
  - `58d4584` build.astro → `<Form>` + `<Field>` + `<Submit>` (the primary form use case); project select `options={[]}` server-side, script appends one `<option>` per fetched project then rewrites the `Loading projects…` hint paragraph.
  - `a405556` directives/detail.astro inlines `el()` as a per-page helper (the live SSE render path's per-page DOM helper exception per the migration map).
  - `dfd1a07` retires `el()` and `loadInto()` from `lib/api.ts`; flips `[ ] 3.4` → `[x] 3.4` in steps.md and ROADMAP.md; documents the Dashboard-CSS scoping discovery + deferred PageShell decision in components/README.md.
- ADRs decided: none (refactor-only step).
- Issues opened / closed: none.
- Minor fixes: README format issue auto-fixed by `prettier --write` after the components/README.md edit; soft-reset of the failed self-amend reconcile attempt.
- Blockers hit: self-reference in the reconcile commit (described above). Found mid-conversion that Astro's scoped CSS doesn't propagate `data-astro-cid-*` to slot content — Dashboard's class-based rules (`.cards`, `.card`, `.empty`, `.err`, `.btn*`, `.alert*`, `.form-*`, table-base) only match elements rendered directly inside Dashboard's own template (just the `<header class="shell">` chrome and the inner `<h2>`). Decided not to prune those rules during 3.4 (they're already inert for slot content; pruning is safe but the future `<style is:global>` follow-up is what would actually make them load-bearing); recorded in components/README.md.
- All four `pnpm` gates green at every commit and at session end. Test count holds at the prior baseline (state 152, channels 175, daemon 152, brain 93, worker 38, worker-sandbox 86 + 3 skipped, assessor 79, wiki 64, cli 82, providers 39, ipc 28, events 3 — same as last STATE.md baseline of 1040 across 73 files). 3.4 added zero test files (page conversions; `factory-web` still has no vitest harness).
- Two items deferred during 3.4, filed as 3.x backlog rather than 3.5 blockers: (i) `<PageShell>` adoption across all 10 pages — coupled to removing Dashboard's inner `<h2>{title}</h2>` so we don't get double `<h2>`s; (ii) Dashboard primitives (`.cards`, `.empty`, `.err`, `.btn*`, etc.) → `<style is:global>` so they actually apply to slot-level elements. Both fit cleanly in one focused follow-up commit.
- Step 3.1 deferred work (still open): `finding.created` and `log.line` brain emission. 3.5 specifically needs `log.line` (one bubble per agent message in the chat page); pin `log.line` emission as part of 3.5's scope OR a 3.5-prerequisite mini-step.
- Loose ends from prior sessions still open (not blocking 3.5): synthetic smoke directive `01KQPDMQE6QTQZ3QMDD69019YK` + `demo-project` in DB; factoryd PID 32436 may still be running.
- Next: step 3.5 — `/app/chat` page (browser mirror of `factory chat`); reuses Phase 2's `command-handlers.ts` for `/cmd` shortcut path; SSE stream consumed via `apiStream` helper from 3.2.

## 2026-05-03 — Steps 3.2 + 3.3 shipped (SSE FE wiring + Astro component library)

- Step range 3.2-3.3 across `db61baf..94b8b71` (3 commits including a session-start drift reconcile).
- Session-start drift reconcile (`db61baf`) — same post-session-end pointer drift as `cce7065` last session: STATE.md "Last commit" pointed at `772f9f3` (the 3.1 feat) but actual HEAD was the `15bbad3` session-end docs commit. Caught by the SessionStart hook's `[control:drift] commit-mismatch` block; reconciled by updating STATE.md's Git state pointer + adding a new "Recently completed" entry for `15bbad3`. The reconcile commit itself reproduces the same one-behind shape — structural to the no-amend protocol.
- Step 3.2 (`998e7d8`) — `feat(3.2): wire directive detail page to SSE stream`. New `apiStream<T>(path, callbacks)` helper in `apps/factory-web/src/lib/api.ts`: token-auths via `?t=<UI_TOKEN>` query param (EventSource cannot set custom headers), Zod-validates each payload via `directiveStreamEventSchema`, surfaces a six-state connection machine (connecting → live → reconnecting → polling | disconnected → completed), degrades to a 5 s polling fallback when EventSource gives up retrying. `directives/detail.astro` rewritten: incremental `Map<taskId, Task>` render, atomic spend swap, log tail panel rendered only after the first `log.line` arrives, connection-state pip in the header (subtle pulse on green when live, blink amber when reconnecting, dashed grey when completed). Polling-fallback merge rule prefers existing live-event Map entries over snapshot rows so a brief polling window can't clobber a finer-grained `task.completed` status with a stale `running` row.
- New `./sse` sub-export on `@factory5/ipc` keeps `undici` / `@factory5/logger` out of the FE bundle: package.json gains the sub-export entry; tsup build script gains `src/sse.ts` as a second entry; emits `dist/sse.js` + `dist/sse.d.ts` alongside existing `index.js`. `apps/factory-web` adds `@factory5/ipc: workspace:*` to dependencies.
- Step 3.3 (`94b8b71`) — `feat(3.3): astro component library — Card / Table / EmptyState / Alert / Form / PageShell`. Eight new server-rendered components under `apps/factory-web/src/components/` (Card, Table, EmptyState, Alert, Form, Field, Submit, PageShell). Each ships with a typed `interface Props`, scoped `<style>` mirroring `Dashboard.astro`'s `color-mix(in srgb, currentColor X%, transparent)` palette + form/btn/alert/card class styles, and TSDoc'd intent. Component README documents conventions + per-component usage examples + the 3.4 migration map.
- 3.3 is **library-only** — no page consumes the components yet. Visual is unchanged because each component duplicates the matching CSS from `Dashboard.astro` under Astro's auto-scoping. The Dashboard-level CSS gets gutted in 3.4 once every page has migrated.
- Tests: 0 new tests this session. Workspace test count holds at 1040 across 73 files (the prior STATE.md's "76" was a slight miscount — actual is 73; the 3.1 session added `sse.test.ts` + `directive-stream-route.test.ts` for +2 files vs. earlier baseline, putting us at 73). The SSE FE wiring relies on the existing 152 daemon SSE-route tests for wire shape; `factory-web` has no vitest harness yet (deferred — adding jsdom + EventSource polyfill is a multi-file change for one test file's worth of value).
- ADRs decided: none. ADR 0029 (directive-stream protocol) still gated on FE consumer being smoke-tested end-to-end against a real factoryd, which lands as part of 3.4's acceptance smoke.
- Issues opened / closed: none.
- Minor fixes: textarea whitespace handling in `Field.astro` — Astro preserves whitespace inside `<textarea>` so a multi-line expression body would leak indent as the field's initial value; switched to `set:html` for the textarea body.
- Blockers hit: 11 ts(18047) "mount is possibly null" errors in `detail.astro` after the rewrite — TypeScript narrowing didn't propagate `if (mount === null) throw` into closure-scoped function bodies. Fixed by introducing a typed non-null local `const mount: HTMLElement = mountEl` after the throw guard. Single-line README format issue auto-fixed by `prettier --write`.
- All four `pnpm` gates green at every commit: build / test (1040 unchanged) / lint / format:check.
- Step 3.1 deferred work (still open): `finding.created` brain emission and `log.line` forwarder. The FE now has the listener wiring in place via 3.2 (Map mutation + log tail append, both no-op until events flow). Either land in a 3.x sub-step or defer to a follow-up tier — neither blocks 3.4.
- Loose ends still open from prior session (not blocking 3.4): synthetic smoke directive `01KQPDMQE6QTQZ3QMDD69019YK` + `demo-project` in DB; factoryd PID 32436 may still be running.
- Next: step 3.4 — convert all 10 pages in `apps/factory-web/src/pages/` to consume the component library; retire `el()` from `lib/api.ts`; gut the duplicated CSS from `Dashboard.astro`.

## 2026-05-03 — Step 3.1 shipped (SSE backend + brain emission)

- Step range 3.1 across `cce7065..772f9f3` (2 commits).
- Session-start drift reconcile (`cce7065`) — STATE.md "Last commit" pointed at the prior post-phase-2 docs commit (`f7c78ce`) but actual HEAD was the `733ce5a` session-end bookkeeping commit. Caught by the SessionStart hook's `[control:drift] commit-mismatch` block; reconciled by updating STATE.md's Git state pointer (the new commit reproduces the same one-behind shape, but it's structural — every STATE.md-only commit lands one behind itself unless you `--amend`, which CLAUDE.md forbids).
- Step 3.1 (`772f9f3`) — `feat(3.1): SSE on /api/v1/directives/:id/stream`. Six-event protocol pinned in `UPGRADE/specs/sse-directive-stream.md`. Six Zod event schemas + discriminated union in new `packages/ipc/src/sse.ts`. `DirectiveStreamHub` in `packages/daemon/src/directive-stream.ts` (subscribe / emit / closeDirective / shutdown; synchronous dispatch so a slow consumer can't backpressure the producer). Fastify SSE handler in `packages/daemon/src/directive-stream-route.ts` via `reply.hijack()` over `reply.raw` — auth via either `Authorization: Bearer …` or `?t=<token>` query param (browsers' EventSource cannot set headers); 404 / 401 / 503 pre-stream errors; backfill on connect synthesizes `task.started` (+ `task.completed` for terminal tasks) plus a baseline `spend.updated`; already-terminal directives short-circuit with one `directive.completed` and stream close; 15 s heartbeats; cleanup on `request.raw.on('close')`. Brain emission via new `BrainOptions.emitDirectiveEvent` callback typed as `DirectiveEventEmitter` from `@factory5/ipc` — daemon's `brain-supervisor` wires it to `hub.emit`; threaded through `runServe` → `defaultRunOne` → inline `runBrain` so both inline and serve paths emit. Pool emits `task.started` / `task.completed` / `spend.updated`; loop emits `directive.completed` at all six terminal-status branches (chat / non-build / architect-abort / planner-abort / normal / budget-exceeded).
- **Deferred from 3.1:** `finding.created` and `log.line` brain emission. Spec carries the event shapes, route forwards them, schemas + tests cover them. `finding.created` is a small follow-up (one event per entry in `outcome.result.findingsRaised` from `pool.ts`); `log.line` is more invasive (selective pino-stream tap) and may defer indefinitely. FE will refresh findings on each `task.completed` until `finding.created` is wired.
- Tests: 14 new schema cases in `packages/ipc/src/sse.test.ts` + 15 new end-to-end SSE route cases in `packages/daemon/src/directive-stream-route.test.ts` driving a real bound socket via Node's streaming `fetch` body (auth, 404, backfill on connect for in-flight + terminal tasks, already-terminal short-circuit + `blockedReason` forwarding, live `hub.emit` round-trip, directive scoping (events for one don't leak to another), heartbeat after idle with a small heartbeat interval, listener cleanup on client disconnect via `hub.listenerCount`).
- Workspace test count went from prior baseline to 1040 across 76 files (+29 new tests this session). Daemon 137 → 152; ipc 14 → 28. All four `pnpm` gates green.
- ADRs decided: none. Spec lives at `UPGRADE/specs/sse-directive-stream.md`; promotion to **ADR 0029 — directive-stream protocol** held until 3.2 ships and the FE consumer validates the contract end-to-end.
- Issues opened / closed: none. Step 3.1 was infrastructure-only.
- Minor fixes: added `.serena/` to `.gitignore` (Serena MCP server's `activate_project` drops a per-operator project descriptor there; not shared, not part of the build).
- Blockers hit: none. Two ESLint `prefer-const` errors during the lint gate from a `let unsubscribe` / `let heartbeatTimer` pattern in the SSE route handler — refactored to a `const subscription = { unsubscribe?: () => void }` holder so the late-bound subscription handle could be assigned without a `let` binding, and made `heartbeatTimer` `const` from the start.
- Step 2.6 (`factory chat` per-turn timeout) carryforward: infrastructure ready via this SSE stream; full resolution lands in 3.5 when `/app/chat` routes chat directives through it. Until then the 120 s false-timeout could still bite a chat directive that takes longer than 120 s for the first turn — same state as before this session.
- Loose ends still open from prior session (not blocking 3.2): synthetic smoke directive `01KQPDMQE6QTQZ3QMDD69019YK` + `demo-project` in DB; factoryd PID 32436 still running.
- Next: step 3.2 — wire `directives/detail.astro` to the SSE stream + add `apiStream<T>(path)` helper to `lib/api.ts`.

## 2026-05-03 — Session-end after phase-2 close + post-close docs polish

- Single small commit landed after `/phase-close`: `f7c78ce` `docs(2): document factory/factoryd PATH setup in ONBOARDING`. Closes a doc gap surfaced when operator asked "where do I run `factory daemon stop`?" — the existing onboarding doc said "once factory5 is on your `$PATH`" without ever explaining how to put it there.
- Working tree clean at session-end. HEAD = `f7c78ce`. Phase-2 tag on `081b832`.
- Synthetic smoke data still resident in DB; `packages/state/smoke-cleanup.mjs` available. Daemon (PID 32436) still running.
- No blockers. Next session opens at Phase 3.1 (SSE on `/api/v1/directives/:id/stream`).

## 2026-05-03 — Phase 2 closed (tag `phase-2-channel-parity-closed` on `081b832`); Phase 3 kicked off

- Steps 2.3, 2.4, 2.5 all shipped through prior-session work; Step 2.6 deferred to Phase 3 (folded into SSE streaming).
- This session: completed live-smoke checklist for `/phase-close` — Discord+Telegram slash command surfaces verified, free-form chat re-routing verified on both, `factory cancel` IPC route paths verified (NOT_FOUND/ALREADY_TERMINAL/OK), CLI exit codes 0/2/3 verified.
- Out-of-step UX fix: `fix(2.2): show project name in /status output` (`081b832`) — operator complaint that the recent-directives table showed only directive IDs, not which project they belonged to. Added a project column to Discord embed, Telegram HTML reply, and CLI table; new shared `makeProjectNameLookup` helper.
- Tag annotated with full phase-2 shipping summary.
- Phase 3 scaffold landed: `.control/phases/phase-3-web-ui/{README.md, steps.md}` with carry-forward of Step 2.6 in "Why this phase exists".
- Synthetic smoke directive remains in DB (id `01KQPDMQE6QTQZ3QMDD69019YK`, status=failed/cancelled). `packages/state/smoke-cleanup.mjs` available if user wants to reap; permission-gated via Bash settings.
- Gates: build / test (175 channels, 82 cli, full workspace ≥ 938) / lint / format:check all green.
- Next: Step 3.1 — SSE on `/api/v1/directives/:id/stream`.

## 2026-05-02 — Phase 2 session 2a — slash + setMyCommands

- Step range 2.1-2.2 across `8ea8e4a..22e0e54` (2 commits + this session-end).
- Step 2.1 (`8ea8e4a`) — Discord slash commands. New `discord-commands.ts` (single `/factory` command with seven subcommands: status / spend / findings / resume / cancel / budget / build). New `setProjectBudget` callback on `ChannelContext` + `SetProjectBudgetError` sentinel; daemon binds it over `wiki.updateProjectMetadata`. 23 unit tests added covering every subcommand, allow-list gate, and major error paths. Closes structural piece of U011 (live-smoke still required at phase-close).
- Step 2.2 (`22e0e54`) — Extracted transport-agnostic `command-handlers.ts` (each handler returns either typed data or `CommandResult<T>` for user-visible failures with a stable `code`). Refactored `discord-commands.ts` to delegate. Telegram side: added `setMyCommands` to `TelegramApi` (optional in contract; default HTTP factory provides), `parseMode` on `sendMessage`, slash dispatcher replacing the old `/build`-only parser, HTML-mode formatter with `<pre>` blocks for tabular reads. 10 new Telegram tests. Closes structural piece of U012 (live-smoke pending). Side effect: `payload.text` is no longer set on build directives — confirmed unused, dropped one roundtrip-test assertion.
- ADRs decided: none (judgement calls within tier-2 plan, not architectural).
- Issues opened / closed: U011 + U012 partially addressed (structural pieces shipped; live-smoke acceptance held until `/phase-close`).
- Minor fixes: `.claude/hooks/regenerate-next-md.ps1` UTF-8 round-trip fix — read with `-Encoding utf8`, write without BOM via `UTF8Encoding $false` for parity with the bash sibling. Logged as a known bug at end of step 2.0; fixed during the 2.2-2.3 idle window per the "Phase 2 idle" note in last STATE.md.
- Blockers hit: none.
- Gates: build / test (938 total, 103/103 channels) / lint / format:check all green.
- Next: step 2.3 — pending-question button affordances.

## 2026-05-02 — Phase 1 closed; Phase 2 kicked off
- Phase 1 (doc-sweep) closed: tag `phase-1-doc-sweep-closed` on `10e400a`; close commit `1384ae8`.
- Step range 1.1-1.8 across `91541a9..10e400a` (9 commits, doc-only).
- Issues closed: U001, U002, U003, U014, U015, U016, U017 (7 of 23 catalogued; all Tier-1).
- ADRs decided: none (doc-only phase, as anticipated).
- Minor fixes: orphan `factory inspect` reference removed from `packages/logger/README.md` while sweeping for broken refs.
- Blockers hit: none. Hook drift at session start (commit-mismatch + tag-mismatch) reconciled via `91541a9` before any step work began.
- All four `pnpm` gates green throughout (build, test 876p/3s, lint, format:check).
- Next: step 2.1 — wire Discord slash commands.

## 2026-05-02 — Session bootstrap
- Control framework v2.2.1 installed (commit `e94393e`); `/bootstrap` populated SPEC + phase plan + Phase 1 scaffold.
- Next: Phase 1 doc-sweep.
