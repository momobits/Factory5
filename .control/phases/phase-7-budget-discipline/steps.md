# Phase 7 Steps — Budget discipline

> **Placeholder.** Detailed 7a sub-step bodies are authored at the start
> of the 7a session, after the pre-call cost-estimate ADR is decided.
> 7b and 7c placeholders expand when their sessions open.

## Phase 7a — Budget enforcement

- [x] 7a.1 — ADR: pre-call cost estimate approach (how `brain` knows whether the next LLM call will exceed `max_usd`) — [ADR 0020](../../../docs/decisions/0020-pre-call-budget-enforcement.md)
- [x] 7a.2 — `@factory5/state` — running-total query over `model_usage` by directive (`countForDirective` + `averageCostByCategory` + `mode` column via migration 004)
- [x] 7a.3 — `@factory5/providers` — expose per-call cost estimate — **closed as no-op per ADR 0020** (estimator lives in `@factory5/state` + `@factory5/brain`; providers stay dumb about budgets)
- [x] 7a.4 — `@factory5/brain` — pre-call ceiling check in the main loop; halt + escalate when exceeded — migration 005 for `directives.max_usd` / `max_steps`; `budget.ts` module with `assertBudget` / `BudgetExceededError` / `DEFAULT_CATEGORY_COST`; wrappers wired into triage / architect / planner / pool; `loop.ts` catches + flips directive to `blocked` with `formatBlockedReason`
- [x] 7a.5 — `@factory5/cli` — `--max-usd <N>` / `--max-steps <N>` flags on `factory build` write through to `directive.limits`
- [x] 7a.6 — Config defaults in `~/.factory5/config.toml` `[budget.defaults]` (maxUsd / maxSteps). CLI flag wins over config default; both absent = unlimited
- [x] 7a.7 — Regression test: synthetic build hits `max_usd` → clean escalation (not mid-task half-failure). `packages/brain/src/budget-regression.test.ts` covers maxUsd trip (pre-seeded model_usage), maxSteps trip, and the under-budget happy path
- [x] 7a.8 — Live validation: `factory build example --max-usd 3` tripped cleanly at builder-2 dispatch. Directive `01KPRHNEX1T3VR3S4ZTTSJ8F0M`, spent $1.9151 of $3 ceiling, blocked_reason `budget_exceeded_usd: ...`, 5 model_usage rows recorded. Phase 6c $7.71 overshoot not reproducible.
- [x] 7a.9 — Close Phase 7a (tag `phase-7a-budget-enforcement-closed`)

## Phase 7b — Cross-session spend dashboard

- [x] 7b.1 — Data-model prep: [ADR 0021](../../../docs/decisions/0021-first-class-project-identity.md) — first-class project identity via `<project>/.factory/project.json` (ULID, stable across path moves) + migration 006 (`projects.id` PK; `directives.project_id`; `findings_registry` PK rebased on ULID; `learnings.source_project` migrated) + `loadOrCreateProjectMetadata` helper + insert-path wiring + backfill + tests. Commit `92bebf4`. Closes [I008](../../../docs/issues/I008-findings-registry-project-id-collision.md). 375 tests across 13 packages (+28 over Phase 7a close).
- [x] 7b.2 — `@factory5/state.queries.spend` — aggregations by project / directive / day / model (joins `model_usage → directives.project_id`). `perProject` / `perDirective` / `perDay` / `perModel` + `formatProjectDisplay` helper + `SpendFilter` (`since` / `until` / `projectId`). Per-project uses LEFT JOIN through directives/projects so rows with no project collapse into a single `(unassigned)` bucket. 23 new tests (92 state / 398 workspace) covering the I008 regression (two `example` projects with distinct ULIDs surface as distinct rows), filters, orderings, UTC date bucketing.
- [x] 7b.3 — `factory spend` CLI subcommand with filters (`--since`, `--project`, `--group-by`). Subcommand + `runSpend` pure handler + `parseWindowArg` + `resolveProjectRef`. `--group-by project|directive|day|model` (default `project`). `--since` / `--until` accept relative (`7d`/`24h`/`30m`) or ISO8601; rejects bare numeric strings. `--project` accepts full ULID, name, or id suffix (case-insensitive); ambiguous name errors with disambiguation list. `--json` emits NDJSON. `--limit` defaults 50, caps 1000. Live-smoke: 116 calls / $63.17 rolled up across 2 projects + `(unassigned)`. 24 new CLI tests (49 CLI / 422 workspace).
- [x] 7b.4 — Round-trip test: seed two builds in separate workspaces sharing project basename `example` (each with its own `.factory/project.json`) → query dashboard → both projects appear distinctly with correct totals; raw `model_usage` matches per-project rollup. `packages/cli/src/commands/spend-roundtrip.test.ts` — two tmp workspaces, `loadOrCreateProjectMetadata` writes distinct-ULID identity files, directives + model_usage seeded for each, `runSpend` asserts both `example` rows visible with different suffixes, per-project `totalUsd` matches `totalCostForDirective`, `--project <ulid>` / `--project <suffix>` isolate individually, `--project example` hits ambiguity path. I008 regression coverage at the CLI layer. 6 new tests (55 CLI / 428 workspace).
- [x] 7b.5 — Close Phase 7b (tag `phase-7b-spend-dashboard-closed`). `docs/Phase7_Progress.md` 7b section authored and row flipped ✅; `docs/PROGRESS.md` session entry appended; 428 tests / 13 packages green. Tag placed on this commit.

## Phase 7c — Telegram channel

- [x] 7c.1 — **[HALT] cleared** — operator provided Telegram bot token + private chat-id. Persisted to `%LOCALAPPDATA%\factory5\config.toml` under `[channels.telegram]` with keys `botToken` + `testChatId`. Stale `/start` update consumed via `getUpdates?offset=<id+1>` so it doesn't replay at 7c.6 live run. Bot username `@Factory5_bot`; 7c.4 formalises the final config shape and adds `--telegram-*` flags to `factory init` so onboarding stays one-shot.
- [x] 7c.2 — `packages/channels/src/telegram.ts` implementing `ChannelPlugin` (Discord is the reference — ADR 0019 dropped the GitHub channel). `TelegramChannel` with Zod `telegramConfigSchema` (`botToken`, `allowedChatIds`, `buildPrefix`, `pollTimeoutSec`, `testChatId`), `TelegramApi` interface + `defaultTelegramApiFactory` (raw `fetch`, no SDK), private-chat vs group-chat scoping via `isDirectedAtBot`, pending-question answer routing by `reply_to_message_id`, `channelRef = <chatId>#<messageId>` so `send()` can thread via reply-to. 29 new tests (54 channels / 457 workspace). Commit `ef650af`.
- [x] 7c.3 — **Closed as no-op per [ADR 0022](../../../docs/decisions/0022-telegram-polling-in-plugin.md).** Long-polling loop lives inside `TelegramChannel` (AbortController-cancellable, offset-maintained, exponential backoff cap 30s) mirroring Discord's websocket-inside-plugin pattern. Splitting the loop across `@factory5/events` and the plugin turned out to be premature layering for the 1:1 update-to-directive case. If future Telegram signals need `Event` rather than `Directive` treatment, we revisit; YAGNI today.
- [x] 7c.4 — State config for bot-token + allowed-chats allowlist. `telegramConfigSchema` locked in at 7c.2 (`botToken` required, `allowedChatIds` array defaulted `[]` = accept-any, `buildPrefix` default `/build`, `pollTimeoutSec` 0–60 default 30, `testChatId` optional). `factory init` gained `--telegram-token`, `--telegram-allowed-chat` (repeatable collector), `--telegram-test-chat`, `--telegram-poll-timeout-sec` flags with integer parsing + range check. `factory doctor` gained a Telegram probe (hits `getMe` via `defaultTelegramApiFactory`) + `--skip-telegram` flag. Daemon's `buildDefaultChannelPlugins` auto-wires the Telegram plugin when `[channels.telegram].botToken` is set. `@factory5/cli` now depends on `@factory5/channels` so `doctor` can reach the shared api factory. Live smoke: `factory doctor --skip-call --skip-discord` reports `getMe: ok (token accepted)` / `bot: @Factory5_bot` / `testChatId: 1225367797`. `pnpm-lock.yaml` now in `.prettierignore` (auto-generated, shouldn't be formatted).
- [ ] 7c.5 — Round-trip integration test using recorded fixtures
- [ ] 7c.6 — Live run against user-provided test chat
- [ ] 7c.7 — Close Phase 7c (tag `phase-7c-telegram-channel-closed`)

## Phase 7 close

- [ ] Phase-level close: `docs/Phase7_Progress.md` charter complete, 3 sub-phases ✅, tag `phase-7-closed`
