# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.tsbuildinfo

# Astro's generated type cache (apps/factory-web/.astro/) — regenerated on
# every `astro check` / `astro build`; never commit.
.astro/

# Coverage
coverage/
.nyc_output/

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# Environment
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/

# Caches
.turbo/
.cache/
.eslintcache
.parcel-cache/
.tsbuildinfo

# Generated
docs/api/

# Local SQLite databases — factory's runtime state lives in a repo-local
# `.factory/` (see below) or `~/.factory/` fallback; neither should be
# committed.
*.db
*.db-journal
*.db-wal
*.db-shm

# Repo-local factory instance — holds config.toml (Discord + Telegram bot
# tokens), factory.db (spend history + directives), and logs/. NEVER commit
# — one `git add -A` would leak tokens to git history.
.factory/

notes.md

# --- Control framework ---
.control/snapshots/
.control/.is-source-repo
.claude/settings.local.json
# --- /Control ---

# Per-operator MCP server scratch (Serena's activate_project drops a
# project descriptor here; not shared, not part of the build).
.serena/
